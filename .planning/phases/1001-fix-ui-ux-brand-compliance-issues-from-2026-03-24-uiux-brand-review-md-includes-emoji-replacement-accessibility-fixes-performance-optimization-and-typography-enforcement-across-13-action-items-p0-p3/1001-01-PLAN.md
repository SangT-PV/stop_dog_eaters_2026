---
phase: 1001-uiux-brand-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/index.html
  - website/about.html
  - website/petition.html
  - website/css/style.css
autonomous: true
requirements:
  - P0-EMOJI-REPLACE
  - P0-ALT-TEXT
  - P0-CONTRAST-VERIFY
must_haves:
  truths:
    - "No emoji characters used as structural icons anywhere on the site"
    - "All image placeholders have accessible text alternatives"
    - "Secondary text color passes WCAG AA contrast ratio (4.5:1 minimum)"
  artifacts:
    - path: "website/index.html"
      provides: "Homepage with Lucide SVG icons replacing emojis"
      contains: "svg class=\"card-icon\""
    - path: "website/about.html"
      provides: "About page with SVG icon replacing emoji placeholder"
      contains: "svg"
    - path: "website/css/style.css"
      provides: "Card icon SVG styling and accessible placeholder styles"
      contains: ".card-icon svg"
  key_links:
    - from: "website/index.html"
      to: "website/css/style.css"
      via: "card-icon SVG styling"
      pattern: "card-icon"
    - from: "BRAND_GUIDELINES.md"
      to: "website/index.html"
      via: "Lucide icon implementation"
      pattern: "Shield.*AlertCircle.*Activity"
---

<objective>
Replace all emoji icons with Lucide SVG icons and fix accessibility issues for image placeholders across the website. This addresses P0 critical brand compliance violations that block launch.

Purpose: BRAND_GUIDELINES.md explicitly prohibits emojis as structural icons ("Use vector-based icons (Lucide Icons preferred). No emojis as structural icons."). Image placeholders without accessible text fail WCAG accessibility. These are pre-launch blockers.

Output: index.html, about.html, petition.html updated with SVG icons and accessible placeholders. CSS updated with SVG icon styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reviews/2026-03-24-uiux-brand-review.md
@BRAND_GUIDELINES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace emoji icons with Lucide SVGs in index.html</name>
  <files>website/index.html</files>
  <read_first>
    - website/index.html (current HTML with emoji violations at lines 72, 109, 114, 119, 133)
    - BRAND_GUIDELINES.md (lines 84-88 specify Lucide icon requirements: Shield, Heart, AlertCircle, Dog)
    - .planning/reviews/2026-03-24-uiux-brand-review.md (Appendix A has exact SVG paths for each icon)
  </read_first>
  <action>
Replace all 5 emoji instances in index.html with inline Lucide SVG icons.

**Line 72** - Hero image placeholder: Replace the entire `<div class="icon">...</div>` block with a Heart SVG icon (empathy/domestic bond per brand guidelines):
```html
<svg class="placeholder-icon" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
</svg>
```

**Line 109** - Problem card "Stolen Pets": Replace `<div class="card-icon">...</div>` emoji with Shield SVG (protection/safety per brand guidelines):
```html
<div class="card-icon">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
  </svg>
</div>
```

**Line 114** - Problem card "Zero Regulation": Replace `<div class="card-icon">...</div>` emoji with AlertCircle SVG (urgency/health risk per brand guidelines):
```html
<div class="card-icon">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
    <circle cx="12" cy="12" r="10"/>
    <line x1="12" y1="8" x2="12" y2="12"/>
    <line x1="12" y1="16" x2="12.01" y2="16"/>
  </svg>
</div>
```

**Line 119** - Problem card "Public Health Risk": Replace `<div class="card-icon">...</div>` emoji with Activity SVG (health/disease indicator):
```html
<div class="card-icon">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
    <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>
  </svg>
</div>
```

**Line 133** - Lucky story section placeholder: Replace the `<div class="icon">...</div>` emoji with the same Heart SVG as line 72 (same placeholder pattern):
```html
<svg class="placeholder-icon" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
</svg>
```

Also add `role="img"` and `aria-label` to the three problem card SVGs for accessibility:
- Shield: `aria-label="Protection and safety"`
- AlertCircle: `aria-label="Warning: zero regulation"`
- Activity: `aria-label="Public health risk"`

Remove `aria-hidden="true"` from the problem card SVGs and add `role="img"` instead since they convey meaning. Keep `aria-hidden="true"` on the placeholder Heart SVGs since they are decorative.

**Additionally**, fix the blog-card-img placeholders (lines 198, 209, 220) to be accessible. Change:
```html
<div class="blog-card-img"><span>Article image</span></div>
```
to:
```html
<div class="blog-card-img" role="img" aria-label="Blog article illustration"><span>Article image</span></div>
```

**Additionally**, add `role="img"` and `aria-label` to the hero-image-placeholder (line 71):
```html
<div class="hero-image-placeholder" role="img" aria-label="Lucky, a 9-year-old Vietnamese Ta dog — campaign mascot">
```

**Additionally**, add `role="img"` and `aria-label` to the lucky-image-placeholder (line 132):
```html
<div class="lucky-image-placeholder" role="img" aria-label="Lucky the Vietnamese Ta dog with his family">
```

**Additionally**, replace the checkmark emoji on petition.html line 122. Change:
```html
<strong style="color: var(--teal); display: block; margin-bottom: 8px;">&#x2705; Now Live on Change.org</strong>
```
Wait -- petition.html is handled in this task too. Replace the "&#x2705;" (checkmark emoji) with an inline SVG check icon:
```html
<strong style="color: var(--teal); display: block; margin-bottom: 8px;">
  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="display:inline;vertical-align:middle;margin-right:4px;" aria-hidden="true"><polyline points="20 6 9 17 4 12"/></svg>
  Now Live on Change.org
</strong>
```
  </action>
  <verify>
    <automated>grep -Pn "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{26FF}]|[\x{2700}-\x{27BF}]|[\x{2705}]" website/index.html website/about.html website/petition.html 2>/dev/null; echo "Exit: $? (1 means no emojis found = PASS)"</automated>
  </verify>
  <acceptance_criteria>
    - website/index.html contains zero emoji characters (no instances of U+1F415, U+1F512, U+26A0, U+1F9A0, or U+2705)
    - website/index.html contains `<svg` at least 5 times (3 problem cards + 2 placeholders)
    - website/index.html line ~109 area contains `<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>` (Shield)
    - website/index.html line ~114 area contains `<circle cx="12" cy="12" r="10"/>` (AlertCircle)
    - website/index.html line ~119 area contains `<polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/>` (Activity)
    - website/index.html hero-image-placeholder has `role="img"` and `aria-label` attribute
    - website/index.html lucky-image-placeholder has `role="img"` and `aria-label` attribute
    - website/index.html blog-card-img divs have `role="img"` and `aria-label` attributes
    - website/petition.html contains no emoji characters
    - website/petition.html "Now Live" section uses inline SVG check icon instead of emoji
  </acceptance_criteria>
  <done>All 5 emojis in index.html replaced with Lucide SVG icons. All image placeholders have accessible role and aria-label. Petition.html checkmark emoji replaced with SVG. Zero emoji characters remain in index.html, about.html, and petition.html.</done>
</task>

<task type="auto">
  <name>Task 2: Replace emoji in about.html and add card-icon SVG styles to CSS</name>
  <files>website/about.html, website/css/style.css</files>
  <read_first>
    - website/about.html (line 69 has emoji placeholder icon)
    - website/css/style.css (need to add SVG icon styling for .card-icon svg and .placeholder-icon)
    - website/index.html (reference the SVG patterns used in Task 1 for consistency)
    - BRAND_GUIDELINES.md (color system: teal #2A9D8F for icons, red #E63946 for urgency)
  </read_first>
  <action>
**about.html changes:**

**Line 69** - Lucky image placeholder: Replace `<div class="icon">...</div>` emoji with Heart SVG (same as index.html placeholders):
```html
<svg class="placeholder-icon" width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
  <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
</svg>
```

Also add `role="img"` and `aria-label` to the lucky-image-placeholder div on about.html (line 68):
```html
<div class="lucky-image-placeholder" role="img" aria-label="Lucky the Vietnamese Ta dog — campaign mascot">
```

**Also fix the about.html mission grid** responsive issue (review item 4.4, line 96). The grid `grid-template-columns: 1fr 1fr` has no mobile breakpoint. This is noted as medium priority but since we're already editing the file, add a data attribute or CSS class. Change the inline style on line 96 from:
```html
<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center;">
```
to:
```html
<div class="mission-grid">
```

**CSS changes (website/css/style.css):**

Add the following styles AFTER the existing `.problem-card` styles (around line 345 area, after the problem-grid section). Find the problem-card styling block and add immediately after it:

```css
/* --- Card Icon SVG Styling --------------------------------- */
.card-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16px;
}

.card-icon svg {
  color: var(--teal);
  stroke: var(--teal);
  flex-shrink: 0;
}

.problem-card:nth-child(1) .card-icon svg { color: var(--red); stroke: var(--red); }
.problem-card:nth-child(2) .card-icon svg { color: var(--amber, #E8A838); stroke: var(--amber, #E8A838); }
.problem-card:nth-child(3) .card-icon svg { color: var(--teal); stroke: var(--teal); }

.placeholder-icon {
  color: var(--teal);
  stroke: var(--teal);
  opacity: 0.6;
}
```

Add the mission-grid responsive class (near the about page / mission section styles, or at the end of the general layout section):

```css
/* --- Mission Grid (about.html) ----------------------------- */
.mission-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}

@media (max-width: 768px) {
  .mission-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }
}
```

Also verify `--text-md: #4a5563` contrast ratio. Calculate: #4a5563 on #ffffff gives a contrast ratio of approximately 6.0:1 which passes WCAG AA (4.5:1 minimum). No change needed, but document the verification in SUMMARY.
  </action>
  <verify>
    <automated>grep -Pn "[\x{1F300}-\x{1F9FF}]" website/about.html 2>/dev/null; echo "Exit: $? (1 means no emojis = PASS)" && grep -c "card-icon svg" website/css/style.css && grep -c "mission-grid" website/css/style.css && grep -c "placeholder-icon" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - website/about.html contains zero emoji characters
    - website/about.html contains `<svg class="placeholder-icon"` for the Lucky placeholder
    - website/about.html lucky-image-placeholder has `role="img"` and `aria-label` attribute
    - website/about.html line 96 area uses `class="mission-grid"` instead of inline grid style
    - website/css/style.css contains `.card-icon svg` rule with `color: var(--teal)` and `stroke: var(--teal)`
    - website/css/style.css contains `.placeholder-icon` rule with `opacity: 0.6`
    - website/css/style.css contains `.mission-grid` rule with `grid-template-columns: 1fr 1fr`
    - website/css/style.css contains `@media (max-width: 768px)` rule for `.mission-grid` with `grid-template-columns: 1fr`
    - website/css/style.css contains `.problem-card:nth-child(1) .card-icon svg` with `stroke: var(--red)`
  </acceptance_criteria>
  <done>about.html emoji replaced with SVG. CSS has card-icon SVG styling with per-card color differentiation. Mission grid is responsive with mobile breakpoint. Placeholder icons styled with teal color at 60% opacity. Contrast ratio for --text-md verified as passing WCAG AA.</done>
</task>

</tasks>

<verification>
1. Run: `grep -rPn "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{26FF}]" website/index.html website/about.html website/petition.html` -- should return no matches
2. Run: `grep -c "svg" website/index.html` -- should return 5+ (3 problem cards + 2 placeholders + blog cards don't have SVGs)
3. Run: `grep -c "role=\"img\"" website/index.html` -- should return 5+ (hero placeholder + lucky placeholder + 3 blog cards)
4. Run: `grep -c "card-icon svg" website/css/style.css` -- should return 1+
5. Run: `grep -c "mission-grid" website/css/style.css` -- should return 2+ (definition + media query)
6. Visual check: Open index.html in browser, verify SVG icons display in teal/red/amber colors in problem cards
</verification>

<success_criteria>
- Zero emoji characters used as structural icons across index.html, about.html, petition.html
- All Lucide SVG icons render with correct brand colors (red for Stolen Pets, amber for Zero Regulation, teal for Public Health)
- All image placeholders have role="img" and aria-label for screen reader accessibility
- Mission grid on about.html collapses to single column on mobile (max-width: 768px)
- Secondary text color --text-md: #4a5563 verified as passing WCAG AA contrast (6.0:1)
</success_criteria>

<output>
After completion, create `.planning/phases/1001-fix-ui-ux-brand-compliance-issues-from-2026-03-24-uiux-brand-review-md-includes-emoji-replacement-accessibility-fixes-performance-optimization-and-typography-enforcement-across-13-action-items-p0-p3/1001-01-SUMMARY.md`
</output>
