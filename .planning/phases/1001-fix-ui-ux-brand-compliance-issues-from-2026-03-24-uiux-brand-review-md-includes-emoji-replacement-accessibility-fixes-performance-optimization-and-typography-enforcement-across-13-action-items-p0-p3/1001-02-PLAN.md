---
phase: 1001-uiux-brand-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website/css/style.css
  - website/blog.html
  - website/js/main.js
autonomous: true
requirements:
  - P1-LOADING-STATES
  - P2-TOUCH-ACTION
  - P2-EMPTY-STATES
  - P2-COLOR-ALIAS-CLEANUP
must_haves:
  truths:
    - "Blog listing shows a loading spinner while articles fetch"
    - "Blog listing shows an empty state message when no articles match a filter"
    - "Buttons and links have touch-action: manipulation to eliminate 300ms tap delay on mobile"
    - "Backward-compat color aliases are documented with deprecation intent"
  artifacts:
    - path: "website/css/style.css"
      provides: "Loading spinner animation, empty state styling, touch-action rules"
      contains: "@keyframes spin"
    - path: "website/blog.html"
      provides: "Blog with loading spinner SVG and empty state element"
      contains: "spinner"
    - path: "website/js/main.js"
      provides: "Empty state handling when tag filter returns zero posts"
  key_links:
    - from: "website/blog.html"
      to: "website/css/style.css"
      via: "spinner and empty-state CSS classes"
      pattern: "spinner.*empty-state"
    - from: "website/js/main.js"
      to: "website/blog.html"
      via: "empty state display toggle"
      pattern: "empty-state"
---

<objective>
Add loading states, empty states, touch interaction fixes, and prepare color token cleanup. This addresses P1 (loading states) and P2 (touch-action, empty states, color aliases) from the UI/UX review.

Purpose: Users currently see raw "Loading articles..." text with no visual indicator. Empty filter results show nothing. Mobile users experience 300ms tap delay on buttons. These are UX issues that hurt engagement and perceived performance.

Output: CSS with spinner animation and empty state styles. blog.html with loading spinner SVG. main.js with empty state logic. Touch-action applied globally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reviews/2026-03-24-uiux-brand-review.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add loading spinner, empty states, and touch-action to CSS</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (full file to understand existing structure and find correct insertion points)
    - .planning/reviews/2026-03-24-uiux-brand-review.md (Section 2.2 touch-action, Section 2.3 loading states, Section 3 skeleton screens)
    - website/blog.html (lines 97-104 to understand current loading markup structure)
  </read_first>
  <action>
Add the following CSS blocks to website/css/style.css. Insert them in logical locations within the existing file structure.

**1. Touch-action fix (add near the button styles section, after the existing `.btn` rules around line 120):**

```css
/* --- Touch Optimization ------------------------------------ */
button, .btn, a[href], input[type="submit"], input[type="button"],
[role="button"], .sidebar-tag, .view-btn, .nav-toggle {
  touch-action: manipulation;
}
```

**2. Loading spinner (add after the entrance animations section, around line 104):**

```css
/* --- Loading Spinner --------------------------------------- */
.loading-spinner {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  padding: 48px 0;
  color: var(--text-md);
  font-size: 0.9rem;
}

.loading-spinner svg {
  color: var(--teal);
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}

.loading-spinner svg {
  animation: spin 1s linear infinite;
}
```

**3. Empty state (add after the loading spinner rules):**

```css
/* --- Empty State ------------------------------------------- */
.empty-state {
  text-align: center;
  padding: 64px 24px;
  color: var(--text-md);
}

.empty-state svg {
  color: var(--border-dk);
  margin-bottom: 16px;
}

.empty-state h3 {
  font-size: 1.1rem;
  color: var(--slate);
  margin-bottom: 8px;
}

.empty-state p {
  font-size: 0.9rem;
  max-width: 360px;
  margin: 0 auto;
}
```

**4. Color alias deprecation comment (update the existing backward-compat aliases comment in :root, around lines 27-33):**

Change the comment from:
```css
  /* Backward-compat aliases (used in inline styles) */
```
to:
```css
  /* Backward-compat aliases — DEPRECATED: migrate to semantic tokens (--slate, --red, --teal, --mist) */
  /* --navy maps to --slate, --offwhite maps to --mist, --amber has no brand equivalent */
```

Do NOT remove the aliases yet (they are used in inline styles across multiple HTML files). Just update the comment to signal deprecation intent for future cleanup.
  </action>
  <verify>
    <automated>grep -c "touch-action: manipulation" website/css/style.css && grep -c "@keyframes spin" website/css/style.css && grep -c "empty-state" website/css/style.css && grep -c "DEPRECATED" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - website/css/style.css contains `touch-action: manipulation` applied to `button, .btn, a[href]` and other interactive elements
    - website/css/style.css contains `@keyframes spin` animation rule
    - website/css/style.css contains `.loading-spinner` class with `display: flex` and `align-items: center`
    - website/css/style.css contains `.loading-spinner svg` with `animation: spin 1s linear infinite`
    - website/css/style.css contains `.empty-state` class with `text-align: center` and `padding: 64px 24px`
    - website/css/style.css contains `.empty-state h3` with `font-size: 1.1rem`
    - website/css/style.css backward-compat aliases comment contains "DEPRECATED" and "migrate to semantic tokens"
  </acceptance_criteria>
  <done>CSS has touch-action for all interactive elements, loading spinner with @keyframes spin animation, empty state styling with centered layout, and backward-compat aliases marked as deprecated with migration guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Add loading spinner SVG and empty state markup to blog.html</name>
  <files>website/blog.html, website/js/main.js</files>
  <read_first>
    - website/blog.html (lines 95-105 current loading text markup, lines 107-138 sidebar structure)
    - website/js/main.js (to understand if tag filtering already exists and where to add empty state logic)
    - website/js/blog-timeline.js (if it exists, to understand how blog posts are loaded and rendered)
  </read_first>
  <action>
**blog.html changes:**

**Replace the timeline loading text (line 98)** from:
```html
<p id="timeline-loading" style="color:var(--text-md); padding:40px 0;">Loading articles...</p>
```
to:
```html
<div id="timeline-loading" class="loading-spinner">
  <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
    <circle cx="12" cy="12" r="10" opacity="0.25"/>
    <path d="M12 2a10 10 0 0 1 10 10" stroke-linecap="round"/>
  </svg>
  Loading articles...
</div>
```

**Replace the grid loading text (line 103)** from:
```html
<p id="blog-loading" style="color:var(--text-md); padding:40px 0;">Loading articles...</p>
```
to:
```html
<div id="blog-loading" class="loading-spinner">
  <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true">
    <circle cx="12" cy="12" r="10" opacity="0.25"/>
    <path d="M12 2a10 10 0 0 1 10 10" stroke-linecap="round"/>
  </svg>
  Loading articles...
</div>
```

**Add empty state elements** after each loading element. After the timeline loading div (inside `#blog-timeline`):
```html
<div id="timeline-empty" class="empty-state" style="display:none;">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
    <circle cx="11" cy="11" r="8"/>
    <line x1="21" y1="21" x2="16.65" y2="16.65"/>
  </svg>
  <h3>No articles found</h3>
  <p>No articles match your selected filter. Try selecting a different topic or choose "All" to see everything.</p>
</div>
```

After the grid loading div (inside `#blog-list`):
```html
<div id="grid-empty" class="empty-state" style="display:none;">
  <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
    <circle cx="11" cy="11" r="8"/>
    <line x1="21" y1="21" x2="16.65" y2="16.65"/>
  </svg>
  <h3>No articles found</h3>
  <p>No articles match your selected filter. Try selecting a different topic or choose "All" to see everything.</p>
</div>
```

**main.js or blog-timeline.js changes:**

Check if `blog-timeline.js` handles the tag filtering and post rendering. If so, modify that file instead of main.js. The logic needed is:

After the tag filter renders posts (when a sidebar-tag button is clicked), check how many posts are visible. If zero posts match the filter, show the empty state:

```javascript
// After filtering posts by tag:
const timelineEmpty = document.getElementById('timeline-empty');
const gridEmpty = document.getElementById('grid-empty');
if (visibleCount === 0) {
  if (timelineEmpty) timelineEmpty.style.display = 'block';
  if (gridEmpty) gridEmpty.style.display = 'block';
} else {
  if (timelineEmpty) timelineEmpty.style.display = 'none';
  if (gridEmpty) gridEmpty.style.display = 'none';
}
```

Find the exact location in blog-timeline.js where posts are filtered by tag and add this empty state toggle there. If the filtering is done by hiding/showing DOM elements, count the visible ones. If it's done by re-rendering, check the filtered array length.

NOTE: Read blog-timeline.js first to understand the exact filtering mechanism. Do NOT blindly add code -- find the right integration point.
  </action>
  <verify>
    <automated>grep -c "loading-spinner" website/blog.html && grep -c "empty-state" website/blog.html && grep -c "timeline-empty\|grid-empty" website/blog.html</automated>
  </verify>
  <acceptance_criteria>
    - website/blog.html contains `class="loading-spinner"` in both timeline and grid loading elements
    - website/blog.html contains `<svg` inside the loading spinner divs with circle and path elements
    - website/blog.html contains `id="timeline-empty"` div with `class="empty-state"`
    - website/blog.html contains `id="grid-empty"` div with `class="empty-state"`
    - website/blog.html empty state elements contain "No articles found" text
    - website/blog.html empty state elements have `style="display:none;"` by default
    - The JS file handling tag filtering (blog-timeline.js or main.js) contains logic to show/hide `timeline-empty` and `grid-empty` based on filtered post count
    - Filtering to a tag with zero posts shows the empty state message
    - Filtering back to "All" hides the empty state and shows all posts
  </acceptance_criteria>
  <done>Blog loading states show animated spinner SVG instead of plain text. Empty state with search icon and helpful message appears when tag filter matches zero posts. Filter reset to "All" hides empty state and shows all posts.</done>
</task>

</tasks>

<verification>
1. Open blog.html in browser -- loading spinner should be visible briefly before articles render
2. Click a tag filter that has no matching posts -- "No articles found" empty state should appear
3. Click "All" -- empty state disappears, all posts show
4. On mobile (or DevTools mobile view): tap a button -- should feel instant (no 300ms delay)
5. Run: `grep -c "touch-action" website/css/style.css` -- should return 1+
6. Run: `grep -c "loading-spinner" website/blog.html` -- should return 2
7. Run: `grep -c "empty-state" website/blog.html` -- should return 2
</verification>

<success_criteria>
- Loading spinner with animation replaces plain "Loading articles..." text in both timeline and grid views
- Empty state with icon, title, and help text appears when no articles match a tag filter
- Touch-action: manipulation applied to all interactive elements in CSS
- Backward-compat color aliases marked as DEPRECATED with migration guidance
- No JavaScript errors in console when filtering, loading, or viewing empty states
</success_criteria>

<output>
After completion, create `.planning/phases/1001-fix-ui-ux-brand-compliance-issues-from-2026-03-24-uiux-brand-review-md-includes-emoji-replacement-accessibility-fixes-performance-optimization-and-typography-enforcement-across-13-action-items-p0-p3/1001-02-SUMMARY.md`
</output>
