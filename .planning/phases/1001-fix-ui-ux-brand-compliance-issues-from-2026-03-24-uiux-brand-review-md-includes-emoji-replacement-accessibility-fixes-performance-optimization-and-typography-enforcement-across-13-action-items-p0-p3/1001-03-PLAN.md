---
phase: 1001-uiux-brand-compliance
plan: 03
type: execute
wave: 2
depends_on:
  - 1001-01
  - 1001-02
files_modified:
  - website/css/style.css
  - website/index.html
  - website/about.html
  - website/blog.html
  - website/petition.html
  - website/donate.html
  - website/token.html
  - website/post.html
autonomous: false
requirements:
  - P3-INTERACTIVE-STATES
  - P3-ACCESSIBILITY-ENHANCEMENTS
  - P3-REDUCED-MOTION
must_haves:
  truths:
    - "Cards have hover shadow elevation (resting -> hover -> active depth)"
    - "Skip-to-content link is present and works for keyboard navigation"
    - "Users with prefers-reduced-motion see no animations"
    - "Dynamic content areas use aria-live for screen reader announcements"
  artifacts:
    - path: "website/css/style.css"
      provides: "Enhanced interactive states, skip-to-content styling, reduced-motion query"
      contains: "prefers-reduced-motion"
    - path: "website/index.html"
      provides: "Skip-to-content link and aria-live regions"
      contains: "skip-to-content"
  key_links:
    - from: "website/css/style.css"
      to: "website/index.html"
      via: "skip-to-content styling and reduced-motion query"
      pattern: "skip-to-content.*prefers-reduced-motion"
---

<objective>
Add enhanced interactive states (card hover shadows), accessibility improvements (skip-to-content, aria-live, reduced-motion), and polish the UI for production readiness. This addresses P3 (nice-to-have) items from the UI/UX review plus remaining P2 accessibility items.

Purpose: These polish items make the difference between "functional website" and "professional campaign site." Skip-to-content and reduced-motion are accessibility best practices. Card hover shadows add tactile depth. aria-live regions ensure screen reader users know when dynamic content updates.

Output: Enhanced CSS with hover states, reduced-motion query, and skip-to-content styling. All HTML pages updated with skip-to-content link. Blog and petition pages updated with aria-live regions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reviews/2026-03-24-uiux-brand-review.md
@.planning/phases/1001-fix-ui-ux-brand-compliance-issues-from-2026-03-24-uiux-brand-review-md-includes-emoji-replacement-accessibility-fixes-performance-optimization-and-typography-enforcement-across-13-action-items-p0-p3/1001-01-SUMMARY.md
@.planning/phases/1001-fix-ui-ux-brand-compliance-issues-from-2026-03-24-uiux-brand-review-md-includes-emoji-replacement-accessibility-fixes-performance-optimization-and-typography-enforcement-across-13-action-items-p0-p3/1001-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive states, reduced-motion, and skip-to-content CSS</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (full file, especially button styles around line 106-130, card styles, and animation section)
    - .planning/reviews/2026-03-24-uiux-brand-review.md (Section 3 for depth system recommendations, Section 5 item 11 for interactive states, item 12 for accessibility)
    - BRAND_GUIDELINES.md (Animation section: "Hover effects: -translate-y-2, color fills on icons" and "Transitions: 300-700ms")
  </read_first>
  <action>
Add the following CSS blocks to website/css/style.css.

**1. Skip-to-content link (add at the very top of the file, right after the reset rules and before :root, around line 8):**

```css
/* --- Skip to Content (Accessibility) ----------------------- */
.skip-to-content {
  position: absolute;
  top: -100%;
  left: 16px;
  z-index: 10000;
  padding: 12px 24px;
  background: var(--slate);
  color: var(--white);
  font-family: var(--font-body);
  font-size: 0.9rem;
  font-weight: 600;
  border-radius: var(--radius-sm);
  text-decoration: none;
  transition: top 0.2s ease;
}

.skip-to-content:focus {
  top: 16px;
  color: var(--white);
}
```

**2. Enhanced card hover states (find existing `.problem-card`, `.help-card`, `.blog-card`, `.team-card` styles and add hover rules after each):**

After `.problem-card` styles:
```css
.problem-card {
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.problem-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
```

After `.help-card` styles:
```css
.help-card {
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.help-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
```

After `.blog-card` styles:
```css
.blog-card {
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.blog-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow);
}
```

After `.team-card` styles:
```css
.team-card {
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.team-card:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow);
}
```

NOTE: If any of these card classes already have `transition` property set, merge the values instead of overriding. Check for existing transition declarations first.

**3. Enhanced button hover state (update existing `.btn-primary:hover` if exists, or add after `.btn-primary` definition):**

```css
.btn-primary:hover {
  background: var(--red-dk);
  transform: translateY(-2px);
  box-shadow: var(--shadow-red);
}
```

**4. Focus-visible styles (add after the button section):**

```css
/* --- Focus Visible (Keyboard Only) ------------------------- */
:focus-visible {
  outline: 3px solid var(--teal);
  outline-offset: 2px;
}

button:focus:not(:focus-visible),
a:focus:not(:focus-visible) {
  outline: none;
}
```

**5. Reduced-motion media query (add at the very end of the file, before any existing media queries or at the bottom):**

```css
/* --- Reduced Motion ---------------------------------------- */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }

  .will-animate {
    opacity: 1;
    transform: none;
  }

  html {
    scroll-behavior: auto;
  }
}
```

**6. Visually-hidden utility class (add near the layout utilities section, around line 86):**

```css
/* Screen reader only */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```
  </action>
  <verify>
    <automated>grep -c "skip-to-content" website/css/style.css && grep -c "prefers-reduced-motion" website/css/style.css && grep -c "focus-visible" website/css/style.css && grep -c "sr-only" website/css/style.css && grep -c "problem-card:hover" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - website/css/style.css contains `.skip-to-content` with `position: absolute` and `top: -100%`
    - website/css/style.css contains `.skip-to-content:focus` with `top: 16px`
    - website/css/style.css contains `.problem-card:hover` with `transform: translateY(-4px)` and `box-shadow: var(--shadow)`
    - website/css/style.css contains `.help-card:hover` with `transform: translateY(-4px)`
    - website/css/style.css contains `.blog-card:hover` with `transform: translateY(-4px)`
    - website/css/style.css contains `.team-card:hover` with `transform: translateY(-2px)`
    - website/css/style.css contains `:focus-visible` rule with `outline: 3px solid var(--teal)`
    - website/css/style.css contains `@media (prefers-reduced-motion: reduce)` with `animation-duration: 0.01ms`
    - website/css/style.css contains `.sr-only` with `position: absolute` and `clip: rect(0, 0, 0, 0)`
    - website/css/style.css contains `.btn-primary:hover` with `transform: translateY(-2px)` and `box-shadow: var(--shadow-red)`
  </acceptance_criteria>
  <done>CSS has enhanced card hover states with elevation transitions, focus-visible keyboard ring styles, skip-to-content link styling, reduced-motion media query that disables all animations, sr-only utility class, and enhanced button hover state with red shadow.</done>
</task>

<task type="auto">
  <name>Task 2: Add skip-to-content link and aria-live regions to all HTML pages</name>
  <files>website/index.html, website/about.html, website/blog.html, website/petition.html, website/donate.html, website/token.html, website/post.html</files>
  <read_first>
    - website/index.html (to see where to insert skip-to-content link -- must be first element after body open tag)
    - website/blog.html (lines 97-105 to identify dynamic content areas needing aria-live)
    - website/petition.html (lines 111-114 for signature count area needing aria-live)
    - website/about.html (to see body open tag location)
    - website/donate.html (to see body open tag location)
    - website/token.html (to see body open tag location)
    - website/post.html (to see body open tag location)
  </read_first>
  <action>
**Add skip-to-content link to ALL 7 main HTML pages (index.html, about.html, blog.html, petition.html, donate.html, token.html, post.html).**

In each file, add the following as the FIRST element inside `<body>`, BEFORE the `<!-- NAV -->` comment:

```html
  <a href="#main-content" class="skip-to-content">Skip to content</a>
```

Then add `id="main-content"` to the first major content section AFTER the nav in each file:

- **index.html**: Add `id="main-content"` to the `<section class="hero">` tag (line 56):
  `<section class="hero" id="main-content">`

- **about.html**: Add `id="main-content"` to the page header section (line 53):
  `<section style="..." id="main-content">`

- **blog.html**: Add `id="main-content"` to the `<div class="blog-header">` (line 53):
  `<div class="blog-header" id="main-content">`

- **petition.html**: Add `id="main-content"` to `<section class="petition-hero">` (line 53):
  `<section class="petition-hero" id="main-content">`

- **donate.html**: Add `id="main-content"` to the first section after nav (check the file to find the right element)

- **token.html**: Add `id="main-content"` to the first section after nav (check the file to find the right element)

- **post.html**: Add `id="main-content"` to the main article container (check the file to find the right element)

**Add aria-live regions for dynamic content:**

**petition.html** -- Add `aria-live="polite"` and `aria-atomic="true"` to the petition count div (line 111):
```html
<div class="petition-count" aria-live="polite" aria-atomic="true">
```

**blog.html** -- Add `aria-live="polite"` to the blog content wrapper (line 95):
```html
<div class="blog-content-wrapper" aria-live="polite">
```

This ensures screen readers announce when the blog content area changes (e.g., when posts are filtered by tag or loaded dynamically).
  </action>
  <verify>
    <automated>for f in website/index.html website/about.html website/blog.html website/petition.html website/donate.html website/token.html website/post.html; do echo "=== $f ==="; grep -c "skip-to-content" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 7 HTML files (index, about, blog, petition, donate, token, post) contain `<a href="#main-content" class="skip-to-content">Skip to content</a>` as first element inside body
    - All 7 HTML files contain `id="main-content"` on their first major content section after nav
    - website/petition.html petition-count div has `aria-live="polite"` and `aria-atomic="true"`
    - website/blog.html blog-content-wrapper has `aria-live="polite"`
    - Skip-to-content link appears on Tab keypress, focusing on main content area
  </acceptance_criteria>
  <done>All 7 pages have skip-to-content link as first body element linking to id="main-content". Petition signature count has aria-live for screen reader announcements. Blog content wrapper has aria-live for filter change announcements.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual and keyboard verification of all UI/UX compliance fixes</name>
  <files>website/index.html, website/blog.html, website/petition.html, website/about.html</files>
  <action>
Human verification of the complete UI/UX brand compliance fixes across all 3 plans. This is the final quality gate before the phase is marked complete.

What was built across Plans 01-03:
- SVG icons replacing all emojis (index.html, about.html, petition.html)
- Accessible placeholders with role="img" and aria-label
- Loading spinners for blog listing
- Empty states for tag filters with zero matches
- Touch-action: manipulation on all interactive elements
- Card hover effects with elevation shadows
- Skip-to-content link on all 7 pages
- Reduced-motion media query
- aria-live regions on blog and petition
- Focus-visible keyboard ring styles

Verification steps:
1. Start local server: `cd website && python -m http.server 8000`
2. Open http://localhost:8000/index.html -- verify SVG icons in problem cards (Shield=red, AlertCircle=amber, Activity=teal), Heart SVGs in placeholders, card hover shadows
3. Open http://localhost:8000/blog.html -- verify loading spinner, try filtering to empty results for empty state
4. Open http://localhost:8000/petition.html -- verify SVG checkmark instead of emoji
5. Open http://localhost:8000/about.html -- verify Heart SVG in placeholder, mobile-responsive mission grid
6. Keyboard: Tab on any page to see skip-to-content link, Enter to jump, Tab through elements for teal focus ring
7. DevTools: Search for aria-live, role="img" in elements panel
8. DevTools Rendering: Enable prefers-reduced-motion to verify animations disabled
  </action>
  <verify>
    <automated>echo "Manual visual verification required"</automated>
  </verify>
  <done>Human confirms: SVG icons display correctly with brand colors, card hover effects work, skip-to-content is functional, reduced-motion disables animations, no emoji characters visible on any page, overall UI looks professional and polished.</done>
</task>

</tasks>

<verification>
1. Run: `grep -rPn "[\x{1F300}-\x{1F9FF}]|[\x{2600}-\x{26FF}]" website/*.html` -- should return zero matches (or only test-password.html)
2. Run: `grep -c "skip-to-content" website/index.html website/about.html website/blog.html website/petition.html` -- each should return 1
3. Run: `grep -c "prefers-reduced-motion" website/css/style.css` -- should return 1+
4. Run: `grep -c "aria-live" website/blog.html website/petition.html` -- each should return 1+
5. Run: `grep -c "focus-visible" website/css/style.css` -- should return 1+
6. Visual check: All SVG icons render at correct sizes with brand colors
7. Keyboard test: Tab through page, skip-to-content works, focus rings visible
</verification>

<success_criteria>
- Cards have smooth hover elevation (translateY + shadow) per brand guidelines animation spec
- Skip-to-content link visible on keyboard Tab, navigates to main content
- prefers-reduced-motion query disables all CSS animations and transitions
- Focus-visible ring in teal for keyboard navigation, no ring for mouse clicks
- aria-live on petition count and blog content wrapper for screen reader updates
- sr-only utility class available for future accessible text needs
- Human visual verification confirms all changes look professional and correct
</success_criteria>

<output>
After completion, create `.planning/phases/1001-fix-ui-ux-brand-compliance-issues-from-2026-03-24-uiux-brand-review-md-includes-emoji-replacement-accessibility-fixes-performance-optimization-and-typography-enforcement-across-13-action-items-p0-p3/1001-03-SUMMARY.md`
</output>
