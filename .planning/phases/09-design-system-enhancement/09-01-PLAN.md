---
phase: 09-design-system-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-04
must_haves:
  truths:
    - "Blog cards display bold activism aesthetic with teal left accent, dramatic hover shadow, and oversized Montserrat titles"
    - "Timeline cards have teal dot pulse animation and enhanced hover with translateX lift"
    - "All card components site-wide use border-radius var(--radius-lg) (1.75rem)"
    - "Tag badges are color-coded by category (red for Public Health, teal for Regulation, amber for Pet Theft, etc.)"
    - "Stat callout component displays large Montserrat Black numbers with red left border accent"
    - "Image placeholder cards show teal-to-slate gradient instead of plain background"
  artifacts:
    - path: "website/css/style.css"
      provides: "Bold activism blog card styles, site-wide rounded corners, tag badge colors, stat callout"
      contains: ".blog-card-bold"
  key_links:
    - from: "website/css/style.css"
      to: "website/js/blog-timeline.js"
      via: "CSS classes applied to dynamically rendered blog cards"
      pattern: "blog-card|timeline-post-card|blog-tag"
---

<objective>
Apply bold activism aesthetic to blog listing views (timeline + grid) and enforce large rounded corners (--radius-lg) site-wide on all card components.

Purpose: Transform the blog listing from functional to emotionally compelling with dramatic shadows, oversized typography, color-coded tags, and bold visual accents per DESIGN-01. Enforce brand-compliant 1.75rem rounded corners on all card components per DESIGN-04.

Output: Enhanced style.css with populated Phase 9 stubs, updated card border-radius values, and new tag badge color system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-enhancement/09-CONTEXT.md
@.planning/phases/09-design-system-enhancement/09-UI-SPEC.md
@website/css/style.css

<interfaces>
<!-- Key CSS selectors and current values the executor needs to modify -->

From website/css/style.css :root (lines 12-56):
```css
--radius-sm: 0.5rem;
--radius: 0.875rem;
--radius-md: 1.25rem;
--radius-lg: 1.75rem;
--radius-pill: 9999px;
--shadow-sm: 0 2px 8px rgba(38,70,83,0.07);
--shadow: 0 4px 24px rgba(38,70,83,0.10);
--shadow-lg: 0 8px 48px rgba(38,70,83,0.16);
--shadow-red: 0 8px 32px rgba(230,57,70,0.25);
--font-head: 'Montserrat', sans-serif;
--font-body: 'Inter', system-ui, sans-serif;
```

Card components needing border-radius update to --radius-lg:
- .blog-card (line 626): currently `var(--radius-md)`
- .blog-post-card (line 1361): currently `var(--radius-md)`
- .timeline-post-card (line 2097): currently `var(--radius-md)`
- .problem-card (line 459): currently `var(--radius-md)`
- .problem-card::before (line 473): currently `var(--radius-md) var(--radius-md) 0 0`
- .help-card (line 579): currently `var(--radius-md)`
- .team-card (line 986): currently `var(--radius-md)`
- .donate-tier-card (line 1078): currently `6px`
- .transparency-box (line 1057): currently `var(--radius-md)`

Phase 9 CSS stubs to populate (lines 692-720):
```css
.blog-card-bold { /* empty */ }
.blog-card-editorial { /* empty */ }
.stat-callout { text-align: center; padding: 2rem; border-radius: var(--radius-lg); }
.stat-callout__number { font-family: var(--font-head); font-weight: 900; font-size: 3rem; line-height: 1; color: var(--red); }
.stat-callout__label { font-size: 0.875rem; text-transform: uppercase; letter-spacing: 0.1em; color: var(--text-md); margin-top: 0.5rem; }
```

Current .blog-tag (line 654-667):
```css
.blog-tag {
  display: inline-block;
  background: rgba(42,157,143,0.10);
  color: var(--teal-dk);
  font-family: var(--font-body);
  font-size: 0.7rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  padding: 4px 10px;
  border-radius: var(--radius-pill);
  margin-bottom: 12px;
  border: 1px solid rgba(42,157,143,0.20);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update site-wide card border-radius to --radius-lg and enhance blog card bold activism styles</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (full file — executor must see current state of all card components)
    - .planning/phases/09-design-system-enhancement/09-UI-SPEC.md (canonical visual spec)
    - .planning/phases/09-design-system-enhancement/09-CONTEXT.md (locked decisions D-01 through D-21)
  </read_first>
  <action>
**Part A: Update all card component border-radius to var(--radius-lg) per D-11.**

Update these selectors (change `var(--radius-md)` or hardcoded values to `var(--radius-lg)`):
1. `.blog-card` (line ~626): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
2. `.blog-post-card` (line ~1361): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
3. `.timeline-post-card` (line ~2097): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
4. `.problem-card` (line ~459): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
5. `.problem-card::before` (line ~473): `border-radius: var(--radius-md) var(--radius-md) 0 0` → `border-radius: var(--radius-lg) var(--radius-lg) 0 0`
6. `.help-card` (line ~579): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
7. `.team-card` (line ~986): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`
8. `.donate-tier-card` (line ~1078): `border-radius: 6px` → `border-radius: var(--radius-lg)`
9. `.transparency-box` (line ~1057): `border-radius: var(--radius-md)` → `border-radius: var(--radius-lg)`

Per D-13, add overflow: hidden to image containers within cards (`.blog-card-img`, `.blog-post-img`, `.timeline-post-image`) — these already have overflow: hidden, verify and confirm.

Per D-14, verify input/form elements use `border-radius: var(--radius)` (0.875rem). No change needed if already correct.

**Part B: Enhance blog card hover to bold activism per D-01 and D-19.**

Update `.blog-card` hover (line ~634):
```css
.blog-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(38,70,83,0.15);
}
```

Update `.blog-card` base to add teal left accent border per D-01:
Add to `.blog-card` (line ~623): `border-left: 4px solid var(--teal);`

Update transition timing on `.blog-card` to `transition: transform 0.3s ease, box-shadow 0.3s ease;` per D-19.

**Part C: Enhance blog card title typography per D-15.**

Update `.blog-card h3` (line ~669):
```css
.blog-card h3 {
  font-family: var(--font-head);
  font-size: 1.25rem;
  font-weight: 900;
  letter-spacing: -0.02em;
  line-height: 1.3;
  margin-bottom: 10px;
  color: var(--slate);
  flex: 1;
}
```

**Part D: Update blog card excerpt per D-16.**

Update `.blog-card p` (line ~677):
```css
.blog-card p {
  font-family: var(--font-body);
  font-size: 0.9rem;
  line-height: 1.6;
  color: var(--text-md);
  margin-bottom: 0;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
```

**Part E: Update blog card meta per D-17.**

Update `.blog-card-meta` (line ~683):
```css
.blog-card-meta {
  font-family: var(--font-body);
  font-size: 0.8rem;
  font-weight: 500;
  color: var(--gray);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  margin-top: 16px;
  display: flex;
  align-items: center;
  gap: 6px;
}
```

**Part F: Enhance timeline card hover per D-03 and D-19.**

Update `.timeline-post-card` base (line ~2094): add `box-shadow: var(--shadow);` as default shadow.
Update `.timeline-post-card:hover` (line ~2117):
```css
.timeline-post-card:hover {
  border-color: var(--teal);
  box-shadow: var(--shadow-lg);
  transform: translateX(8px);
}
```

Update `.timeline-month-title` (find existing selector) to use: `font-family: var(--font-head); font-weight: 900; font-size: 1.1rem; text-transform: uppercase; letter-spacing: 0.08em;` per D-03.

**Part G: Enhance blog-post-card (grid view) per D-04 and D-19.**

Update `.blog-post-card` hover (line ~1367):
```css
.blog-post-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(38,70,83,0.15);
}
```

Update `.blog-post-card` base transition to `transition: transform 0.3s ease, box-shadow 0.3s ease;`

Update `.blog-post-body h2` (line ~1381): add `font-family: var(--font-head); font-weight: 900; letter-spacing: -0.02em;`

**Part H: Update image placeholder gradient per D-21.**

Update `.blog-card-img` (line ~639): already has `background: linear-gradient(135deg, var(--teal), var(--slate));` — this is correct per D-21. Verify and keep.

Update `.blog-post-img` (line ~1372): currently `background: linear-gradient(160deg, var(--teal-dk), var(--slate));` — update to `background: linear-gradient(135deg, var(--teal), var(--slate));` for consistency per D-21.
  </action>
  <verify>
    <automated>cd "C:/Users/sangm/OneDrive/_WorkFolder/_Personal/Start-ups/stop_dog_eaters" && grep -c "border-radius: var(--radius-lg)" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - grep "border-radius: var(--radius-lg)" style.css returns at least 9 matches (blog-card, blog-post-card, timeline-post-card, problem-card, help-card, team-card, donate-tier-card, transparency-box, stat-callout)
    - grep "border-left: 4px solid var(--teal)" style.css matches on .blog-card selector
    - grep "0 12px 40px rgba(38,70,83,0.15)" style.css returns at least 2 matches (blog-card:hover, blog-post-card:hover)
    - grep "font-size: 1.25rem" style.css matches in .blog-card h3 context
    - grep "letter-spacing: -0.02em" style.css matches for card title
    - grep "-webkit-line-clamp: 3" style.css matches for blog-card p
    - grep "letter-spacing: 0.04em" style.css matches for blog-card-meta
    - grep "letter-spacing: 0.08em" style.css matches for timeline-month-title
    - grep "transition: transform 0.3s ease" style.css returns at least 2 matches
    - No raw `border-radius: 6px` remaining on donate-tier-card
    - No `border-radius: var(--radius-md)` remaining on any card component listed in D-11
  </acceptance_criteria>
  <done>All 9 card components use border-radius var(--radius-lg). Blog cards have teal left accent border with dramatic 0 12px 40px hover shadow. Timeline cards enhanced with uppercase month headers. Grid view cards have bold Montserrat titles and 3-line excerpt clamp. All transitions use 0.3s ease.</done>
</task>

<task type="auto">
  <name>Task 2: Populate blog-card-bold stub, add tag badge color mapping, and enhance stat callout</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (must see state after Task 1 modifications)
    - .planning/phases/09-design-system-enhancement/09-CONTEXT.md (decisions D-02, D-05, D-18, D-20)
    - .planning/phases/09-design-system-enhancement/09-UI-SPEC.md (tag badge color table, stat callout spec)
  </read_first>
  <action>
**Part A: Populate .blog-card-bold stub (line ~693) per D-01 summary.**

Replace the empty stub with:
```css
.blog-card-bold {
  border-left: 4px solid var(--teal);
  box-shadow: var(--shadow-sm);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.blog-card-bold:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(38,70,83,0.15);
}

.blog-card-bold h3 {
  font-family: var(--font-head);
  font-size: 1.25rem;
  font-weight: 900;
  letter-spacing: -0.02em;
  line-height: 1.3;
}
```

**Part B: Add tag badge color-coded variants per D-02 and D-20.**

Add after the existing `.blog-tag` rule (after line ~667), before the Phase 9 stubs section:
```css
/* Tag badge color variants per category (D-20) */
.blog-tag--health {
  background: var(--red);
  color: var(--white);
  border-color: var(--red);
}

.blog-tag--regulation {
  background: var(--teal);
  color: var(--white);
  border-color: var(--teal);
}

.blog-tag--theft {
  background: var(--amber);
  color: var(--white);
  border-color: var(--amber);
}

.blog-tag--support {
  background: var(--slate);
  color: var(--white);
  border-color: var(--slate);
}

.blog-tag--lucky {
  background: var(--red-lt);
  color: var(--white);
  border-color: var(--red-lt);
}

.blog-tag--updates {
  background: var(--teal-lt);
  color: var(--white);
  border-color: var(--teal-lt);
}
```

Also update `.blog-tag` base style: change `font-size: 0.7rem` to `font-size: 0.75rem` and add `font-family: var(--font-head); font-weight: 700;` for Montserrat Bold per D-02. Keep existing uppercase and letter-spacing 0.1em.

Add hover state for tag badges per UI-SPEC interaction states:
```css
.blog-tag:hover {
  transform: scale(1.05);
}
.blog-tag:focus {
  outline: 2px solid currentColor;
  outline-offset: 2px;
}
```

**Part C: Enhance stat callout component per D-05.**

Update the `.stat-callout` stub (line ~701) to:
```css
.stat-callout {
  text-align: left;
  padding: 2rem 2rem 2rem 2.5rem;
  border-radius: var(--radius-lg);
  border-left: 4px solid var(--red);
  background: var(--white);
  box-shadow: var(--shadow-sm);
}
```

The `.stat-callout__number` already has correct values (3rem, Montserrat 900, red). Update color to `color: var(--teal);` per D-05 (spec says teal for the number, red for the border):
```css
.stat-callout__number {
  font-family: var(--font-head);
  font-weight: 900;
  font-size: 3rem;
  line-height: 1;
  color: var(--teal);
}
```

Keep `.stat-callout__label` as-is (already correct: 0.875rem, uppercase, letter-spacing 0.1em).

**Part D: Update section eyebrow letter-spacing per D-18.**

Find `.eyebrow` selector and update to add `letter-spacing: 0.12em;` if not already present. The eyebrow class is used on section headers across the site.

**Part E: Add optional entrance animation trigger class for blog cards.**

Add after the blog card bold section:
```css
/* Blog card entrance animation (IntersectionObserver) */
.blog-card[data-animate],
.blog-post-card[data-animate],
.timeline-post-card[data-animate] {
  opacity: 0;
  transform: translateY(20px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}

.blog-card[data-animate].is-visible,
.blog-post-card[data-animate].is-visible,
.timeline-post-card[data-animate].is-visible {
  opacity: 1;
  transform: translateY(0);
}

/* Respect prefers-reduced-motion */
@media (prefers-reduced-motion: reduce) {
  .blog-card[data-animate],
  .blog-post-card[data-animate],
  .timeline-post-card[data-animate] {
    opacity: 1;
    transform: none;
    transition: none;
  }
}
```
  </action>
  <verify>
    <automated>cd "C:/Users/sangm/OneDrive/_WorkFolder/_Personal/Start-ups/stop_dog_eaters" && grep -c "blog-tag--" website/css/style.css && grep "stat-callout" website/css/style.css | grep -c "border-left"</automated>
  </verify>
  <acceptance_criteria>
    - grep "blog-tag--health" style.css returns match with `background: var(--red)`
    - grep "blog-tag--regulation" style.css returns match with `background: var(--teal)`
    - grep "blog-tag--theft" style.css returns match with `background: var(--amber)`
    - grep "blog-tag--support" style.css returns match with `background: var(--slate)`
    - grep "blog-tag--lucky" style.css returns match with `background: var(--red-lt)`
    - grep "blog-tag--updates" style.css returns match with `background: var(--teal-lt)`
    - grep "blog-card-bold" style.css returns at least 3 matches (base, hover, h3)
    - grep "border-left: 4px solid var(--red)" style.css matches in stat-callout context
    - grep "color: var(--teal)" style.css matches in stat-callout__number context
    - grep "letter-spacing: 0.12em" style.css matches for eyebrow selector
    - grep "prefers-reduced-motion" style.css matches (entrance animation respects a11y)
    - grep "data-animate" style.css returns at least 3 matches
    - The .blog-card-bold stub comment is replaced with actual CSS properties
  </acceptance_criteria>
  <done>Blog-card-bold stub populated with bold activism variant. Six tag badge color variants added (health, regulation, theft, support, lucky, updates) with white text. Stat callout has teal number, red left border, left-aligned text. Eyebrow letter-spacing updated to 0.12em. Entrance animation CSS added with reduced-motion support.</done>
</task>

</tasks>

<verification>
1. All card components use `border-radius: var(--radius-lg)` — grep confirms 9+ matches
2. Blog cards have teal left accent border and 0 12px 40px hover shadow
3. Tag badge color variants exist for all 6 categories
4. Stat callout has left-aligned text with 4px red border and teal number
5. Timeline month headers use Montserrat Black 900 uppercase with 0.08em letter-spacing
6. All transitions use 0.3s ease timing
7. Entrance animation CSS respects prefers-reduced-motion
8. No broken CSS syntax (open browser DevTools, check for parse errors)
</verification>

<success_criteria>
- Blog listing cards display bold activism aesthetic: teal accent, dramatic shadows, Montserrat Black titles
- All 9 card component types use var(--radius-lg) (1.75rem) border-radius
- Tag badges are color-coded by category with white text on colored backgrounds
- Stat callout component renders with large teal number and red left border
- CSS changes apply to both timeline and grid view without JS modifications
- Entrance animation respects prefers-reduced-motion media query
</success_criteria>

<output>
After completion, create `.planning/phases/09-design-system-enhancement/09-01-SUMMARY.md`
</output>
