---
phase: 18-go-live-readiness-fixes
plan: 03
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - website/css/style.css
  - website/index.html
  - website/privacy.html
  - website/terms.html
  - website/404.html
autonomous: true
requirements: ["GO-LIVE-AUDIT"]

must_haves:
  truths:
    - "Mobile nav dropdown has solid background, border separator, and smooth slide-down animation"
    - "Privacy policy page loads at privacy.html with campaign nav/footer and legal content"
    - "Terms of use page loads at terms.html with campaign nav/footer and legal content"
    - "Custom 404 page loads at 404.html with branded layout and navigation links"
    - "Data & Research section on homepage shows ONLY charts — no duplicate stat callouts"
    - "Tab/keyboard focus shows brand-colored outlines on all interactive elements"
  artifacts:
    - path: "website/css/style.css"
      provides: "Mobile nav animation, focus-visible styles"
      contains: "nav-slide-down"
    - path: "website/privacy.html"
      provides: "Privacy policy page"
      min_lines: 80
    - path: "website/terms.html"
      provides: "Terms of use page"
      min_lines: 80
    - path: "website/404.html"
      provides: "Custom branded 404 page"
      min_lines: 50
    - path: "website/index.html"
      provides: "Cleaned homepage without duplicate stats"
  key_links:
    - from: "website/privacy.html"
      to: "website/css/style.css"
      via: "shared stylesheet"
      pattern: "css/style\\.css"
    - from: "website/index.html"
      to: "website/css/style.css"
      via: "focus-visible and nav styles"
      pattern: "focus-visible"
---

<objective>
Fix 6 MEDIUM-severity issues from the go-live E2E audit: mobile nav polish (M1), blog banner fallback styling (M2), privacy/terms pages (M3), duplicate stat removal (M4), custom 404 page (M5), and focus-visible accessibility styles (M6).

Purpose: Resolve all MEDIUM audit findings so only LOW items remain before the final E2E re-audit.
Output: Three new HTML pages (privacy, terms, 404), cleaned homepage, polished mobile nav, accessible focus styles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reviews/2026-03-29-go-live-e2e-audit.md
@website/css/style.css
@website/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mobile nav, duplicate stats, and focus-visible styles</name>
  <files>website/css/style.css, website/index.html</files>
  <action>
**M1 — Mobile Nav Dropdown (style.css lines 5462-5476):**
Replace the current `.nav-links` mobile styles (inside `@media (max-width: 768px)`) with improved version:
- Change `background: rgba(255,255,255,0.95)` to `background: #ffffff` (solid white, no bleed-through)
- Add `border-bottom: 2px solid var(--outline-variant)` for clear visual separation from page content
- Add `border-radius: 0 0 var(--radius) var(--radius)` for polished bottom corners
- Update `.nav-links.open` to add a smooth slide-down animation
- Add the keyframes animation at the end of the file (outside the media query):
```css
@keyframes nav-slide-down {
  from { opacity: 0; transform: translateY(-8px); }
  to { opacity: 1; transform: translateY(0); }
}
```
- Inside the media query, set `.nav-links.open { display: flex; animation: nav-slide-down 0.2s ease-out; }`

**M4 — Remove Duplicate Stats from Data & Research (index.html lines 170-184):**
Delete the entire `.stat-callout-grid` div (lines 170-184, from `<!-- Stat Callouts (left column) -->` through the closing `</div>` of `.stat-callout-grid`). The stats bar at lines 85-102 already displays these same 3 numbers. The Data & Research section should ONLY contain the `.data-charts-grid` div with the two Chart.js canvases.

After removing the stat-callout-grid, also update the `.data-research-grid` CSS to be a single-column layout instead of a two-column layout (since the left stat column is gone). Find the `.data-research-grid` rule in style.css and change it from a 2-column grid to `display: block` or `grid-template-columns: 1fr`.

**M6 — Focus-Visible Styles (style.css):**
Add these rules at the end of the LAYER 3: COMPONENTS section (before the LAYER 4 or responsive media queries):
```css
/* --- Accessibility: Focus-Visible -------------------------------- */
:focus-visible {
  outline: 2px solid var(--primary-container);
  outline-offset: 2px;
}

.btn:focus-visible,
.nav-cta a:focus-visible {
  outline: 2px solid var(--primary-container);
  outline-offset: 3px;
  box-shadow: 0 0 0 4px rgba(29,106,114,0.2);
}

.nav-links a:focus-visible {
  outline: 2px solid var(--primary-container);
  outline-offset: 2px;
  border-radius: var(--radius-sm);
}

.footer-col a:focus-visible,
.footer-social a:focus-visible {
  outline: 2px solid var(--on-primary);
  outline-offset: 2px;
}
```
The footer focus styles use `--on-primary` (white) because the footer has a dark background (`--primary`), so teal outlines would be invisible.

**M2 — Blog Banner Fallback Note:**
No CSS fallback is needed because blog.html and post.html already conditionally render the banner image only when `post.banner_url` exists (the JS checks `if (post.banner_url) { ... }`). The real fix is generating banners for the remaining 8 posts using the blog-banner-generator skill. Add a comment at the top of the task summary noting this.
  </action>
  <verify>
    <automated>cd "C:\Users\sangm\OneDrive\_WorkFolder\_Personal\Start-ups\stop_dog_eaters" && grep -c "nav-slide-down" website/css/style.css && grep -c "focus-visible" website/css/style.css && grep -c "stat-callout-grid" website/index.html</automated>
  </verify>
  <done>
- nav-slide-down keyframes exist in style.css (grep count >= 2: definition + usage)
- focus-visible rules exist in style.css (grep count >= 4)
- stat-callout-grid removed from index.html (grep count = 0)
- Mobile nav uses solid white background with border-bottom
  </done>
</task>

<task type="auto">
  <name>Task 2: Create privacy.html, terms.html, and 404.html pages</name>
  <files>website/privacy.html, website/terms.html, website/404.html</files>
  <action>
**M3 — Privacy Policy Page (privacy.html):**
Create `website/privacy.html` using the same HTML structure as other pages (same `<head>` section with meta tags, same nav, same footer). Content sections:

1. **Data Collection** — We collect petition signatures (name, email) via Change.org. We do not collect data directly on this website. No cookies are used. No analytics tracking is implemented.
2. **Third-Party Services** — Change.org (petition hosting), Telegram (@stopdogeaters channel), Cloudflare (hosting). Each service has its own privacy policy.
3. **AI-Generated Content** — Blog articles are generated by AI (Claude via AWS Bedrock) and reviewed by our editorial team. Content is based on publicly available Vietnamese government reports, WHO data, and news sources.
4. **Data Retention** — Petition signatures are managed by Change.org. We do not store personal data on our servers.
5. **Contact** — Include email: privacy@stopdogeaters.info (or a suitable contact method).

Use the same nav/footer as index.html. Add `pt-nav` class to the first section for fixed nav offset. Use a clean, readable prose layout. Add semantic heading hierarchy (h1 for title, h2 for sections).

Update ALL footer instances across all 7 main HTML files (index, about, blog, petition, donate, token, post) to change `<a href="#">Privacy Policy</a>` to `<a href="privacy.html">Privacy Policy</a>`.

**M3 — Terms of Use Page (terms.html):**
Create `website/terms.html` with similar structure. Content sections:

1. **Website Usage** — This website is operated by the Stop Dog Eaters campaign. By accessing the site, you agree to these terms.
2. **Content Attribution** — Blog articles are AI-generated and reviewed by our team. Statistics are sourced from Vietnamese government agencies and international health organizations (WHO, CDC). We cite sources where available.
3. **Donations** — Funds raised through Change.org and Kickstarter are allocated per our published fund transparency breakdown. All donation platforms have their own terms.
4. **Disclaimer** — This is a grassroots advocacy campaign. We are not a registered nonprofit or NGO. Fund usage is transparently disclosed on our donate page.
5. **Modifications** — We may update these terms. Continued use of the site constitutes acceptance.

Update ALL footer instances to change `<a href="#">Terms of Use</a>` to `<a href="terms.html">Terms of Use</a>`. Also update index.html if it has a separate Terms of Use link outside the footer.

**M5 — Custom 404 Page (404.html):**
Create `website/404.html` with:
- Same `<head>`, nav, and footer as other pages
- A centered section with:
  - Large "404" in `--primary-container` color using `font-size: 8rem` and `--font-head`
  - Subheading: "Page Not Found"
  - Body text: "The page you're looking for doesn't exist. But 5 million dogs still need your voice."
  - Navigation links as pill buttons: Home, Blog, Petition, Donate
  - The Lucky heart SVG placeholder (same as used in index.html) for emotional connection
- Add `pt-nav` class for fixed nav offset
- Note in an HTML comment: `<!-- For Cloudflare Workers: configure wrangler.toml to serve this as the 404 handler -->`
  </action>
  <verify>
    <automated>cd "C:\Users\sangm\OneDrive\_WorkFolder\_Personal\Start-ups\stop_dog_eaters" && test -f website/privacy.html && test -f website/terms.html && test -f website/404.html && grep -c "privacy.html" website/index.html && grep -c "terms.html" website/index.html && echo "All 3 pages exist and footer links updated"</automated>
  </verify>
  <done>
- privacy.html exists with 5 content sections, nav, and footer
- terms.html exists with 5 content sections, nav, and footer
- 404.html exists with branded error page and navigation links
- All 7 HTML files' footer links updated from href="#" to href="privacy.html" and href="terms.html"
- Pages use consistent head/nav/footer structure matching the rest of the site
  </done>
</task>

</tasks>

<verification>
1. Open `website/index.html` locally — verify Data & Research section shows ONLY charts (no stat callouts)
2. Resize browser to 375px width — open mobile nav — verify solid white background with border, slide animation
3. Navigate to `website/privacy.html` — verify full page loads with nav/footer
4. Navigate to `website/terms.html` — verify full page loads with nav/footer
5. Navigate to `website/404.html` — verify branded 404 page with navigation links
6. Tab through any page — verify teal focus outline appears on interactive elements
7. Tab through footer — verify white focus outline appears on dark background links
8. Click footer "Privacy Policy" link — verify navigates to privacy.html (not #)
9. Click footer "Terms of Use" link — verify navigates to terms.html (not #)
</verification>

<success_criteria>
- Zero `href="#"` links for Privacy Policy or Terms of Use in any HTML file
- Mobile nav has solid white background, border-bottom, and slide animation
- stat-callout-grid div completely removed from index.html
- :focus-visible rules present in style.css with brand colors
- Three new HTML pages (privacy.html, terms.html, 404.html) render correctly
- All new pages use the same nav/footer structure as existing pages
</success_criteria>

<output>
After completion, create `.planning/phases/18-go-live-readiness-fixes/18-03-SUMMARY.md`

Note for future work: Blog post banners (M2) — 8 of 11 posts still lack banner SVGs. Use the blog-banner-generator skill to create banners for: vietnams-2026-rabies-crisis, the-unregulated-crisis, zero-registered-slaughterhouses, 5-million-dogs-a-year, the-hidden-victims, from-family-pet-to-slaughterhouse, inside-vietnams-dog-meat-black-market, breaking-the-silence. This is a separate effort from the code fixes in this plan.
</output>
