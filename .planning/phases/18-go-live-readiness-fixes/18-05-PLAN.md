---
phase: 18-go-live-readiness-fixes
plan: 05
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03", "18-04"]
files_modified:
  - .planning/reviews/2026-03-29-go-live-reaudit.md
autonomous: false
requirements: ["GO-LIVE-AUDIT"]
must_haves:
  truths:
    - "All 19 audit issues verified as resolved or acceptably mitigated"
    - "Zero console errors across all 7 pages on desktop and mobile"
    - "Zero dead href='#' links remaining in any HTML file"
    - "All pages render correctly at both 1280px and 375px viewports"
    - "Re-audit report produced with READY or NOT READY verdict"
  artifacts:
    - path: ".planning/reviews/2026-03-29-go-live-reaudit.md"
      provides: "Final E2E re-audit report with pass/fail per issue"
      min_lines: 100
  key_links:
    - from: ".planning/reviews/2026-03-29-go-live-reaudit.md"
      to: ".planning/reviews/2026-03-29-go-live-e2e-audit.md"
      via: "issue-by-issue verification against original audit"
      pattern: "C1|C2|C3|C4|C5|H1|H2|H3|H4|H5|H6|H7|M1|M2|M3|M4|M5|M6|L1|L2|L3"
---

<objective>
Full regression test across all 7 pages to verify ALL 19 issues from the original go-live audit are resolved. This is a verification-only plan — no production code changes.

Purpose: Produce a definitive READY/NOT READY verdict before go-live deployment.
Output: Re-audit report at `.planning/reviews/2026-03-29-go-live-reaudit.md` with per-issue pass/fail status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/reviews/2026-03-29-go-live-e2e-audit.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks across all HTML files</name>
  <files>.planning/reviews/2026-03-29-go-live-reaudit.md</files>
  <action>
Run a comprehensive suite of automated checks against the website files to verify each of the 19 audit issues. Execute these checks in order and record results:

**Static Analysis (no server needed):**

1. **C1 — Placeholder images:** Search all HTML files for "Add Lucky's photo" or "placeholder" text in image sections. Check if `website/assets/lucky-hero.jpg` and `website/assets/lucky-story.jpg` exist. If photos don't exist, verify that a professional fallback (gradient, styled SVG) replaces the developer placeholder text.

2. **C2 — OG share image:** Check if `website/assets/og-share.jpg` (or `.png`) exists. Verify file size is greater than 10KB (not empty). Verify all 7 HTML files reference it in og:image meta tag.

3. **C3 — Fund allocation consistency:** Extract fund allocation percentages from about.html, donate.html, and token.html. Compare values across all 3 pages — they must match exactly.

4. **C4 — Dead links:** Run `grep -rn 'href="#"' website/*.html` — count must be 0. Separately check for `href="#transparency"` and `href="#lucky"` and `href="#team"` which are valid anchor links (these should NOT be counted as dead).

5. **C5 — Petition form:** Check petition.html for either a real form action URL or a redirect to Change.org. Verify no `setTimeout` fake success handler remains in main.js for petition.

6. **H1 — Homepage blog cards:** Check index.html for dynamic blog card loading. Look for `fetch('data/index.json')` or similar in the homepage scripts. Verify no hardcoded blog card HTML with generic titles.

7. **H2 — Kickstarter buttons:** Check donate.html for either real Kickstarter URLs or "Coming Soon" disabled state on tier buttons.

8. **H3 — Stat counter animation:** Check main.js for selector that targets BOTH `.stat-number[data-count]` and `.stat-callout__number[data-count]`. Or verify that stat-callout__number elements were removed (M4 fix).

9. **H4 — Material Symbols a11y:** Run `grep -c 'aria-hidden="true"' website/*.html` for files using material-symbols-outlined. Count material symbols spans vs aria-hidden attributes.

10. **H5 — X/Twitter icon:** Check petition.html for proper X/Twitter logo (SVG or updated icon) instead of "crossword" Material Symbol.

11. **H6 — Skip navigation:** Run `grep -c 'skip' website/*.html` to verify skip-nav links exist on all pages.

12. **H7 — admin-utils.js:** Run `grep -rn 'admin-utils' website/*.html` — must return 0 matches (script tag removed from all pages).

13. **M1 — Mobile nav:** Check style.css for `nav-slide-down` keyframe and solid background on `.nav-links` mobile.

14. **M2 — Blog banners:** Count banner SVGs in `website/assets/banners/`. Note which posts still lack banners (acceptable if CSS fallback handles missing images gracefully).

15. **M3 — Privacy/Terms:** Check if `website/privacy.html` and `website/terms.html` exist. Verify footer links point to these pages (not #).

16. **M4 — Duplicate stats:** Check `website/index.html` for `.stat-callout-grid` — must not exist in the Data & Research section.

17. **M5 — 404 page:** Check if `website/404.html` exists with navigation links.

18. **M6 — Focus-visible:** Check style.css for `:focus-visible` rules.

19. **L1 — Font loading:** Check HTML files for optimized Material Symbols request (`wght,FILL@400,0` not `100..700`).

20. **L2 — Chart.js local:** Check that `website/js/vendor/chart.umd.min.js` exists and no HTML files reference `cdn.jsdelivr.net`.

21. **L3 — Copyright year:** Check main.js for `getFullYear()` dynamic year replacement.

**Compile all results into `.planning/reviews/2026-03-29-go-live-reaudit.md`.**

The report must have frontmatter with fields: type (go-live-reaudit), date, auditor, scope, verdict (READY or NOT READY), issues_total (19), issues_resolved, issues_remaining.

The body must include:
- H1: "Go-Live Re-Audit Report"
- H2: "Verdict" with READY or NOT READY
- H2: "Issue-by-Issue Verification" with a table: ID, Severity, Issue, Status (PASS/FAIL), Evidence
- All 19 issues listed: C1-C5, H1-H7, M1-M6, L1-L3
- H2: "Remaining Items" listing any issues that could not be fully resolved and why
- H2: "Recommendation" with deployment readiness statement
  </action>
  <verify>
    <automated>cd "C:\Users\sangm\OneDrive\_WorkFolder\_Personal\Start-ups\stop_dog_eaters" && test -f .planning/reviews/2026-03-29-go-live-reaudit.md && grep -c "PASS\|FAIL" .planning/reviews/2026-03-29-go-live-reaudit.md && echo "Re-audit report exists with pass/fail verdicts"</automated>
  </verify>
  <done>
- Re-audit report exists at .planning/reviews/2026-03-29-go-live-reaudit.md
- All 19 issues have PASS or FAIL status with evidence
- Verdict is clearly stated as READY or NOT READY
- Any remaining issues documented with reason and mitigation path
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full E2E re-audit of all 19 go-live issues across 7+ website pages. The automated verification report is at `.planning/reviews/2026-03-29-go-live-reaudit.md`.
  </what-built>
  <how-to-verify>
1. Start local server: `cd website && python -m http.server 8000`
2. **Desktop audit (1280px):**
   - Open http://localhost:8000/index.html — check hero image, stats bar, charts, blog cards, footer links
   - Open http://localhost:8000/petition.html — test petition form (should redirect to Change.org), check X/Twitter icon
   - Open http://localhost:8000/blog.html — check blog cards load dynamically, tag filter works
   - Open http://localhost:8000/about.html — check fund allocation matches donate and token pages
   - Open http://localhost:8000/donate.html — check Kickstarter buttons (Coming Soon state), fund tracker
   - Open http://localhost:8000/token.html — check fund chart renders from local Chart.js
   - Open http://localhost:8000/privacy.html — verify content loads correctly
   - Open http://localhost:8000/terms.html — verify content loads correctly
   - Open http://localhost:8000/404.html — verify branded error page
3. **Mobile audit (375px):**
   - Resize to 375px or use DevTools device mode
   - Open mobile nav — verify solid white background, slide animation, border
   - Test on index.html, petition.html, blog.html minimum
4. **Accessibility:**
   - Tab through index.html — verify skip-to-content link appears
   - Tab through buttons — verify teal focus outlines
   - Tab through footer — verify white focus outlines on dark background
5. **Console check:**
   - Open DevTools Console on each page — verify zero errors
6. **Network check:**
   - Open DevTools Network tab — verify no requests to cdn.jsdelivr.net
   - Verify Material Symbols font request is smaller (check URL)
7. **Review the re-audit report:**
   - Read `.planning/reviews/2026-03-29-go-live-reaudit.md`
   - Confirm you agree with the verdict
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass and site is READY for go-live, or describe any remaining issues that need fixing.</resume-signal>
</task>

</tasks>

<verification>
The entire plan IS the verification. Task 1 runs all 19 automated checks and produces the report. Task 2 (checkpoint) gets human confirmation.
</verification>

<success_criteria>
- Re-audit report produced with per-issue PASS/FAIL status
- All 5 CRITICAL issues (C1-C5) show PASS status
- All 7 HIGH issues (H1-H7) show PASS status
- All 6 MEDIUM issues (M1-M6) show PASS or ACCEPTABLE status
- All 3 LOW issues (L1-L3) show PASS status
- Human verifies and approves the go-live readiness verdict
- Final verdict is READY (or remaining items clearly documented with remediation plan)
</success_criteria>

<output>
After completion, create `.planning/phases/18-go-live-readiness-fixes/18-05-SUMMARY.md`

The re-audit report at `.planning/reviews/2026-03-29-go-live-reaudit.md` serves as the definitive go-live gate document.
</output>
