---
phase: 08-css-refactoring-foundation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - website/css/style.css
  - website/post.html
  - website/blog.html
  - website/moderate.html
  - website/index.html
autonomous: false
requirements: [DESIGN-03]

must_haves:
  truths:
    - "ALL 8 HTML pages have zero presentational inline style= attributes"
    - "post.html has zero inline styles except JS-driven display:none"
    - "blog.html has zero inline styles except JS-driven display:none/block"
    - "moderate.html has zero inline styles except JS-driven display:none"
    - "index.html has zero inline styles except JS-driven display:none"
    - "grep across all 8 HTML files confirms total presentational inline styles is zero"
  artifacts:
    - path: "website/css/style.css"
      provides: "Complete 4-layer CSS with all component classes, zero inline styles needed"
      contains: ".post-container, .post-byline, .post-cta-box, .blog-sidebar-cta, .mod-container"
    - path: "website/post.html"
      provides: "Post page with zero presentational inline styles"
    - path: "website/blog.html"
      provides: "Blog page with zero presentational inline styles"
    - path: "website/moderate.html"
      provides: "Moderation page with zero presentational inline styles"
    - path: "website/index.html"
      provides: "Index page with zero presentational inline styles"
  key_links:
    - from: "All 8 HTML files"
      to: "website/css/style.css"
      via: "semantic CSS classes (zero inline styles)"
      pattern: "No style= attributes with presentational CSS"
---

<objective>
Extract all remaining 41 inline styles from post.html (17), blog.html (12), moderate.html (7), and index.html (5). Then verify ALL 8 HTML pages have zero presentational inline styles.

Purpose: This final plan completes the CSS refactoring foundation. After this, DESIGN-03 is fully satisfied -- developers can maintain CSS without inline style conflicts. Phase 9+ can safely enhance the design system.

Output: Zero presentational inline styles across ALL 8 pages. Full CSS layer architecture complete.
</objective>

<execution_context>
@C:\Users\sangm\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sangm\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-css-refactoring-foundation/08-CONTEXT.md
@.planning/phases/08-css-refactoring-foundation/08-01-SUMMARY.md
@.planning/phases/08-css-refactoring-foundation/08-02-SUMMARY.md
@website/css/style.css
@website/post.html
@website/blog.html
@website/moderate.html
@website/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSS classes and replace inline styles in post.html, blog.html, moderate.html, and index.html</name>
  <files>website/css/style.css, website/post.html, website/blog.html, website/moderate.html, website/index.html</files>
  <read_first>
    - website/css/style.css (find existing sections: `/* --- Article/Post Page ---` at line 1043, `/* --- Blog Page ---` at line 1179, `/* --- Moderation Dashboard ---` at line 2728; check what classes Plans 01 and 02 added)
    - website/post.html (read full file -- 17 inline styles focused on article layout, byline, CTA box)
    - website/blog.html (read full file -- 12 inline styles focused on sidebar and community section)
    - website/moderate.html (read full file -- 7 inline styles focused on dashboard layout)
    - website/index.html (read full file -- 5 inline styles focused on CTA button row and transparency pledge)
    - .planning/phases/08-css-refactoring-foundation/08-CONTEXT.md (decisions D-01 through D-11)
  </read_first>
  <action>
This task combines CSS class creation and HTML replacement since the remaining pages are lighter. Work through each file sequentially.

**STEP A: Add CSS classes to style.css**

Under existing `/* --- Article/Post Page ---` section (~line 1043), add:

```css
/* Post page layout */
.post-container { max-width: 1000px; padding: 64px 32px 80px 32px; }
.post-loading { text-align: center; padding: 80px 0; color: var(--text-md); }
.post-article { max-width: 750px; margin: 0 auto; }
.post-banner { width: 100%; height: auto; border-radius: 16px; margin: 0 0 48px 0; box-shadow: var(--shadow-lg); }
.post-tag-block { display: block; margin-bottom: 20px; }
.post-title { font-family: var(--font-head); font-size: clamp(2.4rem, 5.5vw, 3.5rem); line-height: 1.12; margin-bottom: 32px; color: var(--slate); font-weight: 900; letter-spacing: -0.03em; }
.post-byline { display: flex; align-items: center; gap: 14px; font-size: 0.98rem; color: var(--text-md); margin-bottom: 64px; padding-bottom: 32px; border-bottom: 3px solid var(--border); }
.post-byline-author { font-weight: 700; color: var(--slate); }
.post-byline-dot { color: var(--teal); opacity: 0.5; }
.post-byline-date { font-weight: 500; }

/* Post CTA box (call-to-action after article) */
.post-cta-box { margin-top: 80px; background: linear-gradient(135deg, rgba(42,157,143,0.08) 0%, rgba(230,57,70,0.06) 100%); border: 3px solid var(--teal); border-radius: var(--radius-lg); padding: 56px 64px; text-align: center; max-width: 750px; margin-left: auto; margin-right: auto; box-shadow: var(--shadow); position: relative; overflow: hidden; }
.post-cta-accent { position: absolute; top: 0; left: 0; right: 0; height: 4px; background: linear-gradient(90deg, var(--teal) 0%, var(--red) 100%); }
.post-cta-heading { font-family: var(--font-head); font-size: 1.75rem; font-weight: 900; margin-bottom: 18px; color: var(--slate); letter-spacing: -0.02em; }
.post-cta-text { font-size: 1.12rem; color: var(--text); margin-bottom: 36px; line-height: 1.75; font-weight: 500; max-width: 540px; margin-left: auto; margin-right: auto; }
.post-cta-btn { font-size: 1.08rem; padding: 18px 48px; box-shadow: var(--shadow-red); }

/* Post error state */
.post-error { text-align: center; padding: 80px 0; }
.post-error p { color: var(--text-md); }
```

Under existing `/* --- Blog Page ---` section (~line 1179), add:

```css
/* Blog page header description */
.blog-header-desc { color: var(--text-md); max-width: 560px; }

/* Blog sidebar extras */
.blog-sidebar-petition { margin-top: 32px; background: var(--mist); border-radius: 6px; padding: 20px; }
.blog-sidebar-petition h3 { font-size: 0.95rem; margin-bottom: 10px; }
.blog-sidebar-petition p { font-size: 0.85rem; color: var(--text-md); margin-bottom: 14px; }
.blog-sidebar-petition .btn,
.blog-sidebar-cta { width: 100%; display: block; text-align: center; }

/* Blog sidebar Telegram description */
.blog-sidebar-desc { font-size: 0.88rem; color: var(--text-md); line-height: 1.6; margin-bottom: 24px; }
.blog-sidebar-follow-desc { font-size: 0.88rem; color: var(--text-md); line-height: 1.6; margin-bottom: 14px; }

/* Blog container spacing */
.blog-container-spaced { margin-bottom: 32px; }
```

Under existing `/* --- Moderation Dashboard ---` section (~line 2728), add:

```css
/* Moderation layout */
.mod-container { max-width: 900px; padding: 40px 24px; }
.mod-title { margin-bottom: 8px; }
.mod-subtitle { color: var(--text-md); margin-bottom: 32px; }
.mod-submit-btn { width: 100%; margin-top: 12px; }
.mod-gate-error { color: var(--red); font-size: 0.85rem; margin-top: 10px; }
```

For index.html, reuse existing patterns. Under the component section nearest to the hero/CTA area, add:

```css
/* Index page */
.index-cta-row { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
.index-transparency-pledge { font-size: 1rem; max-width: 680px; margin: 0 auto; }
.index-transparency-pledge strong { color: var(--white); }
.index-pledge-link { color: var(--amber); font-weight: 700; }

/* Section header with flex layout (blog listing on index) */
.section-header-flex { display: flex; justify-content: space-between; align-items: flex-end; flex-wrap: wrap; gap: 16px; }

/* Telegram join button override */
.btn--telegram { background: var(--teal); color: var(--white); }
```

Add responsive overrides for post page in the RESPONSIVE OVERRIDES section:

```css
@media (max-width: 768px) {
  .post-container { padding: 32px 16px 48px 16px; }
  .post-cta-box { padding: 32px 24px; }
  .post-byline { flex-wrap: wrap; gap: 8px; }
}
```

**STEP B: Replace inline styles in post.html (17 inline styles)**

1. Container: `<div class="container" style="max-width: 1000px; padding: 64px 32px 80px 32px;">` -> `<div class="container post-container">`
2. Loading: `<div id="post-loading" style="text-align:center; padding: 80px 0; color: var(--text-md);">` -> `<div id="post-loading" class="post-loading">`
3. Article: `<article id="post-article" style="display:none; max-width:750px; margin:0 auto;">` -> `<article id="post-article" class="post-article" style="display:none;">` (KEEP display:none -- JS toggle)
4. Banner img: `<img id="post-banner" style="display:none; width:100%; ...">` -> `<img id="post-banner" class="post-banner" style="display:none;">` (KEEP display:none -- JS toggle)
5. Tag: `<span id="post-tag" class="blog-tag" style="display:block; margin-bottom:20px;">` -> `<span id="post-tag" class="blog-tag post-tag-block">` (display:block is the visible state set by CSS class)
6. Title: `<h1 id="post-title" style="font-family: var(--font-head); font-size: clamp(2.4rem, ...); ...">` -> `<h1 id="post-title" class="post-title">`
7. Byline container: `<div style="display:flex; align-items:center; gap:14px; ...">` -> `<div class="post-byline">`
8. Author: `<span id="post-author" style="font-weight:700; color:var(--slate);">` -> `<span id="post-author" class="post-byline-author">`
9. Dot separator: `<span style="color:var(--teal); opacity:0.5;">` -> `<span class="post-byline-dot">`
10. Date: `<span id="post-date" style="font-weight:500;">` -> `<span id="post-date" class="post-byline-date">`
11. CTA box: `<div style="margin-top:80px; background:linear-gradient(...); ...">` -> `<div class="post-cta-box">`
12. CTA accent line: `<div style="position:absolute; top:0; ...">` -> `<div class="post-cta-accent">`
13. CTA heading: `<p style="font-family:var(--font-head); font-size:1.75rem; ...">` -> `<p class="post-cta-heading">`
14. CTA text: `<p style="font-size:1.12rem; ...">` -> `<p class="post-cta-text">`
15. CTA button: `<a ... class="btn btn-primary" style="font-size:1.08rem; padding:18px 48px; ...">` -> `<a ... class="btn btn-primary post-cta-btn">`
16. Error div: `<div id="post-error" style="display:none; text-align:center; padding:80px 0;">` -> `<div id="post-error" class="post-error" style="display:none;">` (KEEP display:none -- JS toggle)
17. Error text: `<p style="color:var(--text-md);">` -> `<p>` (styled by `.post-error p`)

**STEP C: Replace inline styles in blog.html (12 inline styles)**

1. Header desc: `<p style="color: var(--text-md); max-width: 560px;">` -> `<p class="blog-header-desc">`
2. Container spaced: `<div class="container" style="margin-bottom: 32px;">` -> `<div class="container blog-container-spaced">`
3. Timeline: `<div class="blog-timeline" id="blog-timeline" style="display:block;">` -> `<div class="blog-timeline" id="blog-timeline">` (display:block is default, remove it)
4. List: `<div class="blog-list" id="blog-list" style="display:none;">` -> keep `style="display:none;"` (JS toggle)
5. Empty state: `<div id="blog-empty" class="empty-state" style="display:none;">` -> keep `style="display:none;"` (JS toggle)
6. Sidebar Telegram desc: `<p style="font-size: 0.88rem; color: var(--text-md); line-height: 1.6; margin-bottom: 24px;">` -> `<p class="blog-sidebar-desc">`
7. Sidebar follow desc: `<p style="font-size: 0.88rem; color: var(--text-md); line-height: 1.6; margin-bottom: 14px;">` -> `<p class="blog-sidebar-follow-desc">`
8. Telegram CTA: `<a href="#" class="btn btn-primary" style="width: 100%; display: block; text-align: center;">` -> `<a href="#" class="btn btn-primary blog-sidebar-cta">`
9. Petition sidebar box: `<div style="margin-top: 32px; background: var(--offwhite); border-radius: 6px; padding: 20px;">` -> `<div class="blog-sidebar-petition">`
10. Petition title: `<h3 style="font-size: 0.95rem; margin-bottom: 10px;">` -> `<h3>` (styled by `.blog-sidebar-petition h3`)
11. Petition desc: `<p style="font-size: 0.85rem; color: var(--text-md); margin-bottom: 14px;">` -> `<p>` (styled by `.blog-sidebar-petition p`)
12. Petition CTA: `<a href="petition.html" class="btn btn-primary" style="width: 100%; display: block; text-align: center;">` -> `<a href="petition.html" class="btn btn-primary blog-sidebar-cta">`

**STEP D: Replace inline styles in moderate.html (7 inline styles)**

1. Submit btn: `<button ... style="width:100%; margin-top: 12px;">` -> `<button ... class="btn btn-primary mod-submit-btn">`
2. Error div: `<div id="mod-gate-error" style="display:none; color: var(--red); ...">` -> `<div id="mod-gate-error" class="mod-gate-error" style="display:none;">` (KEEP display:none)
3. Dashboard: `<div id="mod-dashboard" class="mod-dashboard" style="display:none;">` -> keep `style="display:none;"` (JS toggle)
4. Container: `<div class="container" style="max-width: 900px; padding: 40px 24px;">` -> `<div class="container mod-container">`
5. Title: `<h1 style="margin-bottom: 8px;">` -> `<h1 class="mod-title">`
6. Subtitle: `<p style="color: var(--text-md); margin-bottom: 32px;">` -> `<p class="mod-subtitle">`
7. Posts tab: `<div id="mod-posts" class="mod-tab-content" style="display:none;">` -> keep `style="display:none;"` (JS toggle)

**STEP E: Replace inline styles in index.html (5 inline styles)**

1. CTA row: `<div style="display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;">` -> `<div class="index-cta-row">`
2. Section header flex: `<div class="section-header" style="display:flex; justify-content:space-between; ...">` -> `<div class="section-header section-header-flex">`
3. Transparency pledge: `<p class="text-white" style="font-size: 1rem; max-width: 680px; margin: 0 auto;">` -> `<p class="text-white index-transparency-pledge">`
4. Strong: `<strong style="color: #fff;">` -> `<strong>` (styled by `.index-transparency-pledge strong`)
5. Pledge link: `<a href="donate.html#transparency" style="color: var(--amber); font-weight: 700;">` -> `<a href="donate.html#transparency" class="index-pledge-link">`
6. Telegram btn: `<a href="#" class="btn btn-outline" style="background: var(--teal); color: #fff;">` -> `<a href="#" class="btn btn--telegram">`

**Preservation rules (ALL files):**
- KEEP `style="display:none"` on any element whose visibility is toggled by JavaScript (post-article, post-banner, post-error, blog-list, blog-empty, mod-dashboard, mod-gate-error, mod-posts, celebration-banner)
- REMOVE `style="display:block"` where it's the CSS default (e.g., blog-timeline div)
- If an element already has classes, ADD the new class -- don't replace existing classes
  </action>
  <verify>
    <automated>cd "C:/Users/sangm/OneDrive/_WorkFolder/_Personal/Start-ups/stop_dog_eaters" && echo "=== Presentational inline style audit ===" && for f in website/index.html website/about.html website/petition.html website/blog.html website/post.html website/donate.html website/token.html website/moderate.html; do total=$(grep -c "style=" "$f" 2>/dev/null || echo 0); dynamic=$(grep -c 'style="display:none' "$f" 2>/dev/null || echo 0); echo "$f: $total total, $dynamic dynamic (display:none)"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "style=" website/index.html` shows only `style="display:none"` entries (celebration-banner)
    - `grep "style=" website/post.html` shows only `style="display:none"` entries (post-article, post-banner, post-error)
    - `grep "style=" website/blog.html` shows only `style="display:none"` entries (blog-list, blog-empty)
    - `grep "style=" website/moderate.html` shows only `style="display:none"` entries (mod-dashboard, mod-gate-error, mod-posts)
    - website/css/style.css contains `.post-container`
    - website/css/style.css contains `.post-byline`
    - website/css/style.css contains `.post-cta-box`
    - website/css/style.css contains `.post-cta-accent`
    - website/css/style.css contains `.blog-sidebar-petition`
    - website/css/style.css contains `.mod-container`
    - website/css/style.css contains `.index-cta-row`
    - website/css/style.css contains `.section-header-flex`
    - website/post.html contains `class="post-byline"` exactly once
    - website/post.html contains `class="post-cta-box"` exactly once
    - website/blog.html contains `class="blog-sidebar-petition"` exactly once
    - website/moderate.html contains `class="mod-container"` exactly once
    - website/index.html contains `class="index-cta-row"` exactly once
    - Total presentational inline styles across ALL 8 pages is zero (only JS display:none remains)
  </acceptance_criteria>
  <done>ALL 8 HTML pages have zero presentational inline styles. Only JS-driven display:none toggles remain. CSS file contains complete component classes for every page. DESIGN-03 requirement fully satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 8 pages</name>
  <files>website/index.html, website/about.html, website/petition.html, website/blog.html, website/post.html, website/donate.html, website/token.html, website/moderate.html</files>
  <action>Present all 8 pages to user for visual verification. Start local server if not already running. Provide URLs and expected visual state for each page.</action>
  <what-built>Extracted all ~184 inline styles across 8 HTML pages into semantic CSS classes in style.css. Created ~60 new component classes organized under existing Layer 3 sections. All pages should render identically or better than before.</what-built>
  <how-to-verify>
    1. Start local server: `cd website && python -m http.server 8000`
    2. Open each page in browser and verify layout/colors match expectations:
       - http://localhost:8000/index.html - Hero, stats, blog preview, transparency pledge
       - http://localhost:8000/about.html - Lucky section, principle cards (teal/amber/red borders), fund breakdown
       - http://localhost:8000/petition.html - Target tags (navy/teal pills), notice box (green), share buttons
       - http://localhost:8000/blog.html - Sidebar CTA buttons, petition box, Telegram section
       - http://localhost:8000/post.html?id=2026-03-22-the-growing-movement-against-dog-meat-in-vietnam - Article prose, byline, CTA box
       - http://localhost:8000/donate.html - Tier cards (T1/T2/T3), Most Popular badge, fund tracker
       - http://localhost:8000/token.html - Token info rows (S/P/L/C icons), step numbers, warning banner
       - http://localhost:8000/moderate.html - Login gate, dashboard layout
    3. Check mobile responsiveness: resize browser to 375px width, verify all pages stack correctly
    4. Open DevTools Console on each page: verify zero JavaScript errors
  </how-to-verify>
  <verify>Human visual inspection of all 8 pages confirms layout and colors match expectations</verify>
  <done>All 8 pages visually verified as correct by human reviewer</done>
  <resume-signal>Type "approved" if all pages look correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. Run full inline style audit across all 8 pages -- only JS display:none should remain
2. All new CSS classes exist in style.css under correct Layer 3 component sections
3. All CSS classes use CSS variables for brand colors (no raw hex except non-brand colors)
4. style.css Layer 4 (Utilities) untouched -- all new classes are in Layer 3 (Components)
5. Visual parity confirmed by human verification on all 8 pages
</verification>

<success_criteria>
- post.html: 17 inline styles removed (3 JS display:none preserved)
- blog.html: 12 inline styles removed (2 JS display:none preserved)
- moderate.html: 7 inline styles removed (3 JS display:none preserved)
- index.html: 5 inline styles removed (1 JS display:none preserved)
- TOTAL across all 8 pages: ~184 presentational inline styles removed, only ~10 JS display:none preserved
- DESIGN-03 requirement fully satisfied: developer can maintain CSS without inline style conflicts
- Human visual verification passed on all 8 pages
</success_criteria>

<output>
After completion, create `.planning/phases/08-css-refactoring-foundation/08-03-SUMMARY.md`
</output>
