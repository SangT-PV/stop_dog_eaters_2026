---
phase: 16-rebuild-discussion-and-share-your-research-ui-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - website/css/style.css
  - website/js/community-posts.js
autonomous: false
requirements: [REQ-16-02]

must_haves:
  truths:
    - "User sees community post form with editorial aesthetic (generous whitespace, refined typography)"
    - "Form uses v2.0 design tokens for consistent styling with rest of site"
    - "Formatting toolbar buttons use Lucide icons (not text B/I/U)"
    - "Locked state progress bar uses semantic color tokens"
    - "Character counter uses semantic typography scale"
  artifacts:
    - path: "website/css/style.css"
      provides: "Updated .community-* classes with v2.0 design tokens"
      min_lines: 50
      pattern: "(--radius-lg|var\\(--)"
    - path: "website/js/community-posts.js"
      provides: "Updated toolbar SVG icons (Lucide format)"
      pattern: "<svg.*lucide"
  key_links:
    - from: "website/css/style.css"
      to: "CSS custom properties"
      via: "var(--radius-lg), rem spacing values"
      pattern: "community-.*var\\(--"
    - from: "website/js/community-posts.js"
      to: "SVG icon definitions"
      via: "Bold/Italic/Underline Lucide icons"
      pattern: "svg.*stroke.*currentColor"
---

<objective>
Apply v2.0 editorial aesthetic to community post submission form, replacing text-based toolbar buttons with Lucide icons and using generous whitespace for clean form design.

Purpose: Align community post form with v2.0 design system, emphasizing readability and editorial refinement over activism urgency (this is a submission tool, not an engagement component).

Output: CSS refactor of .community-* classes + JS update for Lucide toolbar icons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-rebuild-discussion-and-share-your-research-ui-components/16-RESEARCH.md
@BRAND_GUIDELINES.md
@website/css/style.css
@website/js/community-posts.js
</context>

<interfaces>
<!-- Key CSS custom properties and component patterns -->

**From website/css/style.css (current tokens, verified 2026-03-24):**
```css
/* Spacing - using rem values (Phase 8 would create --space-* tokens) */
/* 0.5rem = 8px, 0.75rem = 12px, 1rem = 16px, 1.5rem = 24px, 2rem = 32px */

/* Border Radius */
--radius:      0.875rem;    /* 14px */
--radius-lg:   1.75rem;     /* 28px — large activism aesthetic */
--radius-pill: 9999px;      /* Full pill */

/* Shadows */
--shadow-sm: 0 2px 8px rgba(38,70,83,0.07);
--shadow:    0 4px 24px rgba(38,70,83,0.10);

/* Colors */
--white:     #ffffff;
--mist:      #F8F9FA;
--teal:      #2A9D8F;
--border:    #e2e8f0;
--text:      #1a2530;
--text-md:   #4a5563;
```

**Current .community-* Classes (lines 2721-2743):**
```css
.community-locked            /* Locked state card */
.community-locked-icon       /* Pencil icon container */
.community-locked-progress   /* Progress bar container */
.community-locked-bar        /* Progress fill */
.community-submit-section    /* Unlocked form container */
.community-form-row          /* Name/email grid row */
.comment-editor              /* Textarea wrapper (shared with comments) */
.comment-toolbar             /* Formatting buttons */
.comment-textarea            /* Content input (shared with comments) */
```

**CommunityPosts.renderSubmissionForm() Structure (lines 83-165):**
```javascript
renderSubmissionForm() {
  container.innerHTML = `
    <div class="community-submit-section">
      <h3>Share Your Research</h3>
      <form class="community-form">
        <!-- Name/email row -->
        <input name="author_name" />
        <input name="author_email" />
        <!-- Title input -->
        <input name="title" />
        <!-- Tag dropdown -->
        <select name="tag">...</select>
        <!-- Formatting toolbar (current: text buttons B/I/U) -->
        <div class="comment-toolbar">
          <button type="button" data-format="bold">B</button>
          <button type="button" data-format="italic">I</button>
          <button type="button" data-format="underline">U</button>
        </div>
        <!-- Content textarea -->
        <textarea name="content"></textarea>
        <!-- Character counter -->
        <div class="community-char-count">0/5000</div>
        <!-- Submit button -->
        <button type="submit">Submit for Review</button>
      </form>
    </div>
  `;
}
```

**Lucide Icon Patterns (Phase 12 social sharing reference):**
```javascript
// Bold icon (Type)
`<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <path d="M6 4h8a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"></path>
  <path d="M6 12h9a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"></path>
</svg>`

// Italic icon
`<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <line x1="19" y1="4" x2="10" y2="4"></line>
  <line x1="14" y1="20" x2="5" y2="20"></line>
  <line x1="15" y1="4" x2="9" y2="20"></line>
</svg>`

// Underline icon
`<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
  <path d="M6 3v7a6 6 0 0 0 6 6 6 6 0 0 0 6-6V3"></path>
  <line x1="4" y1="21" x2="20" y2="21"></line>
</svg>`
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update .community-locked with v2.0 design tokens and generous spacing</name>
  <files>website/css/style.css</files>
  <action>
Update .community-locked classes (line 2721) to use rem-based spacing for editorial aesthetic:

**Current issues:**
- padding: 40px 32px → replace with rem values
- border-radius: var(--radius-lg) ✅ (already correct)
- Icon size hardcoded (2.5rem)

**Changes:**
```css
.community-locked {
  text-align: center;
  padding: 2rem 1.5rem;  /* 32px 24px, generous whitespace (Phase 8 would use --space-8 --space-6) */
  background: var(--mist);
  border-radius: var(--radius-lg);
  border: 2px dashed var(--border);
}

.community-locked-icon {
  font-size: 3rem;  /* Increase from 2.5rem for prominence */
  margin-bottom: 1rem;  /* 16px (Phase 8 would use --space-4) */
  color: var(--text-md);
}

.community-locked h3 {
  font-size: 1.125rem;  /* 18px (Phase 8 would use --text-lg) */
  margin-bottom: 0.75rem;  /* 12px (Phase 8 would use --space-3) */
  font-weight: 700;
}

.community-locked p {
  max-width: 420px;
  margin: 0 auto 1.5rem;  /* 24px bottom margin (Phase 8 would use --space-6) */
  font-size: 1rem;  /* 16px (Phase 8 would use --text-base) */
  color: var(--text-md);
}

.community-locked .btn {
  margin-bottom: 1rem;  /* 16px (Phase 8 would use --space-4) */
}
```

**Progress bar:**
```css
.community-locked-progress {
  height: 8px;
  background: var(--border);
  border-radius: var(--radius-pill);
  overflow: hidden;
  max-width: 300px;
  margin: 0 auto 0.5rem;  /* 8px bottom (Phase 8 would use --space-2) */
}

.community-locked-bar {
  height: 100%;
  background: var(--teal);  /* Use semantic token */
  border-radius: var(--radius-pill);
  transition: width 0.6s ease;
}

.community-locked-amount {
  font-size: 0.875rem;  /* 14px (Phase 8 would use --text-sm) */
  color: var(--text-md);
  font-weight: 600;
}
```

DO NOT change:
- Fund-gating logic (CommunityPosts.renderLocked())
- Progress calculation
  </action>
  <verify>
    <automated>grep -n "community-locked" "website/css/style.css" | grep -i "padding: 2rem"</automated>
  </verify>
  <done>
.community-locked uses 2rem and 1.5rem for generous padding.
Typography scale uses rem values (1.125rem, 1rem, 0.875rem).
Progress bar uses var(--teal) semantic token.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .community-submit-section form with v2.0 spacing and shadows</name>
  <files>website/css/style.css</files>
  <action>
Update .community-submit-section (line 2730) to use rem-based spacing and elevated card style:

**Current issues:**
- padding: 32px → replace with 2rem
- border: 2px solid var(--border) ✅ (already good)
- No box-shadow → add subtle elevation

**Changes:**
```css
.community-submit-section {
  background: var(--white);
  border: 2px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 2rem;  /* 32px (Phase 8 would use --space-8) */
  box-shadow: var(--shadow-sm);  /* Subtle elevation */
}

.community-submit-section h3 {
  font-size: 1.125rem;  /* 18px (Phase 8 would use --text-lg) */
  margin-bottom: 0.75rem;  /* 12px (Phase 8 would use --space-3) */
  font-weight: 700;
}

.community-submit-section > p {
  color: var(--text-md);
  margin-bottom: 1.5rem;  /* 24px, generous spacing (Phase 8 would use --space-6) */
  font-size: 1rem;  /* 16px (Phase 8 would use --text-base) */
}
```

**Form row grid:**
```css
.community-form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;  /* 16px gap (Phase 8 would use --space-4) */
  margin-bottom: 1rem;  /* 16px (Phase 8 would use --space-4) */
}

@media (max-width: 640px) {
  .community-form-row {
    grid-template-columns: 1fr;  /* Stack on mobile */
  }
}
```

**Input styling (shared with .comment-input):**
```css
.comment-input {
  border: 2px solid var(--border);
  border-radius: var(--radius);
  padding: 0.75rem 1rem;  /* 12px 16px (Phase 8 would use --space-3 --space-4) */
  font-size: 1rem;  /* 16px (Phase 8 would use --text-base) */
  transition: border-color 0.2s ease, box-shadow 0.2s ease;
}

.comment-input:focus {
  outline: none;
  border-color: var(--teal);
  box-shadow: 0 0 0 3px rgba(42, 157, 143, 0.1);  /* Teal focus ring */
}
```

DO NOT change:
- Form validation logic
- Submit handler (CommunityPosts.submitPost())
  </action>
  <verify>
    <automated>grep -n "community-submit-section" "website/css/style.css" | grep -i "var(--"</automated>
  </verify>
  <done>
.community-submit-section uses 2rem padding and var(--shadow-sm).
.community-form-row uses 1rem gap.
.comment-input has teal focus ring.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace text toolbar buttons with Lucide SVG icons</name>
  <files>website/js/community-posts.js</files>
  <action>
Update renderSubmissionForm() method (lines 83-165) to use Lucide SVG icons instead of text buttons:

**Current toolbar (line ~110):**
```html
<div class="comment-toolbar">
  <button type="button" data-format="bold">B</button>
  <button type="button" data-format="italic">I</button>
  <button type="button" data-format="underline">U</button>
</div>
```

**Replace with Lucide icons:**
```html
<div class="comment-toolbar">
  <button type="button" data-format="bold" aria-label="Bold">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M6 4h8a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"></path>
      <path d="M6 12h9a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z"></path>
    </svg>
  </button>
  <button type="button" data-format="italic" aria-label="Italic">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <line x1="19" y1="4" x2="10" y2="4"></line>
      <line x1="14" y1="20" x2="5" y2="20"></line>
      <line x1="15" y1="4" x2="9" y2="20"></line>
    </svg>
  </button>
  <button type="button" data-format="underline" aria-label="Underline">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M6 3v7a6 6 0 0 0 6 6 6 6 0 0 0 6-6V3"></path>
      <line x1="4" y1="21" x2="20" y2="21"></line>
    </svg>
  </button>
</div>
```

**Why Lucide icons:**
- Consistency with Phase 12 social share buttons
- Accessible with aria-label
- Scalable SVG (no pixelation)
- Brand guidelines specify Lucide as preferred icon library

DO NOT change:
- handleFormatting() method (line 167-183) — markdown syntax insertion logic
- Event listeners (setupEventListeners already handles data-format attribute)
  </action>
  <verify>
    <automated>grep -n "data-format=\"bold\"" "website/js/community-posts.js" | head -1</automated>
  </verify>
  <done>
Toolbar buttons render Lucide SVG icons (Bold, Italic, Underline).
Each button has aria-label for accessibility.
data-format attributes preserved for event handling.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update .comment-toolbar CSS for icon button styling</name>
  <files>website/css/style.css</files>
  <action>
Add CSS for icon-based toolbar buttons (currently no styling for SVG icons):

**Current .comment-toolbar:**
Likely minimal or no styles for buttons. Add v2.0 interaction states:

```css
.comment-toolbar {
  display: flex;
  gap: 0.5rem;  /* 8px gap between buttons (Phase 8 would use --space-2) */
  margin-bottom: 0.75rem;  /* 12px bottom margin (Phase 8 would use --space-3) */
}

.comment-toolbar button {
  background: var(--mist);
  border: 2px solid var(--border);
  border-radius: var(--radius);
  padding: 0.5rem;  /* 8px, square buttons (Phase 8 would use --space-2) */
  cursor: pointer;
  color: var(--text);
  transition: background-color 0.2s ease, border-color 0.2s ease;
  width: 2.5rem;   /* 40px square */
  height: 2.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.comment-toolbar button:hover {
  background: var(--white);
  border-color: var(--teal);
  color: var(--teal);
}

.comment-toolbar button:active {
  background: rgba(42, 157, 143, 0.1);  /* Teal tint on click */
}

.comment-toolbar button svg {
  width: 20px;
  height: 20px;
  pointer-events: none;  /* Prevent SVG from blocking click events */
}
```

**Character counter (add if missing):**
```css
.community-char-count {
  font-size: 0.875rem;  /* 14px (Phase 8 would use --text-sm) */
  color: var(--text-md);
  text-align: right;
  margin-top: 0.5rem;  /* 8px (Phase 8 would use --space-2) */
  margin-bottom: 1rem;  /* 16px (Phase 8 would use --space-4) */
}
```

DO NOT change:
- Toolbar event handling
- Markdown syntax insertion
  </action>
  <verify>
    <automated>grep -n "comment-toolbar button" "website/css/style.css" | head -1</automated>
  </verify>
  <done>
.comment-toolbar buttons have hover states (teal border).
Buttons are 40x40px squares with centered SVG icons.
Transitions smooth (0.2s ease).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Community post submission form with v2.0 editorial aesthetic (generous whitespace, Lucide icons, refined typography)</what-built>
  <how-to-verify>
1. Open blog.html and scroll to "Share Your Research" section
2. If locked state visible (funds < $2.5K):
   - Verify large padding and generous whitespace
   - Progress bar should use teal fill
   - Typography should be clean and readable
3. If unlocked (funds ≥ $2.5K or admin mode):
   - Verify form has elevated card style with subtle shadow
   - Formatting toolbar shows 3 icon buttons (Bold, Italic, Underline) — NOT text "B/I/U"
   - Hover over toolbar buttons → teal border appears
   - Click into any input field → teal focus ring appears
   - Form layout should have generous spacing (not cramped)
4. Test functionality:
   - Click Bold icon → inserts markdown syntax around selected text
   - Type in textarea → character counter updates (0/5000)
   - Submit form → shows success message (pending approval)

Expected outcome: Form uses editorial aesthetic with Lucide icons, generous whitespace, and v2.0 design tokens throughout.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe visual issues</resume-signal>
</task>

</tasks>

<verification>
1. .community-locked uses rem-based spacing values
2. .community-submit-section has elevated card style with shadow
3. Toolbar buttons render Lucide SVG icons (not text)
4. All inputs have teal focus rings
5. Typography scale consistent using rem values (0.875rem, 1rem, 1.125rem)
6. Locked state progress bar uses semantic var(--teal) token
</verification>

<success_criteria>
- User sees community post form with editorial aesthetic (generous whitespace, refined typography)
- Formatting toolbar uses Lucide icons (Bold, Italic, Underline SVGs)
- Form inputs have teal focus rings matching v2.0 pattern
- Locked state uses semantic color tokens (var(--teal) for progress bar)
- All visual changes verified by human inspection (checkpoint passed)
</success_criteria>

<output>
After completion, create `.planning/phases/16-rebuild-discussion-and-share-your-research-ui-components/16-02-SUMMARY.md`
</output>
