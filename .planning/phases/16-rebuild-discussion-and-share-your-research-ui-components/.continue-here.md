---
phase: 16-rebuild-discussion-and-share-your-research-ui-components
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-03-24T16:17:30.947Z
---

<current_state>
Both UI fixes are implemented and browser-verified. User's final request: add mock data so both components can be tested without manually entering comments each time. Code changes are uncommitted.
</current_state>

<completed_work>

- Task 1: Fix Discussion layout bug — Done
  - `website/js/comments.js`: Added `.chat-message-wrapper` (display:block) wrapping `.chat-message` (flex row) + `.chat-replies` (block sibling). Replies no longer render as horizontal flex children.
  - Updated `handleReplyClick()` to use `.chat-message-wrapper` instead of `.chat-message` for DOM traversal.
  - `website/css/style.css`: Added `.chat-message-wrapper { display: block; }` rule.

- Task 2: Fix Share Your Research form — Done
  - `website/css/style.css`: Added all missing CSS for classes generated by `community-posts.js`: `.comment-input`, `.comment-textarea`, `.comment-editor`, `.comment-toolbar`, `.toolbar-btn`, `.comment-form-footer`, `.comment-form-notice`, `.comment-char-count`. Uses SDE design tokens (--teal, --border, --radius, etc.)

- Task 3: Browser verification — Done
  - Discussion: 3-level threaded nesting confirmed (Tuan Anh → Hieu → Siva) with proper vertical stacking and indentation. Screenshots in `.planning/.testing/`.
  - Share Your Research: Form renders with styled inputs, toolbar, textarea, footer. Verified on blog.html with admin mode.
</completed_work>

<remaining_work>

- Task 4: Add mock data for easy testing
  - Create `website/data/comments/{slug}-comments.json` for 2-3 posts with flat threaded comments (parent_id format, NOT nested — see `data/comments/_schema.json`)
  - Update `website/data/community-posts/submissions.json` with sample submissions (see `data/community-posts/_schema.json`)
  - Commit all changes (both code fixes + mock data)
</remaining_work>

<decisions_made>

- `.chat-message-wrapper` block div wraps flex row + replies — keeps avatar+bubble in flex while replies stack below as block sibling
- Comments data must be **flat array with parent_id** — `buildCommentTree()` in comments.js builds the tree; pre-nested replies won't work
- Admin mode requires BOTH `localStorage('sde-admin-mode','true')` AND `sessionStorage('sde-admin-authenticated','true')` — password-protected via admin-utils.js
- Test data file was created then deleted during verification — need to recreate as permanent mock data
</decisions_made>

<blockers>
None
</blockers>

<context>
User was frustrated after multiple review sessions that identified issues but never fixed them. This session finally implemented both fixes and verified them end-to-end. The user wants permanent mock data so they can test both components easily after /clear without needing to manually add comments.

Key file locations:
- Comment schema: `website/data/comments/_schema.json`
- Community post schema: `website/data/community-posts/_schema.json`
- Existing post slugs: `website/data/posts/*.json` (11 posts)
- Admin unlock: set localStorage + sessionStorage, or use `?admin=true` URL param with password `sde-moderate-2026`
</context>

<next_action>
Start with: Create mock comment files for 2-3 post slugs using flat format with parent_id (use existing slugs from data/posts/). Then populate submissions.json with 2-3 sample community submissions. Finally commit everything: `git add website/css/style.css website/js/comments.js website/data/comments/ website/data/community-posts/submissions.json && git commit`.
</next_action>
