---
phase: 10-data-visualizations
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - website/blog.html
  - website/css/style.css
autonomous: false

requirements: [VIZ-04]

must_haves:
  truths:
    - "User sees compact Key Statistics widget in blog.html sidebar with 3 stat callouts"
    - "Blog sidebar stat callouts use same visual style as index.html stat callouts"
    - "User visually confirms charts render correctly on desktop and mobile"
  artifacts:
    - path: "website/blog.html"
      provides: "Sidebar Key Statistics widget with 3 compact stat callouts"
      contains: "stat-callout"
  key_links:
    - from: "website/blog.html"
      to: "website/css/style.css"
      via: "stat-callout CSS classes"
      pattern: "stat-callout"
---

<objective>
Add compact "Key Statistics" widget to the blog.html sidebar (per D-15) and perform human visual verification of all data visualizations from Plan 10-01.

Purpose: Extend stat callout visibility to blog readers (the most frequent visitors) and confirm charts render correctly across viewports.

Output: Updated blog.html with sidebar stats widget, visual verification of complete Phase 10 data visualizations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-data-visualizations/10-CONTEXT.md
@.planning/phases/10-data-visualizations/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 10-01 outputs -->

From website/css/style.css — stat-callout component (compact variant needed for sidebar):
```css
.stat-callout {
  text-align: left;
  padding: 2rem 2rem 2rem 2.5rem;
  border-radius: var(--radius-lg);
  border-left: 4px solid var(--red);
  background: var(--white);
  box-shadow: var(--shadow-sm);
}
.stat-callout__number { font-size: 3rem; color: var(--teal); }
.stat-callout__label { font-size: 0.875rem; text-transform: uppercase; }
```

From website/blog.html — sidebar structure (insert widget after "Subscribe" section):
```html
<aside class="blog-sidebar">
  <h3>Filter by Topic</h3>
  <div class="sidebar-tag-list" id="tag-filter">...</div>

  <h3>About the Content</h3>
  <p class="blog-sidebar-desc">...</p>

  <h3>Subscribe</h3>
  <p>...</p>
  <a href="#" class="btn btn-primary blog-sidebar-cta">Join Telegram Channel</a>

  <!-- INSERT KEY STATISTICS WIDGET HERE (per D-15) -->

  <div class="blog-sidebar-petition">
    <h3>Sign the Petition</h3>
    ...
  </div>
</aside>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Key Statistics widget to blog.html sidebar</name>
  <files>website/blog.html, website/css/style.css</files>
  <read_first>
    - website/blog.html (full file — locate sidebar section, find insertion point before petition div)
    - website/css/style.css (stat-callout styles — confirm compact variant is needed or base is sufficient)
    - .planning/phases/10-data-visualizations/10-CONTEXT.md (D-15: blog.html sidebar as "Key Statistics" widget with compact stat callouts, not full charts)
  </read_first>
  <action>
**Add Key Statistics sidebar widget to blog.html (per D-15):**

Insert BEFORE the `<div class="blog-sidebar-petition">` block in the sidebar. Use compact stat callouts (not full charts — D-15 specifies "compact stat callouts, not full charts"):

```html
<!-- Key Statistics (per D-15: compact stat callouts in sidebar) -->
<div class="sidebar-stats">
  <h3>Key Statistics</h3>
  <div class="sidebar-stat-list">
    <div class="stat-callout stat-callout--compact">
      <div class="stat-callout__number">5M+</div>
      <div class="stat-callout__label">Dogs killed annually</div>
    </div>
    <div class="stat-callout stat-callout--compact">
      <div class="stat-callout__number">95%</div>
      <div class="stat-callout__label">Public support</div>
    </div>
    <div class="stat-callout stat-callout--compact">
      <div class="stat-callout__number">0</div>
      <div class="stat-callout__label">Registered slaughterhouses</div>
    </div>
  </div>
  <a href="index.html#data-research" class="btn btn-secondary blog-sidebar-cta">See Full Data</a>
</div>
```

Note: These are STATIC values (no data-count animation) because the sidebar is compact and always visible. Animation reserved for the index.html Data & Research section (per D-13).

**Also add compact stat-callout variant CSS to style.css.** Add after the existing `.stat-callout__label` block (~line 826):

```css
/* Compact stat callout for sidebar (Phase 10 per D-15) */
.stat-callout--compact {
  padding: 1rem 1rem 1rem 1.25rem;
  border-left-width: 3px;
}
.stat-callout--compact .stat-callout__number {
  font-size: 1.8rem;
}
.stat-callout--compact .stat-callout__label {
  font-size: 0.75rem;
}

.sidebar-stats {
  margin-top: 2rem;
}
.sidebar-stat-list {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  margin-bottom: 1rem;
}
```
  </action>
  <verify>
    <automated>grep -c "stat-callout" website/blog.html</automated>
  </verify>
  <acceptance_criteria>
    - blog.html contains `<div class="sidebar-stats">` with `<h3>Key Statistics</h3>`
    - blog.html contains 3 `.stat-callout.stat-callout--compact` elements with values "5M+", "95%", "0"
    - blog.html contains link to `index.html#data-research` ("See Full Data")
    - Widget is placed in sidebar BEFORE the petition block
    - style.css contains `.stat-callout--compact` modifier with reduced padding and font size
    - style.css contains `.sidebar-stats` and `.sidebar-stat-list` layout classes
  </acceptance_criteria>
  <done>Blog sidebar shows compact Key Statistics widget with 3 stat callouts and link to full data research section on index.html</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 10 data visualizations</name>
  <files>website/index.html, website/blog.html</files>
  <action>
Human visual verification of all Phase 10 data visualizations. Start local server and verify charts, stat callouts, and responsive behavior across desktop and mobile viewports.
  </action>
  <what-built>
    Complete data visualization suite for Phase 10:
    1. Disease trend line chart (rabies deaths + E. coli reports, 2018-2026) on index.html
    2. Public opinion bar chart (70% 2019 to 95% 2021) on index.html
    3. Three stat callout cards with count-up animation on index.html
    4. Compact Key Statistics sidebar widget on blog.html
    5. Responsive layouts for all viewports
  </what-built>
  <how-to-verify>
    Start local server: `cd website && python -m http.server 8000`

    **Desktop verification (http://localhost:8000/index.html):**
    1. Scroll to "Data & Research" section (between Lucky's Story and "Three Ways")
    2. Verify 3 stat callout cards show count-up animation (5M+, 95%, 0) when scrolled into view
    3. Verify disease trend line chart renders with RED line (Rabies Deaths) and AMBER/YELLOW line (E. coli Reports)
    4. Hover over chart points — tooltips show exact values
    5. Verify opinion bar chart renders with TEAL bars showing 70% (2019) and 95% (2021)
    6. Verify each chart has a small source footnote below it
    7. Check console for errors (F12 > Console)

    **Mobile verification (resize to 375px width or use DevTools mobile mode):**
    8. Stat callouts stack vertically (1 column)
    9. Charts stack vertically (1 column)
    10. Chart height does not exceed 280px
    11. No horizontal scrollbar

    **Blog sidebar (http://localhost:8000/blog.html):**
    12. Scroll to sidebar — verify "Key Statistics" widget visible with 3 compact stat callouts
    13. Click "See Full Data" link — navigates to index.html#data-research
    14. Verify compact callouts are visually smaller than full callouts on index.html

    **Reduced motion (optional):**
    15. Enable "Reduce motion" in OS settings
    16. Refresh index.html — stat callout numbers show final values immediately (no animation)
  </how-to-verify>
  <verify>
    <automated>echo "Human visual verification — no automated check"</automated>
  </verify>
  <done>All Phase 10 visualizations verified: charts render correctly, stat callouts animate, responsive layouts work, no console errors</done>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "stat-callout" website/blog.html` returns 6+ (3 divs with stat-callout class + stat-callout--compact)
2. `grep "See Full Data" website/blog.html` returns the link to index.html#data-research
3. Visual confirmation: charts render, callouts animate, responsive layouts work
4. No console errors on index.html or blog.html
</verification>

<success_criteria>
- Blog sidebar shows compact Key Statistics widget with 3 callouts and "See Full Data" link
- All Phase 10 visualizations verified visually on desktop and mobile
- Charts render with correct colors (red/amber for disease, teal for opinion)
- Stat callout count-up animation fires on scroll
- No console errors on any page
- Responsive behavior correct at all breakpoints
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-visualizations/10-02-SUMMARY.md`
</output>
