---
phase: 17-stitch-design-system-implementation
plan: 05
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - website/donate.html
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "Donate hero has italic Newsreader headline with teal border-left accent on subtext"
    - "Two platform cards (Change.org featured, Kickstarter) with hover translateY(-4px)"
    - "Three Kickstarter tier cards with T2 as featured (scale-105, shadow-xl, gold top border)"
    - "Transparency section has 5/7 grid: pledge left with border-l-8, fund dashboard right"
    - "Fund tracker dashboard preserved with live metrics (total raised, spent, balance)"
    - "SDE Token CTA banner with teal gradient background"
  artifacts:
    - path: "website/donate.html"
      provides: "Redesigned donate page matching Stitch 05-donate"
      contains: "material-symbols-outlined"
    - path: "website/css/style.css"
      provides: "Donate page component styles"
      contains: "donate-hero"
  key_links:
    - from: "website/donate.html"
      to: "website/js/fund-tracker.js"
      via: "script tag"
      pattern: "js/fund-tracker.js"
---

<objective>
Redesign the donate page (donate.html) to match Stitch 05-donate design. Update hero, platform cards, Kickstarter tiers, transparency section with fund tracker dashboard, and SDE token CTA.

Purpose: The donate page drives funding. Stitch design adds professional credibility with institutional-grade financial display.

Output: Updated donate.html and CSS donate components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/05-donate.html
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign donate hero, platform cards, and tier cards</name>
  <files>website/donate.html, website/css/style.css</files>
  <read_first>
    - website/donate.html (ALL current HTML — understand fund-tracker integration)
    - website/css/style.css (donate-related rules, fund-tracker rules)
    - .planning/stitch-reference/05-donate.html (Stitch target design)
  </read_first>
  <action>
**Donate hero:**
```css
.donate-hero {
  padding: var(--section-py) var(--section-px);
  max-width: var(--max-w);
  margin: 0 auto;
}
.donate-hero .eyebrow {
  color: var(--primary-container);
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  font-size: 0.875rem;
  margin-bottom: 1rem;
}
.donate-hero h1 {
  font-family: var(--font-head);
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 700;
  font-style: italic;
  color: var(--primary);
  line-height: 1.1;
  letter-spacing: -0.02em;
  margin-bottom: 2rem;
}
.donate-hero p {
  font-size: 1.25rem;
  color: var(--on-surface-variant);
  line-height: 1.7;
  max-width: 42rem;
  border-left: 4px solid var(--primary-container);
  padding-left: 2rem;
}
```

**Platform cards (2-column grid):**
```css
.platform-cards {
  background: var(--surface-container-low);
  padding: 5rem var(--section-px);
}
.platform-cards-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  max-width: var(--max-w);
  margin: 0 auto;
}
@media (max-width: 768px) {
  .platform-cards-grid { grid-template-columns: 1fr; }
}
.platform-card {
  background: var(--surface);
  padding: 3rem;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  transition: transform 0.3s;
  position: relative;
  overflow: hidden;
}
.platform-card:hover { transform: translateY(-4px); }
.platform-card .featured-badge {
  display: inline-block;
  padding: 0.25rem 0.75rem;
  background: rgba(0,108,81,0.1);
  color: var(--secondary);
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  margin-bottom: 1.5rem;
}
.platform-card h2 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  margin-bottom: 1rem;
}
.platform-card p {
  color: var(--on-surface-variant);
  font-size: 1.125rem;
  line-height: 1.7;
  margin-bottom: 2rem;
}
.platform-card-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-weight: 700;
  color: var(--primary-container);
  transition: gap 0.2s;
}
.platform-card-link:hover { gap: 1rem; }
.platform-card-icon {
  position: absolute;
  top: 0;
  right: 0;
  padding: 1rem;
  opacity: 0.2;
  font-size: 3.75rem;
}
```

**Kickstarter tier cards (3-column grid):**
```css
.tier-section {
  padding: var(--section-py) var(--section-px);
  max-width: var(--max-w);
  margin: 0 auto;
}
.tier-section h2 {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-style: italic;
  color: var(--primary);
  letter-spacing: -0.02em;
}
.tier-section .accent-bar {
  height: 4px;
  width: 5rem;
  background: var(--amber);
  margin-top: 1rem;
  margin-bottom: 4rem;
}
.tier-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}
@media (max-width: 768px) {
  .tier-grid { grid-template-columns: 1fr; }
}
.tier-card {
  padding: 2.5rem;
  border-top: 2px solid var(--outline-variant);
}
.tier-card h3 {
  font-family: var(--font-head);
  font-size: 1.5rem;
  margin-bottom: 1rem;
}
.tier-card p {
  color: var(--on-surface-variant);
  margin-bottom: 2rem;
}
.tier-card button {
  width: 100%;
  padding: 1rem;
  border: 2px solid var(--primary);
  background: transparent;
  color: var(--primary);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  cursor: pointer;
  transition: all 0.2s;
}
.tier-card button:hover {
  background: var(--primary);
  color: var(--on-primary);
}
.tier-card--featured {
  background: var(--surface-container-highest);
  border-top: 4px solid var(--amber);
  box-shadow: var(--shadow-lg);
  transform: scale(1.05);
  position: relative;
  z-index: 10;
}
.tier-card--featured .most-popular {
  position: absolute;
  top: -1rem;
  left: 50%;
  transform: translateX(-50%);
  background: var(--amber);
  color: var(--primary);
  padding: 0.25rem 1rem;
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: -0.02em;
}
.tier-card--featured button {
  background: var(--primary-container);
  color: var(--on-primary);
  border-color: var(--primary-container);
}
.tier-card--locked {
  opacity: 0.6;
  filter: grayscale(1);
}
.tier-card--locked button {
  border-color: var(--outline);
  color: var(--outline);
  cursor: not-allowed;
}
```

**Update donate.html HTML:**
1. Replace current hero with `.donate-hero` structure: eyebrow "Our Mission", italic headline "Support the Campaign", border-left subtext
2. Platform cards section with Change.org (featured) and Kickstarter
3. Kickstarter tiers: T1 "Awareness Advocate", T2 "Community Dialogue Sponsor" (featured with "Most Popular" badge), T3 "Coming Soon" (locked)
4. Add Material Symbols icons for platform card decorative icons (`campaign` for Change.org) and arrow_forward for links

**CRITICAL**: Do NOT modify the transparency section or fund-tracker section yet (Task 2). Keep all existing `id` attributes and `<script>` tags intact.
  </action>
  <verify>
    <automated>grep -c "donate-hero" website/css/style.css && grep -c "platform-card" website/css/style.css && grep -c "tier-card--featured" website/css/style.css && grep -c "material-symbols-outlined" website/donate.html</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.donate-hero` rule (grep >= 1)
    - style.css has `.platform-card` rules (grep >= 1)
    - style.css has `.tier-card--featured` with scale and shadow (grep >= 1)
    - donate.html has Material Symbols icons (grep >= 3)
    - All script tags preserved
    - Fund tracker section HTML not modified yet
  </acceptance_criteria>
  <done>Donate hero, platform cards, and Kickstarter tier cards match Stitch design.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign transparency section, fund dashboard, and SDE token CTA</name>
  <files>website/donate.html, website/css/style.css</files>
  <read_first>
    - website/donate.html (current transparency and fund-tracker sections)
    - website/css/style.css (fund-tracker rules, transparency rules)
    - .planning/stitch-reference/05-donate.html (Stitch transparency section)
  </read_first>
  <action>
**Transparency section (12-col grid: 5 pledge / 7 dashboard):**
```css
.transparency-section {
  padding: var(--section-py) var(--section-px);
  background: var(--surface-container-low);
  border-top: 1px solid var(--outline-variant);
  border-bottom: 1px solid var(--outline-variant);
}
.transparency-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 4rem;
  max-width: var(--max-w);
  margin: 0 auto;
}
@media (min-width: 1024px) {
  .transparency-grid {
    grid-template-columns: 5fr 7fr;
  }
}
```

Pledge card:
```css
.transparency-pledge {
  background: var(--surface);
  padding: 2rem;
  border-left: 8px solid var(--primary);
  box-shadow: var(--shadow-sm);
}
.transparency-pledge p {
  color: var(--on-surface-variant);
  line-height: 1.7;
  margin-bottom: 1.5rem;
}
.transparency-pledge li {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  margin-bottom: 1rem;
}
.transparency-pledge li .material-symbols-outlined {
  color: var(--primary-container);
  margin-top: 0.125rem;
}
```

Fund tracker dashboard header:
```css
.fund-dashboard-header {
  background: var(--primary);
  color: var(--on-primary);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0.5rem 1rem;
  margin-bottom: 1.5rem;
  font-size: 0.625rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.2em;
}
.fund-dashboard-header .last-updated {
  opacity: 0.7;
  font-style: italic;
  font-weight: 500;
}
```

Metric cards (3-col grid):
```css
.fund-metrics {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
  margin-bottom: 2rem;
}
.fund-metric-card {
  background: var(--surface);
  padding: 1.5rem;
}
.fund-metric-card .label {
  font-size: 0.75rem;
  text-transform: uppercase;
  font-weight: 700;
  letter-spacing: 0.15em;
  color: var(--on-surface-variant);
  margin-bottom: 0.5rem;
}
.fund-metric-card .value {
  font-family: var(--font-head);
  font-size: 1.875rem;
}
.fund-metric-card .value--raised { color: var(--amber); }
.fund-metric-card .value--spent { color: var(--error); }
.fund-metric-card .value--balance { color: var(--primary-container); }
```

Allocation table:
```css
.fund-allocation {
  background: var(--surface);
  padding: 2rem;
}
.fund-allocation h3 {
  font-family: var(--font-head);
  font-size: 1.25rem;
  margin-bottom: 1.5rem;
}
.fund-progress-row {
  margin-bottom: 1.5rem;
}
.fund-progress-row .row-header {
  display: flex;
  justify-content: space-between;
  font-size: 0.875rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}
.fund-progress-bar {
  width: 100%;
  height: 0.375rem;
  background: var(--surface-container-highest);
}
.fund-progress-fill {
  height: 100%;
  background: var(--primary-container);
}
.fund-sources {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: 2rem;
  padding-top: 1.5rem;
  border-top: 1px solid var(--outline-variant);
}
.fund-source-badge {
  padding: 0.25rem 0.75rem;
  background: var(--surface-container-high);
  border-radius: var(--radius-pill);
  font-size: 0.75rem;
  font-weight: 500;
}
```

**SDE Token CTA banner:**
```css
.token-cta-banner {
  background: var(--primary-container);
  padding: 4rem var(--section-px);
}
.token-cta-inner {
  max-width: var(--max-w);
  margin: 0 auto;
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 2rem;
}
.token-cta-inner h2 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  color: var(--on-primary);
  margin-bottom: 0.5rem;
}
.token-cta-inner p {
  color: rgba(255,255,255,0.8);
}
.token-cta-link {
  display: inline-block;
  padding: 1rem 2.5rem;
  background: var(--amber);
  color: var(--primary);
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  transition: background 0.2s;
}
.token-cta-link:hover { background: #d49830; }
```

**Update donate.html HTML:**
1. Restructure transparency section into `.transparency-grid` > `.transparency-pledge` + fund dashboard
2. Add pledge checklist items with `check_circle` Material Symbols icons
3. Add fund dashboard header bar, metric cards, allocation table, and funding sources
4. Add SDE Token CTA banner at bottom before footer
5. Keep all existing fund-tracker `id` attributes and JS data hooks

**CRITICAL**: The fund-tracker.js module may populate values dynamically. Preserve:
- `id="fund-tracker"` container (if exists)
- `id="total-raised"`, `id="total-spent"`, `id="balance"` (if they exist)
- Any data attributes used by fund-tracker.js
- All `<script>` tags (fund-tracker.js, admin-utils.js, main.js)
  </action>
  <verify>
    <automated>grep -c "transparency-grid" website/css/style.css && grep -c "fund-dashboard" website/css/style.css && grep -c "token-cta-banner" website/css/style.css && grep -c "check_circle" website/donate.html && grep "fund-tracker" website/donate.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.transparency-grid` rule (grep >= 1)
    - style.css has `.fund-dashboard-header` rule (grep >= 1)
    - style.css has `.token-cta-banner` rule (grep >= 1)
    - donate.html has `check_circle` Material Symbol icons in pledge list (grep >= 3)
    - Fund tracker JS integration preserved
    - All script tags intact
  </acceptance_criteria>
  <done>Transparency section has 5/7 grid with pledge card and fund dashboard. SDE Token CTA banner at bottom. All fund-tracker JS preserved.</done>
</task>

</tasks>

<verification>
1. Fund tracker loads with correct values (test with admin mode)
2. Platform cards hover effect works
3. Tier cards show T2 as featured with "Most Popular" badge
4. Transparency pledge shows checklist with icons
5. Token CTA link points to token.html
6. No JS errors
</verification>

<success_criteria>
- Donate page matches Stitch 05-donate design
- Fund tracker dashboard preserved and functional
- All links work (Change.org, Kickstarter, token.html)
- Responsive on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-05-SUMMARY.md`
</output>
