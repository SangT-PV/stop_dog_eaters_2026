---
phase: 17-stitch-design-system-implementation
plan: 07
type: execute
wave: 3
depends_on: ["17-01", "17-02", "17-03", "17-04", "17-05", "17-06"]
files_modified:
  - website/about.html
  - website/css/style.css
  - website/moderate.html
autonomous: false
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "About page applies Stitch design tokens: Newsreader headings, Inter body, MD3 colors, card shadows"
    - "moderate.html applies general Stitch design patterns (tokens, typography, buttons)"
    - "All 7 pages responsive at 375px mobile width (nav collapses, grids stack, text readable)"
    - "All 7 pages pass basic accessibility check (no missing alt text, proper heading hierarchy, focus visible)"
    - "All JS functionality works across all pages (comments, fund-tracker, voting, charts, blog fetching)"
  artifacts:
    - path: "website/about.html"
      provides: "About page with Stitch design tokens applied"
      contains: "material-symbols-outlined"
    - path: "website/css/style.css"
      provides: "Responsive breakpoints and about page styles"
      contains: "@media"
  key_links:
    - from: "website/about.html"
      to: "website/css/style.css"
      via: "stylesheet link"
      pattern: "css/style.css"
---

<objective>
Apply Stitch design to about.html (not in Stitch reference — derive from established patterns), apply general patterns to moderate.html, then run responsive QA across all 7 pages and visual verification.

Purpose: Complete the design system rollout. About page and moderate.html are the remaining pages. Responsive QA ensures mobile users (3G Vietnam) have a good experience.

Output: Updated about.html, moderate.html, responsive CSS rules, and human-verified visual confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/DESIGN-REFERENCE.md
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
@.planning/phases/17-stitch-design-system-implementation/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Stitch design to about.html and moderate.html</name>
  <files>website/about.html, website/moderate.html, website/css/style.css</files>
  <read_first>
    - website/about.html (ALL current HTML structure)
    - website/moderate.html (ALL current HTML if it exists)
    - website/css/style.css (about page rules, moderation rules)
    - .planning/stitch-reference/01-homepage.html (derive about page patterns from homepage)
  </read_first>
  <action>
**About page design (derived from Stitch homepage patterns since Stitch didn't generate an about page):**

The about page has these sections: team intro, Lucky's story, team members, transparency statement. Apply Stitch tokens:

1. **About hero**: Same pattern as petition hero — italic Newsreader headline, eyebrow text, editorial feel:
```css
.about-hero {
  padding: var(--section-py) var(--section-px);
  max-width: var(--max-w);
  margin: 0 auto;
}
.about-hero h1 {
  font-family: var(--font-head);
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 800;
  color: var(--primary);
  line-height: 1.1;
  margin-bottom: 1.5rem;
}
.about-hero p {
  font-size: 1.25rem;
  color: var(--on-surface-variant);
  line-height: 1.7;
  max-width: 42rem;
}
```

2. **Lucky's story section**: Use same pattern as homepage lucky section (surface-container-low bg, split grid, editorial blockquote with border-l-4 tertiary, blur circle decoration)

3. **Team section**: Cards with avatar placeholder, name in Newsreader, role in Inter:
```css
.team-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 2rem;
  max-width: var(--max-w);
  margin: 0 auto;
}
@media (max-width: 768px) {
  .team-grid { grid-template-columns: 1fr 1fr; }
}
.team-card {
  background: var(--surface);
  border-radius: var(--radius-lg);
  padding: 2rem;
  text-align: center;
  box-shadow: var(--shadow-sm);
  transition: transform 0.3s;
}
.team-card:hover { transform: translateY(-4px); }
.team-card .avatar {
  width: 5rem;
  height: 5rem;
  border-radius: 50%;
  background: var(--surface-container-high);
  margin: 0 auto 1.5rem;
  display: flex;
  align-items: center;
  justify-content: center;
}
.team-card h3 {
  font-family: var(--font-head);
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--on-surface);
  margin-bottom: 0.25rem;
}
.team-card .role {
  font-size: 0.875rem;
  color: var(--on-surface-variant);
  margin-bottom: 1rem;
}
.team-card p {
  font-size: 0.875rem;
  color: var(--on-surface-variant);
  line-height: 1.6;
}
```

4. **Transparency section**: Same pattern as donate page transparency pledge (border-l-8 primary, checklist with check_circle icons)

5. **Update about.html HTML:**
- Replace font imports in `<head>` with Newsreader/Inter/Material Symbols (if not already done in Plan 01)
- Apply `.about-hero` to hero section
- Apply team card structure to team members
- Replace any SVG icons with Material Symbols equivalents
- Add `person` Material Symbol in team card avatar circles

**Moderate.html:**
- Update font imports (if not already done)
- Apply general Stitch tokens: background `var(--surface)`, headings in `var(--font-head)`, buttons using `var(--primary)` / `var(--primary-container)` colors
- Update card styles to use `var(--shadow)` and `var(--radius-lg)`
- Keep ALL existing moderation JS hooks and functionality intact

**CRITICAL**: Do not modify any JavaScript functionality. moderate.html has complex moderation workflow — only update visual styles.
  </action>
  <verify>
    <automated>grep -c "about-hero" website/css/style.css && grep -c "team-grid" website/css/style.css && grep -c "Newsreader" website/about.html && grep -c "material-symbols-outlined" website/about.html</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.about-hero` rule (grep >= 1)
    - style.css has `.team-grid` rule (grep >= 1)
    - about.html loads Newsreader font (grep >= 1)
    - about.html has Material Symbols icons (grep >= 1)
    - moderate.html loads Newsreader font (if file exists)
    - All script tags preserved on both pages
  </acceptance_criteria>
  <done>About page and moderate.html use Stitch design tokens. Team cards, editorial layout, and Material Symbols icons applied.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive CSS QA and fix breakpoints across all pages</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (ALL current @media queries)
    - .planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md (responsive is Claude's discretion)
  </read_first>
  <action>
Review and update ALL @media breakpoints in style.css to ensure consistent responsive behavior across the Stitch redesign. The key breakpoints:

**Breakpoints:**
- `max-width: 768px` — Tablet and below
- `max-width: 480px` — Mobile phones

**Nav responsive (768px and below):**
```css
@media (max-width: 768px) {
  .nav-links {
    display: none;
    position: absolute;
    top: 5rem;
    left: 0;
    right: 0;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(16px);
    flex-direction: column;
    padding: 1.5rem;
    box-shadow: var(--shadow-lg);
    gap: 0.5rem;
  }
  .nav-links.open { display: flex; }
  .nav-toggle { display: flex; }
  .nav-links a {
    padding: 0.75rem 1rem;
    width: 100%;
    font-size: 1rem;
  }
}
```

**Hero responsive:**
```css
@media (max-width: 768px) {
  .hero {
    min-height: auto;
    padding: 3rem 1.5rem;
  }
  .hero-inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .hero h1 {
    font-size: 2.5rem;
  }
}
```

**Stats bar responsive:**
```css
@media (max-width: 768px) {
  .stats-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
    text-align: center;
  }
}
```

**Problem, help, blog grids:**
```css
@media (max-width: 768px) {
  .problem-grid,
  .help-grid,
  .blog-grid {
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }
}
```

**Lucky section:**
```css
@media (max-width: 768px) {
  .lucky-inner {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
}
```

**Blog layout (sidebar stacks below):**
```css
@media (max-width: 1023px) {
  .blog-layout {
    grid-template-columns: 1fr;
  }
}
```

**Petition two-column (sign widget stacks above on mobile):**
```css
@media (max-width: 1023px) {
  .petition-content {
    grid-template-columns: 1fr;
  }
  .petition-sign-widget {
    position: static;
    order: -1;
  }
}
```

**Donate platform cards and tier grid:**
```css
@media (max-width: 768px) {
  .platform-cards-grid,
  .tier-grid {
    grid-template-columns: 1fr;
  }
  .tier-card--featured {
    transform: none;
  }
}
```

**Token hero and info grid:**
```css
@media (max-width: 768px) {
  .token-hero-inner,
  .token-info-grid {
    grid-template-columns: 1fr;
  }
}
```

**Footer responsive:**
```css
@media (max-width: 768px) {
  .footer-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  .footer-bottom {
    flex-direction: column;
    text-align: center;
  }
}
```

**Roadmap responsive:**
```css
@media (max-width: 768px) {
  .roadmap-grid {
    grid-template-columns: 1fr;
  }
  .fund-dashboard-grid {
    grid-template-columns: 1fr;
  }
}
```

**Data research responsive:**
```css
@media (max-width: 768px) {
  .data-research-grid {
    grid-template-columns: 1fr;
  }
  .data-charts-grid {
    grid-template-columns: 1fr;
  }
  .stat-callout-grid {
    grid-template-columns: 1fr;
  }
}
```

**Post prose responsive (handle negative margins on blockquote):**
```css
@media (max-width: 768px) {
  .post-prose blockquote {
    margin-left: 0;
    margin-right: 0;
  }
}
```

Also ensure:
- Text doesn't overflow on narrow screens (check headings with large font sizes)
- Touch targets are at least 44x44px
- No horizontal scroll on any page at 375px width
- Existing reduced-motion media query preserved
  </action>
  <verify>
    <automated>grep -c "@media" website/css/style.css && grep -c "max-width: 768px" website/css/style.css && grep -c "max-width: 1023px" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - style.css has multiple @media queries (grep >= 8)
    - style.css has `max-width: 768px` breakpoints for mobile (grep >= 5)
    - style.css has `max-width: 1023px` breakpoints for tablet (grep >= 2)
    - Nav collapses on mobile with .nav-toggle visible
    - All grids stack to single column on mobile
    - No horizontal scrollbar at 375px width
    - Reduced-motion media query still present
  </acceptance_criteria>
  <done>All pages responsive at 375px and 768px breakpoints. Grids stack, nav collapses, text readable, no overflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full Stitch design system across all pages</name>
  <files>website/index.html, website/blog.html, website/post.html, website/petition.html, website/donate.html, website/token.html, website/about.html</files>
  <action>
Human visually verifies the complete Stitch design system implementation across all 7 pages. Start local server and test each page at desktop and mobile (375px) widths. Check for visual correctness, JS functionality, and responsive behavior.
  </action>
  <verify>
    <automated>python -c "import http.server" && echo "Python HTTP server available for testing"</automated>
  </verify>
  <done>Human has approved the visual design across all 7 pages at desktop and mobile widths, with zero JS errors confirmed.</done>
  <what-built>
Full Stitch design system applied across all 7 website pages + moderate.html:
- Plan 01: Design tokens, fonts, icons, nav, footer
- Plan 02: Homepage (hero, stats, cards, lucky, action, blog preview)
- Plan 03: Blog listing (timeline, sidebar) + post detail (editorial prose, comments)
- Plan 04: Petition (editorial hero, arguments, sticky sign widget)
- Plan 05: Donate (platform cards, tiers, fund dashboard)
- Plan 06: Token (hero badge, roadmap milestones, voting)
- Plan 07: About page, moderate.html, responsive QA
  </what-built>
  <how-to-verify>
1. Start local server: `cd website && python -m http.server 8000`
2. Open http://localhost:8000/index.html — verify:
   - Frosted glass nav with Newsreader logo
   - Dark hero with large headline, rotated image placeholder
   - Stats bar with red numbers on light grey bg
   - Problem cards with colored circle icons
   - Action cards that invert on hover
   - Dark footer with 4 columns
3. Check http://localhost:8000/blog.html:
   - Timeline view with dot markers
   - Sidebar with topic pills and stats widget
4. Check http://localhost:8000/post.html?id=2026-03-22-the-hidden-rabies-risk:
   - Editorial prose with Newsreader
   - Comments section working
5. Check http://localhost:8000/petition.html:
   - Sticky sign widget on right
   - Dark demand section
6. Check http://localhost:8000/donate.html:
   - Platform cards, tier cards, fund tracker
7. Check http://localhost:8000/token.html:
   - Token badge hero, roadmap milestones, voting section
8. Check http://localhost:8000/about.html:
   - Team cards, editorial layout
9. Test at 375px width (Chrome DevTools mobile) — all pages should stack gracefully
10. Open Console tab — zero JS errors on all pages
  </how-to-verify>
  <resume-signal>Type "approved" if design looks correct, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All 8 pages load without errors (7 main + moderate.html)
2. Typography is Newsreader/Inter across all pages
3. Material Symbols icons render correctly
4. All JS functionality works (blog fetching, comments, fund-gating, voting, charts)
5. Responsive at 375px — no horizontal scroll, text readable, grids stacked
6. Human visual verification passed
</verification>

<success_criteria>
- All pages match Stitch design patterns
- Responsive on mobile (375px) and tablet (768px)
- Zero JS errors across all pages
- Human visual approval received
- WCAG AA accessibility maintained (heading hierarchy, alt text, focus visible)
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-07-SUMMARY.md`
</output>
