---
phase: 17-stitch-design-system-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/css/style.css
  - website/index.html
  - website/blog.html
  - website/post.html
  - website/petition.html
  - website/donate.html
  - website/token.html
  - website/about.html
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "All pages load Newsreader and Inter fonts from Google Fonts"
    - "All pages load Material Symbols Outlined icon font"
    - "CSS :root contains all MD3 color tokens, new radius, shadow, and spacing variables"
    - "Nav renders with white/frosted-glass background, Newsreader logo, and Material Symbols icon for mobile toggle"
    - "Footer renders with dark primary background, 4-column grid, and social icon circles"
  artifacts:
    - path: "website/css/style.css"
      provides: "Updated :root tokens, nav component, footer component"
      contains: "--primary: #052a2c"
    - path: "website/index.html"
      provides: "Updated font imports and nav/footer HTML"
      contains: "Newsreader"
  key_links:
    - from: "website/index.html"
      to: "fonts.googleapis.com"
      via: "link rel=stylesheet"
      pattern: "Newsreader.*Inter.*Material"
    - from: "website/css/style.css"
      to: "all HTML pages"
      via: "CSS custom properties"
      pattern: "--primary.*--surface.*--on-surface"
---

<objective>
Establish the Stitch design system foundation: update CSS custom properties (colors, typography, spacing, shadows, radii), swap font imports to Newsreader/Inter/Material Symbols on ALL pages, and rebuild the nav and footer components to match Stitch homepage patterns.

Purpose: Every subsequent plan depends on these shared tokens and components being in place. The nav and footer appear on all 7 pages, so they must be done first.

Output: Updated style.css with new design tokens, all HTML pages with correct font/icon imports, new nav and footer matching Stitch design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/DESIGN-REFERENCE.md
@.planning/stitch-reference/01-homepage.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS design tokens and base typography</name>
  <files>website/css/style.css</files>
  <read_first>
    - website/css/style.css (current tokens and structure)
    - .planning/stitch-reference/DESIGN-REFERENCE.md (target token values)
    - .planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Replace the entire :root block in style.css with the new Stitch design tokens. Keep backward-compat aliases but update them to point to new values. Specifically:

**Colors (replace/add all of these in :root):**
```
--primary: #052a2c;
--primary-container: #1d6a72;
--secondary: #006c51;
--tertiary: #540005;
--tertiary-container: #b33023;
--surface: #f9f9f9;
--surface-container-low: #f3f3f3;
--surface-container: #eeeeee;
--surface-container-high: #e8e8e8;
--surface-container-highest: #e2e2e2;
--on-surface: #1a1c1c;
--on-surface-variant: #414848;
--outline: #717879;
--outline-variant: #c1c8c8;
--amber: #e8a838;
--error: #ba1a1a;
--error-container: #ffdad6;
--primary-fixed: #c6e9eb;
--primary-fixed-dim: #aacdcf;
--secondary-container: #86f7ce;
--on-primary: #ffffff;
--on-secondary: #ffffff;
--on-tertiary: #ffffff;
```

**Typography variables (replace):**
```
--font-head: 'Newsreader', Georgia, serif;
--font-body: 'Inter', system-ui, sans-serif;
```

**Spacing and shape (replace/add):**
```
--radius-sm: 0.5rem;
--radius: 1rem;
--radius-md: 1.25rem;
--radius-lg: 2rem;
--radius-xl: 3rem;
--radius-pill: 9999px;
--max-w: 1280px;
--section-py: 6rem;
--section-px: 2rem;
--card-padding: 2.5rem;
```

**Shadows (replace):**
```
--shadow-sm: 0 4px 12px rgba(26,28,28,0.04);
--shadow: 0 24px 48px -12px rgba(26,28,28,0.06);
--shadow-lg: 0 32px 64px -16px rgba(26,28,28,0.12);
--shadow-nav: 0 8px 30px rgb(0,0,0,0.04);
--shadow-hover: 0 20px 40px rgba(26,28,28,0.08);
```

**Backward-compat aliases (keep but update):**
```
--red: var(--tertiary-container);
--red-dk: var(--tertiary);
--teal: var(--primary-container);
--teal-dk: var(--primary);
--slate: var(--primary);
--mist: var(--surface);
--navy: var(--primary);
--offwhite: var(--surface);
--text: var(--on-surface);
--text-md: var(--on-surface-variant);
--text-dark: var(--on-surface);
--border: var(--outline-variant);
--border-dk: var(--outline);
```

**Base body rule**: Update body background to `var(--surface)`, font-family to `var(--font-body)`.

**Heading rule**: Update `h1, h2, h3, h4` to use `font-family: var(--font-head); color: var(--on-surface);` and change font-weight to 800 (Newsreader extrabold).

**Material Symbols base rule (add after body rule):**
```css
.material-symbols-outlined {
  font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
  vertical-align: middle;
  display: inline-flex;
}
```

Do NOT change any component rules in this task (nav, hero, footer, cards etc.) — only the :root tokens and base typography.
  </action>
  <verify>
    <automated>grep -c "\-\-primary: #052a2c" website/css/style.css && grep -c "\-\-font-head: 'Newsreader'" website/css/style.css && grep -c "\.material-symbols-outlined" website/css/style.css && grep -c "\-\-surface: #f9f9f9" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - :root contains `--primary: #052a2c` (grep returns 1)
    - :root contains `--font-head: 'Newsreader', Georgia, serif` (grep returns 1)
    - .material-symbols-outlined rule exists with font-variation-settings (grep returns >= 1)
    - :root contains `--surface: #f9f9f9` (grep returns 1)
    - :root contains `--max-w: 1280px` (grep returns 1)
    - :root contains `--shadow-nav` (grep returns >= 1)
    - Backward-compat alias `--teal: var(--primary-container)` exists
  </acceptance_criteria>
  <done>All CSS custom properties updated to Stitch MD3 tokens. Base typography points to Newsreader/Inter. Material Symbols base class added.</done>
</task>

<task type="auto">
  <name>Task 2: Update font/icon imports on all HTML pages and rebuild nav + footer</name>
  <files>website/index.html, website/blog.html, website/post.html, website/petition.html, website/donate.html, website/token.html, website/about.html, website/css/style.css</files>
  <read_first>
    - website/index.html (current head section and nav/footer structure)
    - website/blog.html (current head section)
    - website/post.html (current head section)
    - website/petition.html (current head section)
    - website/donate.html (current head section)
    - website/token.html (current head section)
    - website/about.html (current head section)
    - .planning/stitch-reference/01-homepage.html (target nav and footer HTML)
    - .planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md (nav and footer design specs)
  </read_first>
  <action>
**Part A: Update Google Fonts import on ALL 7 HTML pages.**

Replace the existing Montserrat/Inter font link in each HTML `<head>` with:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@0,400;0,700;0,800;1,400&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet">
```

Remove the old Montserrat import link. Keep all other `<head>` content (meta, title, og tags, favicon, style.css link) intact.

**Part B: Rebuild the nav component in style.css.**

Replace the `.nav`, `.nav-inner`, `.nav-logo`, `.nav-links`, `.nav-cta`, `.nav-toggle` rules with Stitch nav design:

```css
.nav {
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 100;
  background: rgba(255,255,255,0.85);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  box-shadow: var(--shadow-nav);
}

.nav-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 1.5rem;
  height: 5rem;
  max-width: var(--max-w);
  margin: 0 auto;
}

.nav-logo {
  font-family: var(--font-head);
  font-weight: 900;
  font-size: 1.5rem;
  letter-spacing: -0.04em;
  color: var(--primary);
}
.nav-logo span { color: var(--primary); }
```

Nav links: `font-family: var(--font-head); font-weight: 700; font-size: 1.125rem; color: var(--on-surface-variant);` with `hover:color: var(--primary); transition: color 0.2s;` and active state gets `border-bottom: 2px solid var(--primary); color: var(--primary); padding-bottom: 2px;`.

Nav CTA "Sign Now" button: `background: var(--primary); color: var(--on-primary); border-radius: 0.5rem; font-weight: 700; padding: 0.5rem 1.5rem;` with `active:scale(0.95); transition: all 0.15s;`.

**Part C: Update nav HTML on ALL 7 pages.**

Change `.nav` from dark slate bar to frosted glass. The HTML structure stays the same (nav > nav-inner > logo + links + toggle) but:
- Add `pt-20` equivalent padding to the `<main>` or first section on each page (use `style="padding-top: 5rem;"` or a utility class `.pt-nav { padding-top: 5rem; }` added to style.css)
- The nav is now `position: fixed` (was `sticky`), so content needs top padding to not hide under it

**Part D: Rebuild the footer component in style.css.**

Replace `.footer`, `.footer-grid`, `.footer-brand`, `.footer-col`, `.footer-bottom` rules with Stitch footer:

```css
.footer {
  background: var(--primary);
  color: var(--on-primary);
  padding: 5rem 0 2.5rem;
}

.footer-grid {
  display: grid;
  grid-template-columns: 1.5fr 1fr 1fr 1fr;
  gap: 3rem;
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 2rem;
}

.footer-brand h3 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  font-weight: 700;
  color: var(--on-primary);
  margin-bottom: 1.5rem;
}
.footer-brand h3 span { color: var(--on-primary); }

.footer-brand p {
  color: rgba(161,161,170,1);
  font-size: 0.875rem;
  line-height: 1.7;
}

.footer-col h4 {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.25rem;
  color: var(--on-primary);
  margin-bottom: 1.5rem;
}

.footer-col a {
  color: rgba(161,161,170,1);
  font-size: 0.875rem;
  transition: color 0.2s, transform 0.2s;
  display: inline-block;
}
.footer-col a:hover {
  color: var(--on-primary);
  transform: translateX(4px);
}

.footer-bottom {
  max-width: var(--max-w);
  margin: 5rem auto 0;
  padding: 2rem 2rem 0;
  border-top: 1px solid rgba(255,255,255,0.05);
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  gap: 1rem;
  font-size: 0.75rem;
  color: rgba(113,113,122,1);
}
```

Add social icon circle styles:
```css
.footer-social {
  display: flex;
  gap: 1rem;
  margin-top: 1.5rem;
}
.footer-social a {
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background 0.2s;
}
.footer-social a:hover {
  background: rgba(255,255,255,0.1);
  transform: none;
}
```

**Part E: Update footer HTML on ALL 7 pages.**

Update the footer-brand section to include social icon circles using Material Symbols:
```html
<div class="footer-social">
  <a href="#" aria-label="Website"><span class="material-symbols-outlined">public</span></a>
  <a href="#" aria-label="Telegram"><span class="material-symbols-outlined">send</span></a>
</div>
```

Keep existing footer column links and content intact. Just update the structure to match the 4-column grid.

**CRITICAL**: Do NOT modify any `<script>` tags or JS references. Do NOT modify any section content between nav and footer. Only change `<head>` imports, nav, and footer.
  </action>
  <verify>
    <automated>grep -l "Newsreader" website/index.html website/blog.html website/post.html website/petition.html website/donate.html website/token.html website/about.html | wc -l && grep -l "Material+Symbols" website/index.html website/blog.html website/post.html website/petition.html website/donate.html website/token.html website/about.html | wc -l && grep -c "position: fixed" website/css/style.css && grep -c "backdrop-filter" website/css/style.css</automated>
  </verify>
  <acceptance_criteria>
    - All 7 HTML files contain "Newsreader" in their font import (grep returns 7)
    - All 7 HTML files contain "Material+Symbols" in their font import (grep returns 7)
    - style.css .nav rule contains `position: fixed` (grep returns >= 1)
    - style.css contains `backdrop-filter` for frosted glass nav (grep returns >= 1)
    - style.css .footer rule contains `background: var(--primary)` (grep match)
    - No `<script>` tags removed or modified in any HTML file
    - No Montserrat font imports remain in any HTML file
  </acceptance_criteria>
  <done>All 7 pages import Newsreader, Inter, and Material Symbols. Nav is frosted-glass fixed. Footer is dark primary with 4-col grid and social circles. Pages have top padding to account for fixed nav.</done>
</task>

</tasks>

<verification>
1. Open each page in browser — nav should be white frosted-glass with blur, fixed at top
2. Footer should be dark (#052a2c) with white text and 4 columns
3. All text should render in Newsreader (headings) and Inter (body) — verify via DevTools computed font
4. No JS errors in console (all existing scripts still work)
5. Material Symbols font loaded (check network tab)
</verification>

<success_criteria>
- Design token foundation established in :root with all MD3 colors, spacing, shadows
- Newsreader/Inter/Material Symbols loaded on all 7 pages
- Nav and footer match Stitch design across all pages
- Zero JS breakage — all existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md`
</output>
