---
phase: 17-stitch-design-system-implementation
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - website/index.html
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "Homepage hero is full-width dark (#052a2c) with 12-col grid (7 content / 5 image), 8xl headline, and rotated image placeholder"
    - "Stats bar has dark surface-container-highest bg with 6xl tertiary-colored numbers"
    - "Problem cards are white with colored circle icons, dramatic hover lift translateY(-8px)"
    - "Lucky section has split layout with editorial blockquote border-l-4 tertiary"
    - "Take Action cards invert to primary bg on hover with white text"
    - "Blog preview cards have aspect-video image area with group hover scale"
    - "Transparency banner uses primary-container bg"
  artifacts:
    - path: "website/index.html"
      provides: "Redesigned homepage matching Stitch 01-homepage layout"
      contains: "material-symbols-outlined"
    - path: "website/css/style.css"
      provides: "Updated hero, stats, problem, lucky, action, blog preview component styles"
      contains: "translateY(-8px)"
  key_links:
    - from: "website/index.html"
      to: "website/css/style.css"
      via: "CSS classes"
      pattern: "hero.*stats-bar.*problem-card"
    - from: "website/index.html"
      to: "website/js/main.js"
      via: "script tag"
      pattern: "js/main.js"
    - from: "website/index.html"
      to: "website/js/data-charts.js"
      via: "script tag"
      pattern: "js/data-charts.js"
---

<objective>
Redesign the homepage (index.html) to match the Stitch 01-homepage design. Update hero, stats bar, problem cards, Lucky section, data section, action cards, blog preview, and transparency banner.

Purpose: The homepage is the primary landing page and sets the visual tone for the entire campaign. It must match the Stitch design while preserving all JS functionality (stat counters, chart.js visualizations, fund-roadmap celebration banner).

Output: Fully redesigned index.html with matching CSS component updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/DESIGN-REFERENCE.md
@.planning/stitch-reference/01-homepage.html
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign hero, stats bar, and problem cards</name>
  <files>website/index.html, website/css/style.css</files>
  <read_first>
    - website/index.html (current homepage structure — ALL of it)
    - website/css/style.css (current hero, stats-bar, problem-card rules)
    - .planning/stitch-reference/01-homepage.html (target hero/stats/problem HTML)
  </read_first>
  <action>
**HERO section in style.css — replace existing rules:**

```css
.hero {
  position: relative;
  background: var(--primary);
  overflow: hidden;
  padding: 5rem 2rem;
  min-height: 54rem;
  display: flex;
  align-items: center;
}

.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at 70% 30%, var(--primary-container) 0%, var(--primary) 100%);
  opacity: 0.5;
}

.hero::after { display: none; }

.hero-inner {
  position: relative;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 3rem;
  align-items: center;
  max-width: var(--max-w);
  margin: 0 auto;
  z-index: 10;
}
```

Hero headline: `font-size: clamp(3.5rem, 8vw, 6rem); font-weight: 900; line-height: 1.05; letter-spacing: -0.02em; color: var(--on-primary);`

Add italic accent class for "Not Food": `.hero h1 .hero-accent { color: var(--primary-fixed-dim); font-style: italic; }`

Hero sub: `font-size: 1.25rem; color: rgba(137,171,173,1); line-height: 1.7; max-width: 42rem;`

Hero image placeholder: `border-radius: var(--radius-lg); aspect-ratio: 4/5; transform: rotate(2deg); box-shadow: var(--shadow-lg);`

Add hero image overlay badge:
```css
.hero-image-badge {
  position: absolute;
  bottom: -1.5rem;
  left: -1.5rem;
  background: var(--secondary-container);
  padding: 1.5rem;
  border-radius: 0.5rem;
  box-shadow: var(--shadow-lg);
  max-width: 12.5rem;
  transform: rotate(-3deg);
}
.hero-image-badge p {
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 1.25rem;
  line-height: 1.3;
  color: var(--on-surface);
}
```

Hero buttons: Primary CTA uses `background: var(--tertiary-container); color: var(--on-tertiary); border-radius: 0.5rem; padding: 1rem 2rem; font-size: 1.125rem; box-shadow: var(--shadow-lg); hover: translateY(-2px);`. "Meet Lucky" uses `border: 1px solid var(--outline-variant); color: var(--on-primary); background: transparent; hover: background rgba(255,255,255,0.1);`

**Update hero HTML in index.html:**
- Wrap "Not Food." in `<span class="hero-accent">Not Food.</span>`
- Make hero-image a relative container, add `.hero-image-badge` div inside with text "Lucky's Story: 9 Years of Family"
- Change btn-primary to appropriate class for tertiary-container CTA
- Keep all data-count attributes on stat elements untouched

**STATS BAR in style.css — replace existing rules:**

```css
.stats-bar {
  background: var(--surface-container-highest);
  padding: 4rem 2rem;
  position: relative;
  z-index: 20;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 3rem;
  max-width: var(--max-w);
  margin: 0 auto;
  text-align: center;
}

.stat-item {
  padding: 0;
  text-align: center;
}

.stat-item + .stat-item::before { display: none; }

.stat-number {
  font-family: var(--font-head);
  font-weight: 900;
  font-size: clamp(3rem, 6vw, 3.75rem);
  color: var(--tertiary);
  line-height: 1;
  margin-bottom: 0.5rem;
  letter-spacing: -0.02em;
}

.stat-label {
  font-size: 1.125rem;
  font-weight: 500;
  color: var(--on-surface-variant);
  text-transform: uppercase;
  letter-spacing: 0.15em;
  max-width: none;
}
```

**PROBLEM CARDS in style.css — replace existing rules:**

```css
.problem-card {
  background: var(--surface);
  border: none;
  border-radius: var(--radius-lg);
  padding: var(--card-padding);
  box-shadow: var(--shadow);
  transition: transform 0.3s ease;
  position: relative;
  overflow: visible;
}

.problem-card::before { display: none; }

.problem-card:hover {
  transform: translateY(-8px);
}
```

Add colored circle icon containers:
```css
.card-icon-circle {
  width: 4rem;
  height: 4rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 2rem;
}
.card-icon-circle--error { background: var(--error-container); color: var(--error); }
.card-icon-circle--secondary { background: var(--secondary-container); color: var(--secondary); }
.card-icon-circle--primary { background: var(--primary-fixed); color: var(--primary); }
```

**Update problem cards HTML in index.html:**
Replace SVG icons with Material Symbols inside colored circles:
- Card 1 (Stolen Pets): `<div class="card-icon-circle card-icon-circle--error"><span class="material-symbols-outlined" style="font-size:1.875rem">pets</span></div>`
- Card 2 (Zero Regulation): `<div class="card-icon-circle card-icon-circle--secondary"><span class="material-symbols-outlined" style="font-size:1.875rem">gavel</span></div>`
- Card 3 (Public Health Risk): `<div class="card-icon-circle card-icon-circle--primary"><span class="material-symbols-outlined" style="font-size:1.875rem">medical_services</span></div>`

Add section heading accent bar:
```css
.section-header .accent-bar {
  height: 4px;
  width: 6rem;
  background: var(--tertiary);
  margin-top: 1rem;
}
```

**CRITICAL**: Preserve all `data-count`, `data-suffix` attributes. Preserve all `<script>` tags. Do not remove the celebration-banner div.
  </action>
  <verify>
    <automated>grep -c "hero-accent" website/index.html && grep -c "translateY(-8px)" website/css/style.css && grep -c "material-symbols-outlined" website/index.html && grep -c "surface-container-highest" website/css/style.css && grep "data-count" website/index.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains "hero-accent" class on italic text (grep >= 1)
    - style.css contains `translateY(-8px)` for problem card hover (grep >= 1)
    - index.html contains `material-symbols-outlined` for icon replacements (grep >= 3)
    - style.css references `surface-container-highest` for stats bar (grep >= 1)
    - All data-count attributes preserved in stats and stat-callout sections
    - All script tags preserved (main.js, data-charts.js, chart.js CDN, admin-utils.js, fund-roadmap.js)
  </acceptance_criteria>
  <done>Hero, stats bar, and problem cards match Stitch design. Dark hero with 8xl headline, tertiary stat numbers, white cards with circle icons and dramatic hover.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Lucky section, Data section, Action cards, Blog preview, and Transparency banner</name>
  <files>website/index.html, website/css/style.css</files>
  <read_first>
    - website/index.html (current lucky, data, help, blog sections)
    - website/css/style.css (current lucky, help, blog-card, stat-callout rules)
    - .planning/stitch-reference/01-homepage.html (Stitch target for these sections)
  </read_first>
  <action>
**LUCKY SECTION in style.css — update:**

Background: `var(--surface-container-low)` (not dark slate anymore, per Stitch). Change `.lucky-section { background: var(--surface-container-low); padding: var(--section-py) var(--section-px); overflow: hidden; }`

Lucky inner grid: `gap: 5rem;` and increase grid gap.

Lucky image: Add blur circle decoration behind image:
```css
.lucky-image {
  position: relative;
  border-radius: var(--radius-lg);
  overflow: visible;
}
.lucky-image::after {
  content: '';
  position: absolute;
  top: -2.5rem;
  right: -2.5rem;
  width: 16rem;
  height: 16rem;
  background: var(--secondary-container);
  opacity: 0.2;
  border-radius: 50%;
  filter: blur(3rem);
  z-index: -1;
}
```

Lucky content text colors: Now use dark text since bg is light.
```css
.lucky-content .eyebrow { color: var(--tertiary); font-weight: 700; letter-spacing: 0.15em; text-transform: uppercase; font-size: 0.875rem; }
.lucky-content h2 { color: var(--on-surface); }
.lucky-content p { color: var(--on-surface-variant); }
```

Lucky quote: `border-left: 4px solid var(--tertiary); padding-left: 1.5rem; font-family: var(--font-head); font-style: italic; font-size: 1.875rem; color: var(--primary); line-height: 1.3;`

Lucky CTA button: `background: var(--primary); color: var(--on-primary); border-radius: 0.5rem; display: inline-flex; align-items: center; gap: 0.5rem;`

**Update lucky section HTML:**
- Change "Lucky's Story" eyebrow to "Survivor Spotlight" per Stitch
- Add Material Symbols `arrow_forward` icon inside "Read His Story" button
- Keep image placeholder structure

**DATA & RESEARCH SECTION:**
Update the existing data-research section to use 12-col grid (4-col stat callouts left, 8-col charts right) per Stitch:

```css
.data-research-grid {
  display: grid;
  grid-template-columns: 1fr 2fr;
  gap: 3rem;
}
```

The stat callouts go in a vertical stack on the left. Chart cards go in a 2x1 grid on the right. Update the HTML to wrap stat-callout-grid and data-charts-grid inside a new `.data-research-grid` container.

Stat callout style update: `background: var(--surface-container-low); border-radius: 0.5rem; padding: 2rem; border-left: none;` with number in `color: var(--tertiary);`

**TAKE ACTION (Help) SECTION:**

```css
.help-card {
  background: var(--surface-container-low);
  border: none;
  border-radius: var(--radius-lg);
  padding: 3rem;
  text-align: center;
  transition: all 0.3s ease;
}
.help-card:hover {
  background: var(--primary);
  color: var(--on-primary);
}
.help-card:hover h3,
.help-card:hover p,
.help-card:hover .step-num {
  color: var(--on-primary);
}
```

Step number: Replace circle with large faded number:
```css
.help-card .step-num {
  width: auto;
  height: auto;
  background: none;
  color: var(--on-surface);
  opacity: 0.2;
  font-family: var(--font-head);
  font-size: 3.5rem;
  font-weight: 900;
  margin: 0 auto 1.5rem;
  box-shadow: none;
  display: block;
  border-radius: 0;
  transition: opacity 0.3s;
}
.help-card:hover .step-num { opacity: 0.1; }
```

Format step numbers as "01", "02", "03" in HTML.

Help card button widths: `width: 100%; padding: 1rem; border-radius: 0.5rem;`

**BLOG PREVIEW SECTION:**

Update `.blog-card` to match Stitch:
- Add `overflow: hidden` and `border-radius: var(--radius-lg)`
- Image area: `aspect-ratio: 16/9; overflow: hidden;`
- Image hover: `.blog-card:hover .blog-card-img { transform: scale(1.05); }` with `transition: transform 0.5s;`
- Tag as pill: `border-radius: var(--radius-pill);`
- Card hover: `box-shadow: var(--shadow-hover);`
- Card meta border-top: `border-top: 1px solid var(--outline-variant); padding-top: 1rem;`

Remove the teal left border on `.blog-card`: `border-left: none;`

Update section header to match Stitch: heading on left, "View All News" button with arrow_forward icon on right.

**TRANSPARENCY BANNER:**
Change from teal to primary-container bg:
```css
.section-sm.bg-teal {
  background: var(--primary-container);
  color: var(--on-primary);
  padding: 1.5rem 2rem;
}
```

Add Material Symbols `verified_user` icon before the text in HTML.

**CRITICAL**: Preserve all `id` attributes (data-research, lucky). Preserve all canvas elements and their ids (disease-trend-chart, opinion-chart). Preserve all data-count/data-suffix attributes on stat-callout elements.
  </action>
  <verify>
    <automated>grep -c "help-card:hover" website/css/style.css && grep -c "arrow_forward" website/index.html && grep -c "disease-trend-chart" website/index.html && grep -c "opinion-chart" website/index.html && grep -c "data-count" website/index.html</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.help-card:hover` rule with `background: var(--primary)` (grep >= 1)
    - index.html contains Material Symbols `arrow_forward` icon (grep >= 1)
    - Both chart canvas elements preserved: disease-trend-chart and opinion-chart (grep 1 each)
    - All data-count attributes preserved (grep >= 6)
    - Lucky section now has light background (surface-container-low), not dark slate
    - Help card step numbers display as "01", "02", "03"
    - Blog cards no longer have teal left border
    - All script tags preserved at bottom of page
  </acceptance_criteria>
  <done>All homepage sections redesigned to match Stitch. Lucky section light with editorial blockquote. Action cards hover-invert. Blog preview cards with image zoom. Data section in 4/8 grid. Transparency uses primary-container.</done>
</task>

</tasks>

<verification>
1. Open index.html in browser — visual match to Stitch 01-homepage.html
2. Stat counters animate on load (main.js data-count)
3. Chart.js charts render in Data section
4. Celebration banner still works (admin-utils.js test mode)
5. No console errors
6. All links navigate correctly (petition.html, blog.html, about.html, donate.html)
</verification>

<success_criteria>
- Homepage visually matches Stitch design (hero, stats, cards, lucky, action, blog, footer)
- All JS functionality preserved (stat counters, charts, fund-roadmap, admin-utils)
- Responsive on mobile (test at 375px width)
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-02-SUMMARY.md`
</output>
