---
phase: 17-stitch-design-system-implementation
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - website/blog.html
  - website/post.html
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "Blog listing has 8xl Newsreader headline, timeline/grid view toggle, and sidebar with topic filter pills"
    - "Blog cards in timeline view have left border-l-8 primary accent and timeline dot markers"
    - "Blog post detail has centered prose column at max 680px with serif dropcap first letter"
    - "Comments section preserved with chat bubble style and message input bar"
    - "Teal gradient CTA box present in blog post detail"
    - "Share Your Research form has elevated styling with amber badge"
  artifacts:
    - path: "website/blog.html"
      provides: "Redesigned blog listing matching Stitch 02-blog-listing"
      contains: "material-symbols-outlined"
    - path: "website/post.html"
      provides: "Redesigned post detail matching Stitch 04-blog-post-detail"
      contains: "serif-dropcap"
    - path: "website/css/style.css"
      provides: "Blog listing and post detail component styles"
      contains: "timeline-line"
  key_links:
    - from: "website/blog.html"
      to: "website/js/main.js"
      via: "script tag"
      pattern: "js/main.js"
    - from: "website/post.html"
      to: "website/js/comments.js"
      via: "script tag"
      pattern: "js/comments.js"
---

<objective>
Redesign blog listing (blog.html) and blog post detail (post.html) pages to match Stitch designs 02-blog-listing and 04-blog-post-detail.

Purpose: These are the content hub pages. Blog listing needs the timeline view with sidebar. Post detail needs editorial typography with preserved comment system.

Output: Updated blog.html, post.html, and CSS components for both pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/02-blog-listing.html
@.planning/stitch-reference/04-blog-post-detail.html
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign blog listing page</name>
  <files>website/blog.html, website/css/style.css</files>
  <read_first>
    - website/blog.html (ALL current HTML — understand JS integration points)
    - website/css/style.css (blog-related rules: blog-grid, blog-card, sidebar, timeline, tag system)
    - .planning/stitch-reference/02-blog-listing.html (Stitch target design)
  </read_first>
  <action>
**Blog page layout:** Convert to 12-col grid with 8-col main content + 4-col sidebar.

Add CSS:
```css
.blog-layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: 3rem;
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 var(--section-px);
}
@media (min-width: 1024px) {
  .blog-layout {
    grid-template-columns: 2fr 1fr;
  }
}
```

**Blog header:** Large Newsreader headline:
```css
.blog-page-header h1 {
  font-family: var(--font-head);
  font-size: clamp(3rem, 7vw, 5rem);
  font-weight: 700;
  letter-spacing: -0.02em;
  color: var(--primary);
  line-height: 1;
  margin-bottom: 1.5rem;
}
.blog-page-header p {
  font-size: 1.25rem;
  color: var(--on-surface-variant);
  max-width: 36rem;
  font-weight: 500;
  line-height: 1.7;
}
```

**Timeline/Grid view toggle** (visual only, existing view toggle mechanism stays):
```css
.view-toggle {
  display: inline-flex;
  background: var(--surface-container-low);
  border-radius: var(--radius-pill);
  padding: 0.25rem;
}
.view-toggle .view-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.5rem 1.5rem;
  border-radius: var(--radius-pill);
  font-size: 0.875rem;
  font-weight: 700;
  border: none;
  cursor: pointer;
  background: transparent;
  color: var(--on-surface-variant);
  transition: all 0.2s;
}
.view-toggle .view-btn.active {
  background: var(--primary);
  color: var(--on-primary);
  box-shadow: var(--shadow-sm);
}
```

Add Material Symbols icons to toggle buttons: `view_headline` for timeline, `grid_view` for grid.

**Timeline view posts:**
```css
.timeline-view {
  position: relative;
  padding-left: 3rem;
}
.timeline-view::before {
  content: '';
  position: absolute;
  left: 1.25rem;
  top: 0;
  bottom: 0;
  width: 2px;
  background: var(--outline-variant);
}

.timeline-post-card {
  position: relative;
  margin-bottom: 4rem;
}
.timeline-dot {
  position: absolute;
  left: -3.25rem;
  top: 1.5rem;
  width: 2.5rem;
  height: 2.5rem;
  border-radius: 50%;
  background: var(--primary);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--on-primary);
  box-shadow: 0 0 0 0.5rem var(--surface);
}

.timeline-post-card .blog-post-card {
  background: var(--surface);
  border-radius: var(--radius-lg);
  padding: 2rem;
  box-shadow: var(--shadow);
  border-left: 8px solid var(--primary);
  transition: box-shadow 0.3s;
}
.timeline-post-card .blog-post-card:hover {
  box-shadow: var(--shadow-hover);
}
```

**Sidebar components:**

Topic filter pills:
```css
.sidebar-section {
  background: var(--surface);
  padding: 2rem;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-sm);
  margin-bottom: 2rem;
}
.sidebar-section h4 {
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--on-surface-variant);
  margin-bottom: 1.5rem;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}
```

Filter pills: `border-radius: var(--radius-pill); padding: 0.5rem 1.25rem; font-size: 0.875rem; font-weight: 700;` Active: `background: var(--primary); color: var(--on-primary);` Inactive: `background: var(--surface-container-high); color: var(--on-surface-variant); hover: background var(--primary-container); color var(--on-primary);`

Statistics widget (dark):
```css
.sidebar-stats-widget {
  background: var(--primary);
  color: var(--on-primary);
  padding: 2rem;
  border-radius: var(--radius-lg);
  overflow: hidden;
  position: relative;
}
.sidebar-stats-widget .stat-number {
  font-size: 3rem;
  color: var(--on-primary);
}
```

Telegram CTA card:
```css
.sidebar-telegram {
  background: linear-gradient(135deg, #0088cc, #0077b5);
  color: white;
  padding: 2rem;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  cursor: pointer;
  transition: transform 0.3s;
}
.sidebar-telegram:hover {
  transform: translateY(-4px);
}
```

**Share Your Research form update:**
```css
.community-form-section {
  background: var(--surface-container-low);
  border-radius: var(--radius-lg);
  padding: var(--card-padding);
  position: relative;
  overflow: hidden;
}
```

Add amber "Research Fund Access" badge above form title. Add decorative blur circle in top-right corner using `::before` pseudo-element.

**Update blog.html HTML:**
- Restructure into `.blog-layout` > `.blog-main` + `.blog-sidebar`
- Move existing blog header, timeline/grid toggle, and post container into `.blog-main`
- Move existing sidebar content into `.blog-sidebar`
- Add Material Symbols icons to filter buttons and sidebar headers
- Add view toggle with `view_headline` and `grid_view` icons

**CRITICAL**: Preserve all JS hooks:
- `id="blog-posts"` container
- `id="blog-loading"` loading state
- `id="blog-empty"` empty state
- `id="timeline-posts"` timeline container
- `id="community-post-form"` form
- All sidebar tag filter buttons with existing click handlers
- All `<script>` tags at bottom of page
  </action>
  <verify>
    <automated>grep -c "blog-layout" website/blog.html && grep -c "timeline-view" website/css/style.css && grep -c "sidebar-section" website/css/style.css && grep "blog-posts" website/blog.html | head -1 && grep "community-post-form" website/blog.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - blog.html uses `.blog-layout` wrapper for 2-col grid (grep >= 1)
    - style.css contains `.timeline-view::before` for timeline line (grep >= 1)
    - style.css contains `.sidebar-section` for sidebar card styling (grep >= 1)
    - `id="blog-posts"` preserved in blog.html
    - `id="community-post-form"` preserved in blog.html
    - All script tags preserved (main.js, blog-timeline.js, community-posts.js, feature-voting.js)
    - View toggle buttons have Material Symbols icons
  </acceptance_criteria>
  <done>Blog listing page restructured with 8/4 grid, timeline view with dot markers and border-l-8 cards, styled sidebar with pills/stats/telegram CTA.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign blog post detail page</name>
  <files>website/post.html, website/css/style.css</files>
  <read_first>
    - website/post.html (ALL current HTML — understand comment system integration)
    - website/css/style.css (post detail rules, comment rules, chat-bubble rules)
    - .planning/stitch-reference/04-blog-post-detail.html (Stitch target design)
  </read_first>
  <action>
**Post detail layout:** Centered prose column at 680px max.

```css
.post-header {
  max-width: 64rem;
  margin: 0 auto;
  padding: 0 2rem 3rem;
}
.post-header .back-link {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-size: 0.875rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-weight: 700;
  color: var(--primary-container);
  margin-bottom: 2rem;
  transition: color 0.2s;
}
.post-header .back-link:hover { color: var(--primary); }
```

Post title: `font-family: var(--font-head); font-size: clamp(2.5rem, 5vw, 3.75rem); color: var(--primary); line-height: 1.15; letter-spacing: -0.02em; max-width: 50rem;`

Post meta with author avatar: `display: flex; align-items: center; gap: 1rem; border-top: 1px solid var(--outline-variant); padding-top: 1.5rem; margin-top: 1rem;`

Banner image: `width: 100%; max-width: 80rem; margin: 0 auto 5rem; border-radius: var(--radius-lg); box-shadow: var(--shadow-lg);`

**Article prose:**
```css
.post-prose {
  max-width: 42.5rem;
  margin: 0 auto;
  padding: 0 1.5rem;
  font-family: var(--font-head);
  font-size: 1.125rem;
  line-height: 1.85;
  color: var(--on-surface);
}
```

Dropcap:
```css
.serif-dropcap::first-letter {
  float: left;
  font-size: 5rem;
  line-height: 4rem;
  padding-right: 0.75rem;
  padding-top: 0.25rem;
  font-family: var(--font-head);
  color: var(--primary);
}
```

Blockquote in posts:
```css
.post-prose blockquote {
  margin: 4rem -3rem 4rem -6rem;
  border-left: 4px solid var(--primary-container);
  padding-left: 2rem;
  padding-top: 1rem;
  padding-bottom: 1rem;
}
.post-prose blockquote p {
  font-family: var(--font-head);
  font-size: 1.875rem;
  font-style: italic;
  color: var(--primary-container);
  line-height: 1.35;
}
```

Teal gradient CTA box:
```css
.post-cta-box {
  background: linear-gradient(135deg, var(--primary-container) 0%, var(--primary-fixed) 100%);
  border-radius: var(--radius-lg);
  padding: 2.5rem;
  margin: 4rem 0;
  color: var(--on-primary);
  box-shadow: var(--shadow-lg);
  position: relative;
  overflow: hidden;
}
.post-cta-box::after {
  content: '';
  position: absolute;
  right: -3rem;
  bottom: -3rem;
  width: 16rem;
  height: 16rem;
  background: rgba(255,255,255,0.1);
  border-radius: 50%;
  filter: blur(3rem);
}
.post-cta-box h3 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  color: var(--on-primary);
  margin-bottom: 1rem;
}
.post-cta-box p { color: rgba(255,255,255,0.9); max-width: 31rem; margin-bottom: 2rem; }
.post-cta-box .btn {
  background: var(--on-primary);
  color: var(--primary-container);
  border-radius: 0.5rem;
  box-shadow: var(--shadow);
}
```

**Discussion section heading:** `font-family: var(--font-head); font-size: 1.875rem; color: var(--primary); border-bottom: 1px solid var(--outline-variant); padding-bottom: 1rem; margin-bottom: 3rem;`

**Update post.html HTML:**
- Add "Back to Blog" link with `arrow_back` Material Symbol icon
- Add `.post-header` wrapper around tag + title + meta
- Wrap article body in `.post-prose` class
- Add `.serif-dropcap` class to first paragraph (JS can add this dynamically, or add CSS rule for `.post-prose > p:first-of-type`)
- Update "Sign the Petition" CTA to `.post-cta-box` with teal gradient
- Add Material Symbols icons where appropriate

**Fund-gated discussion section:** Keep existing locked state, progress bar, and chat bubble comments HTML EXACTLY as-is. Only update the section heading style.

**CRITICAL**: Preserve ALL of these:
- `id="post-content"` container
- `id="comments-section"`
- `id="comments-container"`
- `id="comment-form"`
- `id="fund-progress-bar"` / `id="fund-gate"`
- All `<script>` tags (comments.js, fund-roadmap.js, admin-utils.js, main.js)
  </action>
  <verify>
    <automated>grep -c "post-prose" website/css/style.css && grep -c "serif-dropcap" website/css/style.css && grep -c "post-cta-box" website/css/style.css && grep "comments-container" website/post.html | head -1 && grep "comment-form" website/post.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - style.css contains `.post-prose` class with max-width 42.5rem (grep >= 1)
    - style.css contains `.serif-dropcap::first-letter` rule (grep >= 1)
    - style.css contains `.post-cta-box` with gradient background (grep >= 1)
    - `id="comments-container"` preserved in post.html
    - `id="comment-form"` preserved in post.html
    - All script tags preserved (comments.js, fund-roadmap.js, admin-utils.js, main.js)
    - Back link has Material Symbols arrow_back icon
  </acceptance_criteria>
  <done>Blog post detail page has centered 680px prose, serif dropcap, editorial blockquotes, teal gradient CTA, and preserved comment system.</done>
</task>

</tasks>

<verification>
1. Blog listing: posts load from data/index.json, tag filters work, timeline view shows dots, sidebar renders
2. Blog post detail: content loads from data/posts/*.json, comments section works, fund-gating works
3. No JS console errors on either page
4. Typography is Newsreader for headings, Inter for body text
</verification>

<success_criteria>
- Blog listing matches Stitch 02-blog-listing design (timeline, sidebar, filters)
- Blog post matches Stitch 04-blog-post-detail design (editorial prose, dropcap, CTA box)
- All JS functionality preserved (blog fetching, comments, fund-gating, community posts, voting)
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-03-SUMMARY.md`
</output>
