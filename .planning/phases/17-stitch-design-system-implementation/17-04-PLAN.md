---
phase: 17-stitch-design-system-implementation
plan: 04
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - website/petition.html
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "Petition page has dramatic editorial headline with italic styling"
    - "Two-column layout: evidence/arguments left, sticky sign widget right"
    - "Sign widget has signature count, progress bar, form inputs with bottom-border style"
    - "Three evidence sections with border-l-4 accents and editorial typography"
    - "Our Demand section has dark primary background with numbered demands"
    - "Share section at bottom with social share buttons"
  artifacts:
    - path: "website/petition.html"
      provides: "Redesigned petition page matching Stitch 03-petition"
      contains: "material-symbols-outlined"
    - path: "website/css/style.css"
      provides: "Petition page component styles"
      contains: "petition-hero"
  key_links:
    - from: "website/petition.html"
      to: "website/js/main.js"
      via: "script tag"
      pattern: "js/main.js"
---

<objective>
Redesign the petition page (petition.html) to match Stitch 03-petition design. Create editorial-style hero with dramatic headline, two-column arguments + sticky sign widget layout, and share section.

Purpose: The petition is the primary call-to-action page. The Stitch editorial design adds gravitas and urgency to the signing experience.

Output: Updated petition.html and CSS petition components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/03-petition.html
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign petition hero and two-column layout</name>
  <files>website/petition.html, website/css/style.css</files>
  <read_first>
    - website/petition.html (ALL current HTML — understand form structure and JS hooks)
    - website/css/style.css (petition-related rules)
    - .planning/stitch-reference/03-petition.html (Stitch target design)
  </read_first>
  <action>
**Petition page layout:**

```css
.petition-hero {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 2rem var(--section-px) 4rem;
}
.petition-hero .eyebrow {
  font-size: 0.6875rem;
  letter-spacing: 0.2rem;
  color: var(--primary-container);
  font-weight: 700;
  text-transform: uppercase;
  margin-bottom: 1rem;
}
.petition-hero h1 {
  font-family: var(--font-head);
  font-size: clamp(2.5rem, 6vw, 4.5rem);
  font-weight: 800;
  font-style: italic;
  color: var(--primary);
  line-height: 1.1;
  letter-spacing: -0.02em;
  margin-bottom: 2rem;
}
.petition-targets {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  align-items: center;
}
.petition-targets .target-badge {
  background: var(--primary);
  color: var(--on-primary);
  padding: 0.25rem 1rem;
  font-size: 0.75rem;
  font-weight: 700;
}
.petition-targets .target-badge--secondary {
  background: var(--primary-container);
}
```

**Two-column main content:**
```css
.petition-content {
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 var(--section-px);
  display: grid;
  grid-template-columns: 1fr;
  gap: 4rem;
}
@media (min-width: 1024px) {
  .petition-content {
    grid-template-columns: 7fr 5fr;
  }
}
```

**Evidence sections (left column):**
```css
.petition-evidence {
  space-y: 4rem;
}
.petition-section {
  margin-bottom: 4rem;
}
.petition-section h2 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 1.5rem;
}
.petition-section--bordered h2 {
  border-left: 4px solid var(--tertiary-container);
  padding-left: 1.5rem;
}
.petition-section--bordered-teal h2 {
  border-left: 4px solid var(--primary-container);
  padding-left: 1.5rem;
}
.petition-section p {
  font-size: 1.125rem;
  line-height: 1.7;
  color: var(--on-surface-variant);
  text-align: justify;
}
.petition-section strong { color: var(--primary); }
```

Evidence background variant:
```css
.petition-section--bg {
  background: var(--surface-container-low);
  padding: 2.5rem;
  margin-left: -2.5rem;
  margin-right: -2.5rem;
}
```

**Our Demand section (dark):**
```css
.petition-demand {
  background: var(--primary);
  color: var(--on-primary);
  padding: 3rem;
  margin-bottom: 4rem;
}
.petition-demand h2 {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-style: italic;
  color: var(--on-primary);
  border-bottom: 1px solid var(--primary-container);
  padding-bottom: 1rem;
  margin-bottom: 2rem;
}
.petition-demand-list {
  list-style: none;
  padding: 0;
}
.petition-demand-list li {
  display: flex;
  gap: 1rem;
  align-items: flex-start;
  margin-bottom: 1.5rem;
}
.petition-demand-list .demand-num {
  background: var(--primary-container);
  color: var(--on-primary);
  padding: 0.125rem 0.5rem;
  font-size: 0.75rem;
  font-weight: 700;
  flex-shrink: 0;
  margin-top: 0.25rem;
}
.petition-demand-list li p {
  font-size: 1.125rem;
  font-weight: 500;
  color: var(--on-primary);
  line-height: 1.6;
}
```

**Sticky sign widget (right column):**
```css
.petition-sign-widget {
  position: sticky;
  top: 7rem;
}
.petition-sign-card {
  background: var(--surface-container-highest);
  padding: 0.25rem;
}
.petition-sign-inner {
  background: var(--surface);
  padding: 2rem;
}
.petition-sign-count {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-weight: 900;
  color: var(--primary);
}
.petition-sign-goal {
  font-size: 0.6875rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--outline);
}
.petition-progress {
  width: 100%;
  height: 0.75rem;
  background: var(--surface-container);
  margin: 0.5rem 0 1rem;
}
.petition-progress-fill {
  height: 100%;
  background: var(--primary-container);
}
```

Form inputs (bottom-border style):
```css
.petition-form input,
.petition-form select {
  width: 100%;
  border: 0;
  border-bottom: 2px solid var(--outline);
  background: transparent;
  padding: 0.75rem 0;
  font-family: var(--font-body);
  font-size: 1rem;
  font-weight: 500;
  color: var(--primary);
  transition: border-color 0.2s;
  outline: none;
}
.petition-form input:focus,
.petition-form select:focus {
  border-color: var(--primary-container);
}
.petition-form label {
  display: block;
  font-size: 0.6875rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-weight: 700;
  color: var(--outline);
  margin-bottom: 0.25rem;
}
```

Submit button:
```css
.petition-submit {
  width: 100%;
  background: var(--tertiary-container);
  color: var(--on-tertiary);
  font-weight: 900;
  padding: 1rem;
  text-transform: uppercase;
  letter-spacing: 0.2rem;
  font-size: 0.875rem;
  border: none;
  cursor: pointer;
  transition: all 0.2s;
}
.petition-submit:hover {
  background: var(--tertiary);
  color: var(--on-tertiary);
}
```

**Share section at bottom:**
```css
.petition-share {
  background: var(--surface-container-highest);
  padding: 4rem var(--section-px);
  text-align: center;
  margin-top: 6rem;
}
.petition-share h3 {
  font-family: var(--font-head);
  font-size: 1.875rem;
  font-weight: 700;
  font-style: italic;
  color: var(--primary);
  margin-bottom: 2rem;
}
.petition-share-buttons {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 1.5rem;
}
.petition-share-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.75rem;
  background: var(--surface);
  padding: 0.75rem 2rem;
  font-weight: 700;
  border: 1px solid rgba(0,0,0,0.1);
  cursor: pointer;
  transition: all 0.2s;
}
.petition-share-btn:hover {
  background: var(--primary);
  color: var(--on-primary);
}
```

**Update petition.html HTML:**
1. Add petition hero with italic headline, eyebrow "Official Mandate", and target badges
2. Restructure body into `.petition-content` > left evidence column + right sign widget
3. Evidence column: 3 argument sections with editorial text, plus "Our Demand" dark section with 4 numbered demands
4. Right column: Sticky sign widget with progress bar, form, and CTA
5. Add share section at bottom with Facebook, X (Twitter), WhatsApp buttons using Material Symbols (share, crossword, send)

**CRITICAL**: Preserve the existing petition form's `id` attributes and the JS form handler in main.js. Keep the form `id="petition-form"` if it exists. Keep all `<script>` tags.
  </action>
  <verify>
    <automated>grep -c "petition-hero" website/css/style.css && grep -c "petition-content" website/css/style.css && grep -c "petition-sign-widget" website/css/style.css && grep -c "petition-demand" website/css/style.css && grep -c "material-symbols-outlined" website/petition.html</automated>
  </verify>
  <acceptance_criteria>
    - style.css contains `.petition-hero` rule (grep >= 1)
    - style.css contains `.petition-content` two-column grid (grep >= 1)
    - style.css contains `.petition-sign-widget` sticky positioning (grep >= 1)
    - style.css contains `.petition-demand` dark section (grep >= 1)
    - petition.html contains Material Symbols icons (grep >= 3)
    - Petition form functionality preserved (form inputs exist)
    - All script tags preserved (main.js)
    - Share section at bottom with social buttons
  </acceptance_criteria>
  <done>Petition page redesigned with editorial hero, two-column evidence + sticky sign widget, dark demand section, and share buttons.</done>
</task>

</tasks>

<verification>
1. Petition form submits correctly (existing JS handler)
2. Sticky sign widget stays visible while scrolling left column
3. Progress bar renders correctly
4. Share buttons display with icons
5. No console errors
</verification>

<success_criteria>
- Petition page matches Stitch 03-petition design
- Form submission still works via main.js
- Sticky widget visible during scroll
- Responsive on mobile (form stacks above content)
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-04-SUMMARY.md`
</output>
