---
phase: 17-stitch-design-system-implementation
plan: 06
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - website/token.html
  - website/css/style.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "Token hero has dark gradient background with $SDE badge card showing symbol, network, and launch phase"
    - "Amber disclaimer banner below hero with warning icon"
    - "Two-column info grid: Why a Token (left) + Cycle of Impact steps (right)"
    - "Fund Transparency Dashboard on dark primary-container bg with doughnut chart, progress bars, recent expenses"
    - "Fund-gated roadmap with 4 milestone cards (completed, active, locked states)"
    - "Feature voting section preserved with locked/unlocked state"
  artifacts:
    - path: "website/token.html"
      provides: "Redesigned token page matching Stitch 06-token"
      contains: "material-symbols-outlined"
    - path: "website/css/style.css"
      provides: "Token page component styles"
      contains: "token-hero"
  key_links:
    - from: "website/token.html"
      to: "website/js/fund-tracker.js"
      via: "script tag"
      pattern: "js/fund-tracker.js"
    - from: "website/token.html"
      to: "website/js/fund-roadmap.js"
      via: "script tag"
      pattern: "js/fund-roadmap.js"
    - from: "website/token.html"
      to: "website/js/feature-voting.js"
      via: "script tag"
      pattern: "js/feature-voting.js"
---

<objective>
Redesign the token page (token.html) to match Stitch 06-token design. Update hero with $SDE badge card, disclaimer banner, info grid, fund dashboard, roadmap milestones, and feature voting section.

Purpose: The token page is the crypto fundraising hub. It must look professional and trustworthy with transparent financial data and clear roadmap.

Output: Updated token.html and CSS token components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-stitch-design-system-implementation/17-CONTEXT.md
@.planning/stitch-reference/06-token.html
@.planning/phases/17-stitch-design-system-implementation/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign token hero, disclaimer, and info grid</name>
  <files>website/token.html, website/css/style.css</files>
  <read_first>
    - website/token.html (ALL current HTML — understand JS integration for fund-tracker, roadmap, voting)
    - website/css/style.css (token-related rules)
    - .planning/stitch-reference/06-token.html (Stitch target design)
  </read_first>
  <action>
**Token hero (dark gradient):**
```css
.token-hero {
  background: linear-gradient(135deg, var(--primary) 0%, rgba(26,37,64,1) 100%);
  padding: var(--section-py) var(--section-px);
  position: relative;
  overflow: hidden;
}
.token-hero::before {
  content: '';
  position: absolute;
  inset: 0;
  opacity: 0.1;
  background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.1'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
}
.token-hero-inner {
  max-width: var(--max-w);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  position: relative;
  z-index: 10;
}
@media (max-width: 768px) {
  .token-hero-inner { grid-template-columns: 1fr; }
}
.token-hero-content {
  color: var(--on-primary);
}
.token-hero-badge {
  display: inline-block;
  padding: 0.375rem 1rem;
  border-radius: var(--radius-pill);
  background: var(--amber);
  color: var(--primary);
  font-size: 0.875rem;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  margin-bottom: 2rem;
}
.token-hero h1 {
  font-family: var(--font-head);
  font-size: clamp(3rem, 6vw, 4.5rem);
  font-weight: 500;
  color: var(--on-primary);
  line-height: 1.15;
  margin-bottom: 1.5rem;
}
.token-hero h1 .accent {
  font-style: italic;
  font-weight: 700;
  color: var(--primary-fixed);
}
.token-hero p {
  color: rgba(137,171,173,1);
  font-size: 1.125rem;
  line-height: 1.7;
  max-width: 36rem;
  margin-bottom: 2rem;
}
```

Token badge card (right side):
```css
.token-badge-card {
  background: var(--primary-container);
  padding: 3rem;
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  border: 1px solid rgba(255,255,255,0.05);
  position: relative;
}
.token-badge-card::before {
  content: '';
  position: absolute;
  inset: -1rem;
  background: rgba(0,108,81,0.2);
  filter: blur(3rem);
  border-radius: 50%;
  opacity: 0.5;
  z-index: -1;
}
.token-badge-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 2rem;
}
.token-icon {
  width: 4rem;
  height: 4rem;
  background: var(--on-primary);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}
.token-icon .material-symbols-outlined {
  color: var(--primary);
  font-size: 2.5rem;
}
.token-network-info {
  text-align: right;
  color: rgba(137,171,173,1);
}
.token-network-info .label {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
}
.token-network-info .value {
  color: var(--on-primary);
  font-weight: 700;
}
.token-symbol {
  font-family: var(--font-head);
  font-size: 3.5rem;
  font-weight: 700;
  color: var(--on-primary);
  letter-spacing: -0.04em;
  margin-top: 0.5rem;
}
.token-meta-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  margin-top: 2rem;
  padding-top: 2rem;
  border-top: 1px solid rgba(255,255,255,0.1);
}
.token-meta-grid .label {
  font-size: 0.625rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: rgba(137,171,173,1);
}
.token-meta-grid .value {
  color: var(--on-primary);
  font-weight: 600;
}
```

**Amber disclaimer banner:**
```css
.token-disclaimer {
  background: var(--amber);
  padding: 1rem var(--section-px);
}
.token-disclaimer-inner {
  max-width: var(--max-w);
  margin: 0 auto;
  display: flex;
  align-items: center;
  gap: 1rem;
  color: var(--primary);
}
.token-disclaimer p {
  font-size: 0.875rem;
  font-weight: 500;
  line-height: 1.4;
  color: var(--primary);
}
.token-disclaimer strong { font-weight: 700; }
```

**Two-column info grid:**
```css
.token-info-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 6rem;
  max-width: var(--max-w);
  margin: 0 auto;
  padding: var(--section-py) var(--section-px);
}
@media (max-width: 768px) {
  .token-info-grid { grid-template-columns: 1fr; gap: 3rem; }
}
```

Why a Token section:
```css
.token-why h2 {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 1.5rem;
}
.token-why p {
  color: var(--on-surface-variant);
  font-size: 1.125rem;
  line-height: 1.7;
  margin-bottom: 3rem;
}
.token-specs {
  background: var(--surface-container-low);
  border-radius: var(--radius-lg);
  padding: 2rem;
}
.token-specs h3 {
  font-size: 0.875rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--on-surface-variant);
  opacity: 0.6;
  margin-bottom: 1.5rem;
}
.token-spec-row {
  display: flex;
  justify-content: space-between;
  padding: 0.75rem 0;
  border-bottom: 1px solid var(--outline-variant);
}
.token-spec-row:last-child { border-bottom: none; }
.token-spec-row .spec-label { color: var(--on-surface-variant); font-weight: 500; }
.token-spec-row .spec-value { color: var(--primary); font-weight: 700; }
```

Cycle of Impact (vertical timeline steps):
```css
.token-steps {
  position: relative;
}
.token-steps h2 {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 3rem;
}
.token-step {
  display: flex;
  gap: 2rem;
  position: relative;
  margin-bottom: 2rem;
}
.token-step-num {
  width: 3rem;
  height: 3rem;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  color: var(--on-primary);
  flex-shrink: 0;
  box-shadow: var(--shadow-sm);
  z-index: 10;
}
.token-step:nth-child(1) .token-step-num { background: var(--primary); }
.token-step:nth-child(2) .token-step-num { background: var(--primary-container); }
.token-step:nth-child(3) .token-step-num { background: var(--primary-fixed-dim); color: var(--primary); }
.token-step:nth-child(4) .token-step-num { background: var(--surface-container-highest); color: var(--primary); }

.token-steps .steps-container {
  position: relative;
}
.token-steps .steps-container::before {
  content: '';
  position: absolute;
  left: 1.5rem;
  top: 1rem;
  bottom: 1rem;
  width: 1px;
  background: var(--outline-variant);
}
.token-step h4 {
  font-family: var(--font-head);
  font-size: 1.25rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}
.token-step p { color: var(--on-surface-variant); }
```

**Update token.html HTML:**
1. Replace hero with `.token-hero` containing left content + right `.token-badge-card`
2. Add disclaimer banner with `warning` Material Symbol icon (filled)
3. Add two-column info grid: Why section with specs table, Impact steps with numbered circles
4. Add Material Symbols: `pets` for token icon, `arrow_forward` for CTA button, `warning` for disclaimer

**CRITICAL**: Preserve all existing sections below the info grid:
- Fund tracker dashboard (keep all `id` attributes)
- Fund-gated roadmap (keep all milestone elements)
- Feature voting section (keep all voting elements)
- Celebration banner div
- All `<script>` tags (fund-tracker.js, fund-roadmap.js, feature-voting.js, admin-utils.js, main.js)
  </action>
  <verify>
    <automated>grep -c "token-hero" website/css/style.css && grep -c "token-badge-card" website/css/style.css && grep -c "token-disclaimer" website/css/style.css && grep -c "token-info-grid" website/css/style.css && grep "fund-roadmap.js" website/token.html | head -1 && grep "feature-voting.js" website/token.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.token-hero` with gradient background (grep >= 1)
    - style.css has `.token-badge-card` (grep >= 1)
    - style.css has `.token-disclaimer` with amber background (grep >= 1)
    - style.css has `.token-info-grid` two-column layout (grep >= 1)
    - fund-roadmap.js script tag preserved in token.html
    - feature-voting.js script tag preserved in token.html
    - All fund tracker, roadmap, and voting section HTML preserved
  </acceptance_criteria>
  <done>Token hero, disclaimer, and info grid match Stitch design. All JS-driven sections (fund tracker, roadmap, voting) preserved intact.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle fund dashboard, roadmap milestones, and voting section</name>
  <files>website/token.html, website/css/style.css</files>
  <read_first>
    - website/token.html (fund tracker, roadmap, voting sections)
    - website/css/style.css (fund-roadmap, feature-voting styles)
    - .planning/stitch-reference/06-token.html (Stitch fund dashboard and roadmap)
  </read_first>
  <action>
**Fund Transparency Dashboard (dark bg):**

Update existing fund-tracker section wrapper to use dark primary-container background:
```css
.fund-dashboard-section {
  background: var(--primary-container);
  padding: var(--section-py) var(--section-px);
}
.fund-dashboard-section h2 {
  font-family: var(--font-head);
  font-size: clamp(2.5rem, 4vw, 3.5rem);
  font-weight: 700;
  color: var(--on-primary);
  margin-bottom: 1rem;
}
.fund-dashboard-section > p {
  color: rgba(137,171,173,1);
  font-size: 1.125rem;
  max-width: 42rem;
  margin-bottom: 4rem;
}
```

Summary metric cards (in top-right):
```css
.fund-summary-cards {
  display: flex;
  gap: 1rem;
}
.fund-summary-card {
  background: rgba(255,255,255,0.05);
  border: 1px solid rgba(255,255,255,0.1);
  padding: 1rem 1.5rem;
  border-radius: var(--radius-lg);
  text-align: center;
  min-width: 8.75rem;
}
.fund-summary-card .label {
  font-size: 0.625rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: rgba(137,171,173,1);
  margin-bottom: 0.25rem;
}
.fund-summary-card .value {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--on-primary);
}
.fund-summary-card--accent {
  background: rgba(0,108,81,0.2);
  border-color: rgba(0,108,81,0.3);
}
```

Dashboard 3-column grid (chart + progress bars + recent expenses):
```css
.fund-dashboard-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
  max-width: var(--max-w);
  margin: 0 auto;
}
@media (max-width: 768px) {
  .fund-dashboard-grid { grid-template-columns: 1fr; }
}
.fund-dashboard-card {
  background: rgba(255,255,255,0.05);
  border-radius: var(--radius-lg);
  padding: 2rem;
  border: 1px solid rgba(255,255,255,0.05);
}
.fund-dashboard-card h3 {
  color: var(--on-primary);
  font-size: 0.875rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  margin-bottom: 2rem;
}
```

**Fund-gated roadmap milestones (4-col grid):**

Update existing roadmap styling:
```css
.roadmap-section {
  padding: var(--section-py) var(--section-px);
  max-width: var(--max-w);
  margin: 0 auto;
  text-align: center;
}
.roadmap-section h2 {
  font-family: var(--font-head);
  font-size: clamp(2.5rem, 4vw, 3.5rem);
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 1.5rem;
}
.roadmap-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 1.5rem;
  text-align: left;
}
@media (max-width: 768px) {
  .roadmap-grid { grid-template-columns: 1fr 1fr; }
}
```

Milestone card states:
```css
.milestone-card {
  background: var(--surface-container-low);
  border-radius: var(--radius-lg);
  padding: 1.5rem;
  border-bottom: 4px solid var(--outline-variant);
  position: relative;
  transition: transform 0.2s;
}
.milestone-card--completed {
  opacity: 0.6;
  border-bottom-color: var(--primary-container);
}
.milestone-card--active {
  background: var(--surface);
  border-bottom-color: var(--amber);
  box-shadow: var(--shadow-lg);
  transform: scale(1.05);
  z-index: 20;
  ring: 1px solid rgba(var(--amber), 0.2);
}
.milestone-card--locked {
  opacity: 0.4;
}
.milestone-amount {
  font-family: var(--font-head);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 0.25rem;
}
.milestone-status {
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  margin-bottom: 1rem;
}
.milestone-card--completed .milestone-status { color: var(--primary-container); }
.milestone-card--active .milestone-status { color: var(--amber); }
.milestone-card--locked .milestone-status { color: var(--outline); }
```

**Feature voting section (frosted glass when locked):**
```css
.voting-section {
  padding: var(--section-py) var(--section-px);
  background: var(--surface-container-high);
  position: relative;
  overflow: hidden;
}
.voting-glass-card {
  max-width: var(--max-w);
  margin: 0 auto;
  background: rgba(255,255,255,0.4);
  backdrop-filter: blur(12px);
  border-radius: var(--radius-xl);
  padding: 4rem;
  text-align: center;
  border: 1px solid rgba(255,255,255,0.6);
  box-shadow: var(--shadow);
}
.voting-glass-card .voting-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 5rem;
  height: 5rem;
  background: var(--primary);
  color: var(--on-primary);
  border-radius: 50%;
  margin-bottom: 2rem;
}
.voting-glass-card h2 {
  font-family: var(--font-head);
  font-size: 2.5rem;
  font-weight: 700;
  color: var(--primary);
  margin-bottom: 1rem;
}
.voting-glass-card > p {
  color: var(--on-surface-variant);
  font-size: 1.125rem;
  max-width: 36rem;
  margin: 0 auto 2.5rem;
}
```

**Update token.html HTML:**
1. Add dark `.fund-dashboard-section` wrapper around existing fund tracker content
2. Update roadmap to use `.roadmap-grid` with `.milestone-card` states
3. Update voting section to use `.voting-glass-card` frosted glass container
4. Replace existing icon references with Material Symbols where appropriate: `check_circle` (completed), `lock` (locked), `how_to_vote` (voting icon)

**CRITICAL**: Preserve ALL JavaScript integration points:
- Fund tracker `id` attributes
- Roadmap milestone `id` attributes and data attributes used by fund-roadmap.js
- Voting section `id` attributes and button click handlers from feature-voting.js
- Celebration banner
- All `<script>` tags
  </action>
  <verify>
    <automated>grep -c "fund-dashboard-section" website/css/style.css && grep -c "milestone-card--active" website/css/style.css && grep -c "voting-glass-card" website/css/style.css && grep "feature-voting" website/token.html | head -1 && grep "fund-roadmap" website/token.html | head -1</automated>
  </verify>
  <acceptance_criteria>
    - style.css has `.fund-dashboard-section` with dark bg (grep >= 1)
    - style.css has `.milestone-card--active` with amber border and scale (grep >= 1)
    - style.css has `.voting-glass-card` with backdrop-filter (grep >= 1)
    - feature-voting.js script tag preserved
    - fund-roadmap.js script tag preserved
    - All fund tracker and voting JS hooks preserved
  </acceptance_criteria>
  <done>Fund dashboard on dark bg, roadmap milestones with completed/active/locked states, voting section with frosted glass. All JS preserved.</done>
</task>

</tasks>

<verification>
1. Fund tracker loads values correctly
2. Roadmap shows correct milestone states (completed, active, locked)
3. Feature voting buttons work
4. Token badge card displays in hero
5. Disclaimer banner visible with amber bg
6. No JS console errors
</verification>

<success_criteria>
- Token page matches Stitch 06-token design
- All JS functionality preserved (fund-tracker, fund-roadmap, feature-voting)
- Dark dashboard renders correctly
- Responsive on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/17-stitch-design-system-implementation/17-06-SUMMARY.md`
</output>
